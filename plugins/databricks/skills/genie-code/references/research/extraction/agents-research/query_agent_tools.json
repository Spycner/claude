[
  {
    "type": "function",
    "function": {
      "name": "getQueryProfiles",
      "description": "Get the query profile for a specific statement. Use to analyze the performance and execution details of the query. Extract the statement ID (UUID) from the user message.",
      "parameters": {
        "type": "object",
        "properties": {
          "statementId": {
            "type": "string",
            "description": "The statement ID (UUID) to get the query profile for. Extract this from the user message when they provide a query ID like \"/analyze Query <UUID>\"."
          }
        },
        "required": [
          "statementId"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "renameTitle",
      "description": "Rename the title of the current asset you are working on (notebook or SQL query). Use this when the user asks to rename the current file or when you want to suggest a more descriptive title.",
      "parameters": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "description": "New title for the current asset (notebook or SQL query)"
          }
        },
        "required": [
          "title"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "addQueryParameter",
      "description": "Add a new query parameter (widget) to the current notebook or SQL query. Parameters allow users to provide input values at runtime using :param_name or {{param_name}} syntax in SQL. Choose the appropriate type: \"text\" for free-form input, \"dropdown\" for single selection, \"combobox\" for selection with free-form option, \"multiselect\" for multiple selections.",
      "parameters": {
        "type": "object",
        "properties": {
          "param_type": {
            "type": "string",
            "enum": [
              "text",
              "dropdown",
              "combobox",
              "multiselect"
            ],
            "description": "The type of parameter widget to create. \"text\" for free-form input, \"dropdown\" for single selection from choices, \"combobox\" for selection OR free-form entry, \"multiselect\" for multiple selections from choices."
          },
          "name": {
            "type": "string",
            "description": "The programmatic name of the parameter. Used to reference it in SQL queries with :name or {{name}} syntax."
          },
          "default_value": {
            "type": "string",
            "description": "The default value for the parameter. For dropdown/combobox/multiselect, this must be one of the choices."
          },
          "label": {
            "type": "string",
            "description": "The display label shown to users in the parameter widget. Use the same value as \"name\" if no custom label is needed."
          },
          "choices": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of choices for dropdown, combobox, or multiselect types. Pass an empty array [] for \"text\" type. MUST be non-empty for dropdown/combobox/multiselect."
          }
        },
        "required": [
          "param_type",
          "name",
          "default_value",
          "label",
          "choices"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "deleteQueryParameter",
      "description": "Delete an existing query parameter (widget) from the current notebook or SQL query. Use this when the user wants to remove a parameter or when you need to replace an existing parameter with a new configuration.",
      "parameters": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "The programmatic name of the parameter to delete."
          }
        },
        "required": [
          "name"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "modifyQueryParameterValue",
      "description": "Modify the current runtime value of an existing query parameter. This changes the value being used when the query runs, NOT the default value. For dropdown/combobox/multiselect parameters, the new value must be one of the valid choices.",
      "parameters": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "The programmatic name of the parameter to modify."
          },
          "new_value": {
            "type": "string",
            "description": "The new runtime value for the parameter. For dropdown/combobox/multiselect parameters, this must be one of the valid choices."
          }
        },
        "required": [
          "name",
          "new_value"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "getSqlSample",
      "description": "Get sample data from a SQL table or files by running a SELECT query with LIMIT. For tables: use tableName (fully qualified as catalog.schema.name). For files: use filePath with read_files() (e.g. \"/Volumes/main/data/customers/\"). Use readFilesOptions for read_files() options like { multiLine: true } for multi-line JSON, { format: \"csv\", header: true } for CSV, etc. Optionally specify columns, WHERE clause, GROUP BY for aggregate queries, and ORDER BY for sorting results.",
      "parameters": {
        "type": "object",
        "properties": {
          "summary": {
            "type": "string",
            "description": "A short 3-8 word summary starting with a verb describing the specific data being queried, based on the query content — not the user's request (e.g. \"Fetch monthly sales data\")."
          },
          "tableName": {
            "type": "string",
            "description": "The name of the table to get sample data from. Specify as catalog.schema.name. Provide this OR filePath (set the other to empty string)."
          },
          "filePath": {
            "type": "string",
            "description": "Path to files to query directly using read_files(). Auto-detects format (json, csv, parquet, etc.). Provide this OR tableName (set the other to empty string). Example: \"/Volumes/main/data/customers/\""
          },
          "readFilesOptions": {
            "type": "array",
            "description": "Key-value options for read_files() when using filePath. Common options: multiLine (boolean) for multi-line JSON, format (string) to specify file type, header (boolean) for CSV with headers, delimiter (string) for CSV delimiter, etc. Values can be strings, booleans, or numbers. Leave empty if no options are needed.",
            "items": {
              "type": "object",
              "properties": {
                "key": {
                  "type": "string"
                },
                "value": {
                  "type": [
                    "boolean",
                    "number",
                    "string"
                  ]
                }
              },
              "required": [
                "key",
                "value"
              ],
              "additionalProperties": false
            }
          },
          "columns": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Optional array of column names or expressions to select from the selected tableName. Supports aggregate functions like COUNT(*), AVG(column), etc. If not provided, all columns (*) will be selected."
          },
          "whereClause": {
            "type": "string",
            "description": "Optional WHERE clause condition (without the WHERE keyword). Example: \"age > 25 AND status = 'active'\""
          },
          "groupBy": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Optional array of column names to group by. Required when using aggregate functions in the columns parameter. Example: [\"customer_id\", \"region\"]"
          },
          "orderBy": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Optional array of column names or expressions to order by. Each can optionally include ASC or DESC. Example: [\"date DESC\", \"customer_id ASC\"]"
          },
          "limit": {
            "type": "number",
            "description": "The number of rows to return as sample data (default: 100, max: 1000)"
          }
        },
        "required": [
          "summary",
          "tableName",
          "filePath",
          "readFilesOptions",
          "columns",
          "whereClause",
          "groupBy",
          "orderBy",
          "limit"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "scheduleAsset",
      "description": "Schedules an asset (notebook, python file, or pipeline - not a prompt) to run on a recurring schedule. The asset is deterministic — the same code runs each time, e.g. \"run this notebook hourly\", \"schedule this pipeline daily\".",
      "parameters": {
        "type": "object",
        "properties": {
          "jobName": {
            "type": "string",
            "description": "The name for the scheduled job. Unless explicitly provided by the user, come up with a name based on the tasks and schedule."
          },
          "description": {
            "type": [
              "string",
              "null"
            ],
            "description": "Optional description for the scheduled job."
          },
          "task": {
            "description": "The asset to schedule.",
            "anyOf": [
              {
                "type": "object",
                "properties": {
                  "taskKey": {
                    "type": [
                      "string",
                      "null"
                    ],
                    "description": "A unique key for this task within the job. This is only required if you intend to reference this task in another task as a dependency or the user provides the task key explicitly, set to null otherwise."
                  },
                  "assetType": {
                    "type": "string",
                    "enum": [
                      "pipeline"
                    ]
                  },
                  "pipelineId": {
                    "type": "string",
                    "description": "The ID of the SDP (formerly DLT) pipeline to run."
                  }
                },
                "required": [
                  "taskKey",
                  "assetType",
                  "pipelineId"
                ],
                "additionalProperties": false
              },
              {
                "type": "object",
                "properties": {
                  "taskKey": {
                    "type": [
                      "string",
                      "null"
                    ],
                    "description": "A unique key for this task within the job. This is only required if you intend to reference this task in another task as a dependency or the user provides the task key explicitly, set to null otherwise."
                  },
                  "assetType": {
                    "type": "string",
                    "enum": [
                      "notebook",
                      "python_file"
                    ]
                  },
                  "path": {
                    "type": "string",
                    "description": "The absolute workspace path to the file. Required for notebook and python_file task types. Must start with /. Example: \"/Users/user@databricks.com/my-notebook\""
                  }
                },
                "required": [
                  "taskKey",
                  "path",
                  "assetType"
                ],
                "additionalProperties": false
              }
            ]
          },
          "schedule": {
            "type": "object",
            "description": "Schedule configuration for the job. You MUST convert natural language schedules to a valid Quartz cron expression before calling this tool.",
            "properties": {
              "cronExpression": {
                "type": "string",
                "description": "Schedule as a Quartz cron expression with 7 fields: <seconds> <minutes> <hours> <day-of-month> <month> <day-of-week> <year>. Examples: \"0 0 8 * * ? *\" (daily at 8am), \"0 30 10 ? * MON *\" (every Monday at 10:30am), \"0 0 */6 * * ? *\" (every 6 hours)."
              },
              "timezoneId": {
                "type": [
                  "string",
                  "null"
                ],
                "description": "(Optional) Timezone for the schedule (e.g., \"America/Los_Angeles\", \"America/New_York\", \"Europe/London\", \"UTC\"). Set to null if the user does not explicitly specify a timezone."
              }
            },
            "required": [
              "cronExpression",
              "timezoneId"
            ],
            "additionalProperties": false
          }
        },
        "required": [
          "task",
          "jobName",
          "schedule",
          "description"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "compute",
      "description": "Perform a compute-related action for the current editor. The \"list\" action enumerates clusters, SQL warehouses, serverless compute, and database instances the editor can attach to. Use this to help the user choose the right compute for their workload.",
      "parameters": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "enum": [
              "list"
            ],
            "description": "\"list\" enumerates the clusters, SQL warehouses, serverless compute, and database instances available to the current editor."
          }
        },
        "required": [
          "action"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "editAsset",
      "description": "Edit notebooks, queries, or files. Supports editing multiple cells in a single call via the content array. For NOTEBOOKS: Operations are add (new cells), update (modify existing cells), delete (remove cells). Use readAssetById first to discover cell nuids. For add: provide content items with target (cell nuid to insert after, or \"BEGIN\" for start), body, title, and optionally language. Always include a descriptive title for new cells. When adding multiple cells, only the first item needs a target; subsequent items chain automatically. For update: provide content items with target (cell nuid) and body. Include a title if the cell has no title yet. Optionally include language to change the cell language; if omitted, the existing cell language is preserved. For delete: provide content items with target (cell nuid). For QUERIES: Only update operation is supported (queries have a single cell). Do not add or delete cells. For FILES: Only update operation is supported (replaces entire file content). Provide a single content item with body. No target needed for files. For APPS: Only update operation is supported (updates app description). Provide a single content item with body set to the new description. Use the app name as the asset ID.",
      "parameters": {
        "type": "object",
        "properties": {
          "asset": {
            "type": "object",
            "description": "Reference to the asset to edit. Provide the asset type, its identifier, and its display name.",
            "properties": {
              "type": {
                "type": "string",
                "enum": [
                  "notebook",
                  "dashboard",
                  "file",
                  "query",
                  "app"
                ],
                "description": "The type of asset."
              },
              "id": {
                "type": "string",
                "description": "The identifier of the asset. For notebooks, queries, jobs, and dashboards, this is the numeric ID (e.g., \"12345\"). For files, this must be the full workspace path (e.g., \"/Workspace/Users/user@example.com/my_file.py\"). Do NOT use the numeric ID for files."
              },
              "name": {
                "type": "string",
                "description": "The display name of the asset (e.g., file name or notebook title)."
              }
            },
            "required": [
              "type",
              "id",
              "name"
            ],
            "additionalProperties": false
          },
          "operation": {
            "type": "string",
            "enum": [
              "add",
              "update",
              "delete"
            ],
            "description": "The type of edit operation to perform."
          },
          "content": {
            "type": "array",
            "description": "Array of content items describing the edits. Each item specifies a target, body, language, and/or title. For notebooks: supports multiple items to add/update/delete several cells in one call. For queries: only update (it is a single cell); do not add or delete cells. For add, target is the cell nuid to insert after (\"BEGIN\" for start); subsequent items chain automatically. For files: only the first item is used (body replaces entire file content). For jobs: only the first item is used and only update is supported — see the tool description for the exact body shape.",
            "items": {
              "type": "object",
              "properties": {
                "target": {
                  "type": "string",
                  "description": "Cell nuid for notebooks and queries. REQUIRED for notebook/query operations, even empty ones. For add: the cell to insert after (use \"BEGIN\" to insert at the start). For update/delete: the cell to operate on. Not required for file or job edits."
                },
                "body": {
                  "type": "string",
                  "description": "The text/code content for this item. Required for add and update operations."
                },
                "language": {
                  "type": "string",
                  "enum": [
                    "python",
                    "sql",
                    "scala",
                    "r",
                    "md"
                  ],
                  "description": "The language of the content (for code cells in notebooks). For update operations, omitting this field preserves the existing cell language. Only provide this when intentionally changing a cell to a different language."
                },
                "title": {
                  "type": "string",
                  "description": "Optional title for the item. Mandatory for notebook add or update operations."
                }
              },
              "required": [],
              "additionalProperties": false
            }
          }
        },
        "required": [
          "asset",
          "operation",
          "content"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "queryGetResults",
      "description": "Get the execution output results of the current SQL query. Use this to analyze the query results (data table, charts...) before making additional changes.",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "runSqlQuery",
      "description": "Run one or more SQL statements inside the SQL Editor (a query can contain one or more SQL statements separated by semi colons \";\")",
      "parameters": {
        "type": "object",
        "properties": {
          "summary": {
            "type": "string",
            "description": "A short 3-8 word summary starting with a verb describing the specific action the SQL query performs, based on the query content — not the user's request (e.g. \"Aggregate daily sales metrics\")."
          },
          "cells": {
            "type": "array",
            "description": "Index list of the statements of the query to run. One number increment per statement.",
            "items": {
              "type": "object",
              "properties": {
                "index": {
                  "type": "number",
                  "description": "One-based index of the statement position in the query."
                },
                "code": {
                  "type": "string",
                  "description": "The code of the statement at the given index."
                }
              },
              "required": [
                "index",
                "code"
              ],
              "additionalProperties": false
            }
          },
          "runningAllStatements": {
            "type": "boolean",
            "description": "Whether to run all statements in the query. If false, only the statements at the given indices will be run. Defaults to false."
          }
        },
        "required": [
          "summary",
          "cells",
          "runningAllStatements"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "createSqlEditorVisualization",
      "description": "Create a visualization (chart) based on SQL query results in the SQL Editor. The query must have been executed and contain table results. Supports various chart types including bar, line, area, combo, pie, scatter, heatmap, histogram, and box. The visualization will be added as a new tab alongside the table results.",
      "parameters": {
        "type": "object",
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "bar",
              "line",
              "area",
              "combo",
              "pie",
              "scatter",
              "heatmap",
              "histogram",
              "box"
            ],
            "description": "The type of chart to create. \"bar\" for bar/column charts, \"line\" for line charts, \"combo\" for combo charts, \"area\" for area charts, \"pie\" for pie charts, \"scatter\" for scatter plots, \"heatmap\" for heatmaps, \"box\" for box plots, \"histogram\" for histograms."
          },
          "name": {
            "type": "string",
            "description": "User friendly name"
          },
          "xAxis": {
            "type": "object",
            "properties": {
              "columnName": {
                "type": "string",
                "description": "User friendly name for the column"
              },
              "columnExpression": {
                "type": "string",
                "description": "Column name or aggregation expression."
              }
            },
            "required": [
              "columnName",
              "columnExpression"
            ],
            "additionalProperties": false
          },
          "yAxis": {
            "type": "object",
            "properties": {
              "columnName": {
                "type": "string",
                "description": "User friendly name for the column"
              },
              "columnExpression": {
                "type": "string",
                "description": "Column name or aggregation expression."
              }
            },
            "required": [
              "columnName",
              "columnExpression"
            ],
            "additionalProperties": false
          },
          "secondaryYAxis": {
            "anyOf": [
              {
                "type": "object",
                "properties": {
                  "columnName": {
                    "type": "string",
                    "description": "User friendly name for the column"
                  },
                  "columnExpression": {
                    "type": "string",
                    "description": "Column name or aggregation expression."
                  }
                },
                "required": [
                  "columnName",
                  "columnExpression"
                ],
                "additionalProperties": false
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional secondary Y-axis column for combo charts. Pass null when no secondary Y-axis is needed."
          },
          "color": {
            "type": "object",
            "properties": {
              "columnName": {
                "type": "string",
                "description": "User friendly name for the column"
              },
              "columnExpression": {
                "type": "string",
                "description": "Column name or aggregation expression."
              }
            },
            "required": [
              "columnName",
              "columnExpression"
            ],
            "additionalProperties": false
          },
          "angle": {
            "type": "object",
            "properties": {
              "columnName": {
                "type": "string",
                "description": "User friendly name for the column"
              },
              "columnExpression": {
                "type": "string",
                "description": "Column name or aggregation expression."
              }
            },
            "required": [
              "columnName",
              "columnExpression"
            ],
            "additionalProperties": false
          }
        },
        "required": [
          "type",
          "name",
          "xAxis",
          "yAxis",
          "secondaryYAxis",
          "angle",
          "color"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "docSearch",
      "description": "Search the public Databricks documentation for guidance on products, features, APIs, Spark (Python), SQL syntax, configs, troubleshooting, and best practices.",
      "parameters": {
        "type": "object",
        "properties": {
          "searchQuery": {
            "type": "string",
            "description": "keywords used as search query. Does not support AND/OR operators, just a single query."
          }
        },
        "required": [
          "searchQuery"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "readTable",
      "description": "Get detailed information on a workspace SQL table like column types, comments, frequent queries, and joins. Specify the table as catalog.schema.name. When extraDetails is true, the result includes a metadata.properties map with Spark and Delta internals (e.g. spark.sql.statistics.*, delta.*); summarize its key entries in your reply.",
      "parameters": {
        "type": "object",
        "properties": {
          "catalog": {
            "type": "string",
            "description": "Name of the catalog that contains the table to read (required).",
            "minLength": 1
          },
          "schema": {
            "type": "string",
            "description": "Name of the schema (sometimes named database) that contains the table to read (required).",
            "minLength": 1
          },
          "name": {
            "type": "string",
            "description": "The non-prefixed name of the table to read (required).",
            "minLength": 1
          },
          "extraDetails": {
            "type": "boolean",
            "description": "If true, returns the full set of table properties as JSON (can be large). If false (default), returns a compact subset of table properties."
          }
        },
        "required": [
          "catalog",
          "schema",
          "name",
          "extraDetails"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "tableSearch",
      "description": "Search for tables and datasets in the current workspace by keywords or natural language description. Do NOT use this tool if a fully qualified table name (catalog.schema.table) is already provided, use readTable instead to get table details directly.",
      "parameters": {
        "type": "object",
        "properties": {
          "keywords": {
            "type": "array",
            "description": "Keywords to use for the table search",
            "items": {
              "type": "string",
              "minLength": 1
            }
          },
          "searchQuery": {
            "type": "string",
            "description": "Natural language description of what to look for. Be specific about the data characteristics, relationships, or business purpose. Example: \"a table that contains a mapping between user id and pseudo user id\"",
            "minLength": 1
          },
          "dataCatalog": {
            "type": "string",
            "description": "Optional Data Catalog to search tables in (default \"\")"
          },
          "dataSchema": {
            "type": "string",
            "description": "Optional schema to search tables in (default \"\")"
          }
        },
        "required": [
          "keywords",
          "searchQuery",
          "dataCatalog",
          "dataSchema"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "querySearch",
      "description": "Preferred tool to search for queries in the current workspace and provide their definition. When you use this tool, you should format your question/sentence as a how-to question and also provide specific keywords that match query names or content.",
      "parameters": {
        "type": "object",
        "properties": {
          "sentence": {
            "type": "string",
            "description": "A how to question of what to look for. Be specific about the data characteristics, relationships, or business purpose. Example: \"how to map user id to pseudo user id\".",
            "minLength": 1
          },
          "keywords": {
            "type": "string",
            "description": "Specific keywords that match query names or content. Example: \"user_id pseudo_user_id mapping\".",
            "minLength": 1
          },
          "tablesToFilterOn": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Required table name array to filter on to scope down the search. Each value MUST be a fully qualified table name in the form <catalog>.<schema>.<table_name> — partial names, unqualified names, or any other format WILL CAUSE THE SEARCH TO FAIL. If not provided, use the tableSearch tool to gather relevant tables to provide as an argument here to improve the search accuracy. Only provide an empty array here if a preliminary table search was run and did not yield any relevant table names."
          }
        },
        "required": [
          "sentence",
          "keywords",
          "tablesToFilterOn"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "forYouAssets",
      "description": "Fetch personalized assets of the users. Supports optional keyword search for suggested assets. For lineage-based recommendations, also check if related_entities names match your keywords to determine relevance.",
      "parameters": {
        "type": "object",
        "properties": {
          "numResults": {
            "type": "number",
            "description": "The number of results to fetch for each asset type (default: 25).",
            "default": 25
          },
          "includeFavorites": {
            "type": "boolean",
            "description": "Whether to include favorite assets (default: true)."
          },
          "includeSuggestedAssets": {
            "type": "boolean",
            "description": "Whether to include recent assets (default: true)."
          },
          "includePopularAssets": {
            "type": "boolean",
            "description": "Whether to include popular assets (default: false)."
          },
          "assetTypes": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "notebooks",
                "dashboards",
                "pipelines",
                "jobs",
                "datarooms",
                "files",
                "alerts",
                "apps",
                "endpoints",
                "models",
                "folders"
              ]
            },
            "description": "Array of asset types to include."
          },
          "page": {
            "type": "string",
            "description": "The page context for recommendations (default: PAGE_UNSPECIFIED).",
            "enum": [
              "CATALOG_EXPLORER_LANDING_PAGE",
              "DATABRICKS_ONE_FOR_YOU_PAGE",
              "DATABRICKS_ONE_HOME_PAGE",
              "DISCOVER_PAGE",
              "HOME_PAGE",
              "OTHER",
              "PAGE_UNSPECIFIED"
            ],
            "default": "PAGE_UNSPECIFIED"
          },
          "interface": {
            "type": "string",
            "description": "The interface context for recommendations (default: ASSISTANT).",
            "enum": [
              "ASSISTANT",
              "INTERFACE_UNSPECIFIED",
              "ONE_CHAT",
              "UI"
            ],
            "default": "ASSISTANT"
          },
          "keywords": {
            "type": "string",
            "description": "Keywords for searching assets. When provided, enables keyword-based search for suggested assets (default: empty string).",
            "default": ""
          }
        },
        "required": [
          "numResults",
          "includeFavorites",
          "includePopularAssets",
          "includeSuggestedAssets",
          "assetTypes",
          "page",
          "interface",
          "keywords"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "searchAssets",
      "description": "Search for assets in the current workspace using unified search API. Returns comprehensive search results for tables, queries, notebooks, dashboards, pipelines, jobs, datarooms, and files. Supports pagination - use the nextPageToken from response to get additional pages of results.",
      "parameters": {
        "type": "object",
        "properties": {
          "searchQuery": {
            "type": "string",
            "description": "Keywords used as search query. Use an empty string to browse/list assets without keyword filtering (e.g. when listing all assets of a type). Does not support AND/OR operators."
          },
          "assetTypes": {
            "type": "array",
            "items": {
              "type": "string",
              "enum": [
                "notebooks",
                "dashboards",
                "pipelines",
                "jobs",
                "datarooms",
                "files",
                "alerts",
                "apps",
                "endpoints",
                "models",
                "folders"
              ]
            },
            "description": "Array of asset types to search for. If not provided, searches all types."
          },
          "numResultsPerAssetType": {
            "type": "number",
            "description": "The number of results to return by asset type (max: 5).",
            "default": 3
          },
          "sortBy": {
            "type": "string",
            "enum": [
              "score",
              "popularity",
              "updated_at",
              "name"
            ],
            "description": "How to sort the search results (default: score for relevance)."
          },
          "ownerOnly": {
            "type": "boolean",
            "description": "If true, restrict results to assets owned by the current user only."
          },
          "pageToken": {
            "type": "string",
            "description": "Token for paginating through search results. Use the nextPageToken from a previous search response to get the next page."
          }
        },
        "required": [
          "searchQuery",
          "assetTypes",
          "numResultsPerAssetType",
          "sortBy",
          "ownerOnly",
          "pageToken"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "manageTodoList",
      "description": "Manage a structured todo list to track progress and plan tasks throughout your coding session.",
      "parameters": {
        "type": "object",
        "properties": {
          "operation": {
            "type": "string",
            "enum": [
              "write",
              "read"
            ],
            "description": "write: Replace entire todo list with new content. read: Retrieve current todo list. ALWAYS provide complete list when writing - partial updates not supported."
          },
          "todoList": {
            "type": "array",
            "description": "Complete array of all todo items (required for write operation, ignored for read). Must include ALL items - both existing and new.",
            "items": {
              "type": "object",
              "properties": {
                "id": {
                  "type": "number",
                  "description": "Unique identifier for the todo. Use sequential numbers starting from 1."
                },
                "title": {
                  "type": "string",
                  "description": "Concise action-oriented todo label (3-7 words). Displayed in UI."
                },
                "description": {
                  "type": "string",
                  "description": "Detailed context, requirements, or implementation notes. Include file paths, specific methods, or acceptance criteria."
                },
                "status": {
                  "type": "string",
                  "enum": [
                    "not-started",
                    "in-progress",
                    "completed"
                  ],
                  "description": "not-started: Not begun | in-progress: Currently working (max 1) | completed: Fully finished with no blockers"
                }
              },
              "required": [
                "id",
                "title",
                "description",
                "status"
              ],
              "additionalProperties": false
            }
          }
        },
        "required": [
          "operation",
          "todoList"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "readSkillFile",
      "description": "Read a skill file from the unified skills namespace. Use this to access skill documentation, implementation guides, reference materials, code examples... The tool automatically finds the file from the appropriate source. Can also be used to declare that no skills are needed by passing an empty string for filePath.",
      "parameters": {
        "type": "object",
        "properties": {
          "filePath": {
            "type": "string",
            "description": "Path to the skill file to read. Use format: \"skills/{skill-name}/{file}.md\". Examples: \"skills/auto-loader/SKILL.md\", \"skills/auto-loader/auto-loader-python.md\", \"skills/my-custom-skill/SKILL.md\". Use empty string \"\" if no skills are needed for this request."
          }
        },
        "required": [
          "filePath"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "readAssetById",
      "description": "Read detailed information about a workspace asset by its type and ID. Supports pipelines, notebooks, queries, files, directories, dashboards, Visual data preps (designerFile), jobs, MLflow evaluation runs, datasets, judges, and labeling schemas. Use this to read assets found via searchAssets or referenced in conversations. For files, you can optionally specify startLine and endLine to read a specific range of lines.",
      "parameters": {
        "type": "object",
        "properties": {
          "assetType": {
            "type": "string",
            "enum": [
              "pipeline",
              "notebook",
              "query",
              "file",
              "directory",
              "dashboard",
              "designerFile",
              "job",
              "mlflowEvaluationRun",
              "mlflowDataset",
              "mlflowJudge",
              "mlflowLabelingSchema"
            ],
            "description": "The type of asset to read. Use \"pipeline\" for Lakeflow Spark Declarative Pipelines (formerly DLT Pipelines), \"notebook\" for notebooks, \"query\" for saved SQL queries, \"file\" for workspace files, \"directory\" for folders/directories, \"dashboard\" for Lakeview dashboards, \"designerFile\" for Visual data preps, \"job\" for Databricks Jobs."
          },
          "assetId": {
            "type": "string",
            "description": "The unique identifier for the asset. Format varies by type: pipeline UUID for pipelines, numeric ID for notebooks/queries/files/jobs. **For directories: numeric ID or a workspace path (e.g. /Workspace/Users/user@databricks.com/my-directory).** For dashboards: use the dashboard UUID to read the published version, or the treeNodeId (numeric) to read the draft version (which also provides the file path needed for editing). The response includes a \"source\" field indicating which version was returned."
          },
          "startLine": {
            "type": [
              "number",
              "null"
            ],
            "description": "For files only: the line number to start reading from (1-indexed, inclusive). Pass null to read from the beginning."
          },
          "endLine": {
            "type": [
              "number",
              "null"
            ],
            "description": "For files only: the line number to end reading at (1-indexed, inclusive). Pass null to read to the end."
          }
        },
        "required": [
          "assetType",
          "assetId",
          "startLine",
          "endLine"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "fetchOmittedContent",
      "description": "Retrieves omitted content from the full tool output cache.\n\nUse this tool when you see \"__toolCallId\" in a tool result that was compacted. Two main scenarios:\n\n1. **Lazy materialization** (has \"__more\" placeholders) - see examples below\n2. **Summarization** (has \"__toolCallId\" but no \"__more\") - use path \".\" to get full original data\n\n## Common Scenarios & How to Handle Them\n\n### Scenario: Array with Remaining Items\nWhen you see:\n{\n  \"__toolCallId\": \"toolu_bdrk_01ABC...\",\n  \"items\": [\n    {\"id\": 1, \"name\": \"Item 1\"},\n    {\"id\": 2, \"name\": \"Item 2\"},\n    {\"__more\": {\"path\": \".items[2:]\", \"count\": 2}}\n  ]\n}\n\n**What the fields mean:**\n- `path: \".items[2:]\"` = \"Start fetching from index 2 onwards\"\n- `count: 2` = \"You were shown 2 items already\" (aligns with the [2:] in path)\n\n**To get the rest:**\nfetchOmittedContent({toolCallId: \"toolu_bdrk_01ABC...\", path: \".items[2:]\"})\n\n**Don't calculate yourself!** The path is already correct - just copy it.\n\n### Scenario: Truncated String with Preview\nWhen you see:\n{\n  \"__toolCallId\": \"toolu_bdrk_01DEF...\",\n  \"data\": {\n    \"__more\": {\n      \"path\": \".data[9000:]\",\n      \"value\": \"First 9000 characters of the string...\",\n      \"chars\": 9000\n    }\n  }\n}\n\n**What the fields mean:**\n- `path: \".data[9000:]\"` = \"Start fetching from character 9000 onwards\"\n- `value` = \"Preview showing you the first 9000 chars\"\n- `chars: 9000` = \"The preview contains 9000 characters\" (aligns with the [9000:] in path)\n\n**To get the rest:**\nfetchOmittedContent({toolCallId: \"toolu_bdrk_01DEF...\", path: \".data[9000:]\"})\n\n**Note:** Simply copy the path - it already tells you where to continue from!\n\n### Scenario: Nested Placeholder (Drill Down Multiple Times)\nThe fetched content may itself contain \"__more\" placeholders. Just repeat:\n1. Look for \"__more\" in the new result\n2. Copy its \"path\" field\n3. Call fetchOmittedContent again with that path\n\n### Scenario: No Placeholder (Just \"__toolCallId\")\nIf there's \"__toolCallId\" but NO \"__more\" placeholders, the entire result was summarized.\nUse path \".\" to get the full original data:\nfetchOmittedContent({toolCallId: \"toolu_bdrk_01GHI...\", path: \".\"})\n\n## Key Rules\n\n✅ **DO:**\n- Copy the exact path from \"__more.path\" - it's pre-formatted with slice notation for you\n- Use `count` and `chars` to understand what was shown (they align with the slice index)\n- Drill down multiple times as needed if fetched content has more \"__more\" placeholders\n\n❌ **DON'T:**\n- Try to calculate slice indices yourself (the path already has them!)\n- Modify the path from the placeholder (it's already correct)\n- Include \"__more\" in the path (it's just the placeholder key, NOT part of the data path!)\n- Confuse this with simple \"[Truncated: X chars...]\" messages (those aren't retrievable)\n- Try to fetch paths that resolve to {\"__removed\": {...}} — these were permanently removed during pre-cache compression and cannot be retrieved. Retry with a more targeted query instead. (Strings ending in \"[+more removed]\" still contain a truthful preview that you can inspect, but the chars beyond it are gone too.)\n\n**Understanding the fields:**\n- For arrays: `count` = number of items shown, `path` has slice notation like \"[2:]\"\n- For strings: `chars` = length of preview shown, `path` has slice notation like \"[9000:]\"\n- The number in the path and the count/chars value match!\n\n**Common Mistake - Including \"__more\" in the path:**\n```json\n// You see this in the response:\n{\"data\": {\"__more\": {\"path\": \".data[9000:]\", \"value\": \"preview...\"}}}\n\n// ❌ WRONG: Don't include \"__more\" in the path!\nfetchOmittedContent({path: \".data.__more\"})  // This won't work!\n\n// ✅ CORRECT: Use the path from inside __more\nfetchOmittedContent({path: \".data[9000:]\"})  // This is correct!\n```\n\nThe \"__more\" key is just part of the placeholder structure - the actual data path is stored in \"__more.path\".\n\n## Understanding sliceInfo in Responses\n\nWhen you fetch sliced content (arrays or strings), the response includes `sliceInfo` with:\n- `start`: Where this slice starts (e.g., 9438)\n- `end`: Where this slice ends (e.g., 16104)\n- `totalLength`: Total length of the full array/string (e.g., 30000)\n- `hasMore`: Boolean - whether there's more content after this slice\n\n**Example:**\n```json\n{\n  \"value\": \"... 6666 chars ...\",\n  \"sliceInfo\": {\n    \"start\": 9438,\n    \"end\": 16104,\n    \"totalLength\": 30000,\n    \"hasMore\": true\n  }\n}\n```\n\n**How to use it:**\n- If `hasMore: true`, call fetchOmittedContent again with path `\".yourPath[{end}:]\"`\n- If `hasMore: false`, you've fetched everything\n- `totalLength` tells you the size of the complete array/string\n\n## Finding the Tool Call ID\nLook for \"__toolCallId\" at the TOP LEVEL of the tool result (not nested inside).",
      "parameters": {
        "type": "object",
        "properties": {
          "toolCallId": {
            "type": "string",
            "description": "The tool call ID from the original tool invocation that had content omitted. This should be available as __toolCallId at the top level of the tool result."
          },
          "path": {
            "type": "string",
            "description": "The jq-style path to fetch from the cached content. Supports path expressions, array indexing, array slicing, and string slicing:\n- Identity/root: \".\" - retrieves the complete original data (use this to recover full results after summarization)\n- Object property: \".data\", \".data.items\"\n- Array indexing: \".data.items[42]\" - returns single element\n- Array slicing: \".data.items[10:30]\", \".data.items[10:]\", \".data.items[:10]\"\n- String slicing: \".message[0:100]\", \".text[50:]\" - extracts substring (single-index NOT supported for strings)\n- Root-level slicing: If the cached data itself is an array or string, use \".[0:10]\" or \".[5:]\" to slice directly\nOther jq features are not supported."
          }
        },
        "required": [
          "toolCallId",
          "path"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "createAsset",
      "description": "Create a blank Databricks asset (only supported: notebook, dashboard, file, directory, job, query, pipeline, genie space, or designerFile / Visual ETL). The asset is created empty - use the edit asset tool to add content, then openAsset to navigate to it. Files and folders will be created relative to the user's home directory, e.g. \"/Users/bob.jones@databricks.com\". For genie spaces, tableIdentifiers (fully qualified table names) are required.",
      "parameters": {
        "type": "object",
        "properties": {
          "assetType": {
            "type": "string",
            "enum": [
              "notebook",
              "dashboard",
              "file",
              "job",
              "query",
              "pipeline",
              "directory",
              "genie",
              "app",
              "designerFile"
            ],
            "description": "The type of asset to create."
          },
          "name": {
            "type": "string",
            "description": "A descriptive name based on the user's request or task intent (e.g., \"Properties EDA\", \"Revenue Analysis Q4\"). Avoid generic names like \"New Notebook\". For files, include the extension (e.g., \"script.py\", \"config.json\"). For designerFile, the .designer.ipynb extension is added automatically if missing. Use the full path for nested directories (e.g., \"my-project/analysis.py\") — intermediate folders are created automatically."
          },
          "tableIdentifiers": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Required for genie spaces only. Fully qualified Unity Catalog table names (e.g., [\"catalog.schema.table\"])."
          }
        },
        "required": [
          "assetType",
          "name",
          "tableIdentifiers"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "openAsset",
      "description": "Open or get the URL for any Databricks asset. Supports notebooks, dashboards, queries, files, jobs, job runs, pipelines, pipeline editors, folders, tables, experiments, models, endpoints, alerts, genie spaces, and designer files (Visual ETL). By default, navigates to the asset. Set navigate=false to just get the URL.",
      "parameters": {
        "type": "object",
        "properties": {
          "assetType": {
            "type": "string",
            "enum": [
              "notebook",
              "dashboard",
              "query",
              "file",
              "job",
              "job-run",
              "pipeline-monitoring",
              "pipeline-editor",
              "folder",
              "table",
              "experiment",
              "model",
              "endpoint",
              "alert",
              "genie",
              "app",
              "designerFile"
            ],
            "description": "The type of asset to open."
          },
          "assetId": {
            "type": "string",
            "description": "The unique identifier for the asset. For dashboards, use the `id` field from search results (32 hex chars, e.g. \"01ee875dcf1e1aa59e1b2f5a7e60b955\"), NOT the treeNodeId. For notebooks/files/jobs use numeric ID, for queries use UUID, for pipelines use pipeline ID, for tables use fully qualified name. For job-run, prefer \"jobId/runId\" format (e.g. \"123/456\"); plain \"runId\" is also accepted."
          },
          "assetName": {
            "type": "string",
            "description": "Display name or identifier (defaults to ''). Required for tables (catalog.schema.table), models, and endpoints."
          },
          "navigate": {
            "type": "boolean",
            "default": true,
            "description": "Whether to navigate to the asset URL. Defaults to true."
          },
          "continueMessage": {
            "type": "string",
            "description": "Message to send to the assistant on the destination page after navigation. Leave empty ('') to navigate without triggering a follow-up agent run. Provide a non-empty message when the user's request requires work in the destination context (e.g., 'run the cells' after opening a notebook). If the task depends on content from a previous asset, include its assetType and assetId so the destination agent can retrieve it with readAssetById."
          }
        },
        "required": [
          "assetType",
          "assetId",
          "assetName",
          "navigate",
          "continueMessage"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "executeCode",
      "description": "Execute code tool\n\n- When the notebook is active, all the notebook variables and imports are available for use.\n- Use this tool to run short code snippets in the specified language (exploration, validation, small computations). It is not for persisting edits to user assets.\n- Always include your reasoning in the 'thoughts' field.\n- Do NOT use this tool to create, modify, or write workspace files — even when creating multiple files or a project structure. Always use createAsset and editAsset instead, regardless of the number of files involved.\n- **Do NOT use this tool to read, list, or explore workspace files and directories.** Use `readAssetById` instead — it is faster and returns structured results. For example, use `readAssetById` with assetType \"directory\" to list a directory, or assetType \"file\" to read a file. Do not use shell commands like `ls`, `cat`, `find`, `tree`, or Python like `os.listdir`, `open()`, `dbutils.fs.ls` for these purposes.\n- Execution has a default timeout of 15 minutes. Most simple queries should complete well within this time.\n- For expensive queries (large aggregations, complex joins), you can increase the timeout. If a query times out, consider simplifying it or adding filters.\n\n### Shell commands (language: 'sh')\n- Shell commands always run in the user's home directory (/Workspace/Users/{username}).\n- Home directory contains the user's notebooks, queries and files.\n- Notebooks have .ipynb extension. They are stored in Jupyter notebook format (nbformat).\n- Queries have .sql extension.\n- AI/BI dashboard files have lvdash.json extension.\n- Queries are stored with .dbquery.ipynb extension in the user's home directory. They also use the notebook format.\n- Do NOT use shell commands to create, write, or modify files or directories. Use createAsset and editAsset instead.\n- Do NOT use shell commands (ls, cat, find, tree, head, tail, etc.) to read or list workspace files and directories. Use `readAssetById` instead.\n- You can search within notebooks and queries using 'grep' command. Code will be in code cells, markdown in markdown cells. Code can span multiple lines.\n- You can grep for code in files using: grep -r \"search term\" *.ipynb. To identify tables, you can search for keywords like 'SELECT', 'FROM', 'JOIN', 'WHERE', etc. SQL can span multiple lines.\n- 'jq' command line tool is NOT available.\n- If shell execution fails or compute is not available, do NOT retry. Try other tools if available or respond with your final answer.\n\n",
      "parameters": {
        "type": "object",
        "properties": {
          "summary": {
            "type": "string",
            "description": "A short 3-8 word summary starting with a verb describing the specific action the code performs, based on the code content — not the user's request (e.g. \"Calculate average order value by region\")."
          },
          "code": {
            "type": "string",
            "description": "The code to execute",
            "minLength": 1
          },
          "language": {
            "type": "string",
            "description": "The programming language of the code",
            "enum": [
              "python",
              "sh",
              "r",
              "scala",
              "sql"
            ],
            "default": "python"
          },
          "timeoutMinutes": {
            "type": "number",
            "description": "Execution timeout in minutes. Minimum 5, default 15. Increase for expensive queries.",
            "default": 15,
            "minimum": 5
          }
        },
        "required": [
          "summary",
          "code",
          "language",
          "timeoutMinutes"
        ],
        "additionalProperties": false
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "findReferencesTool",
      "description": "Find data lineage and references for tables and columns in Unity Catalog.\n\nThis tool helps discover:\n- Upstream dependencies (where data comes from)\n- Downstream dependencies (where data is used)\n- Related notebooks, jobs, and dashboards\n- Column-level lineage when column name is provided\n\nRequires fully qualified table names in format: catalog.schema.table",
      "parameters": {
        "type": "object",
        "properties": {
          "tableName": {
            "type": "string",
            "description": "Fully qualified table name in format: catalog.schema.table"
          },
          "columnName": {
            "type": "string",
            "description": "Optional column name for column-level lineage analysis"
          },
          "includeEntityLineage": {
            "type": "boolean",
            "description": "Whether to include related notebooks, jobs, and dashboards (default: true)"
          }
        },
        "required": [
          "tableName",
          "columnName",
          "includeEntityLineage"
        ],
        "additionalProperties": false
      }
    },
    "cache_control": {
      "type": "ephemeral"
    }
  }
]
