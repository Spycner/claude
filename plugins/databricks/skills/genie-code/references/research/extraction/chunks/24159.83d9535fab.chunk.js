"use strict";(self.webpackChunkdatabricks_monolith=self.webpackChunkdatabricks_monolith||[]).push([[24159],{846540:e=>{e.exports="# Snowflake — Validation Scan Pattern\n\nCombined `grepAsset` pattern for validating converted Snowflake output. Load this file via `readSkillFile` at the start of the Validate step. Run with `caseInsensitive: true`, `outputMode: \"count\"` first; re-run with `outputMode: \"content\"` for any non-zero count to locate matches. Use the action table below to fix each match.\n\n## Combined pattern\n\n```\n\\$[A-Za-z_]\\w*\\b|:[A-Za-z_]\\w*\\b|\\bIFF\\s*\\(|\\bDIV0(?:NULL)?\\s*\\(|\\bNULLIFZERO\\s*\\(|\\bZEROIFNULL\\s*\\(|\\bOBJECT_CONSTRUCT\\s*\\(|\\bARRAY_CONSTRUCT\\s*\\(|\\bMATCH_RECOGNIZE\\s*\\(|\\bRESULT_SCAN\\s*\\(|\\bFLATTEN\\s*\\(\\s*INPUT\\s*=>|\\bPARSE_JSON\\s*\\(|\\bSYSTEM\\$|\\$\\$|::N?VARCHAR\\b|\\bAS\\s+N?VARCHAR\\b|\\bEXCLUDE\\s*\\(|\\)\\s+IS\\s+(?:NOT\\s+)?DISTINCT\\s+FROM|MD5\\s*\\(\\s*(?:CAST\\s*\\(\\s*'|')|\\bWITHIN\\s+GROUP\\b|\\bCONCAT_WS\\s*\\(|\\bTO_CHAR\\s*\\(|\\bTO_(?:DATE|TIMESTAMP)\\s*\\(|\\bSPLIT\\s*\\(|CAST\\s*\\(\\s*'(?:on|off)'|\\bGROUP\\s+BY\\s+ALL\\b\n```\n\n## Validation Checklist\n\nBefore finalizing a translation, scan the output for these Snowflake remnant patterns that must NOT appear in valid Databricks SQL:\n\n| Pattern                                                      | Snowflake construct                                                                | Action                                                                                                                                                                                                                                                               |\n| ------------------------------------------------------------ | ---------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `\\$[A-Za-z_]\\w*\\b\\|:[A-Za-z_]\\w*\\b`                          | `$var` / `:var` parameter references                                               | Replace with bare DBSQL variable names                                                                                                                                                                                                                               |\n| `\\bIFF\\s*\\(`                                                 | `IFF(`, `IFNULL(`, `NVL(`, `DECODE(` calls                                         | Safe if DBSQL supports them, but prefer `IF` / `COALESCE` / `CASE` for clarity; always confirm semantics                                                                                                                                                             |\n| `\\bDIV0(?:NULL)?\\s*\\(\\|\\bNULLIFZERO\\s*\\(\\|\\bZEROIFNULL\\s*\\(` | `DIV0(`, `DIV0NULL(`, `NULLIFZERO(`, `ZEROIFNULL(`                                 | Must be replaced — these names don't exist in DBSQL (or behave differently)                                                                                                                                                                                          |\n| —                                                            | `NVL2(`, `EQUAL_NULL(`                                                             | Supported natively in DBSQL — safe as-is                                                                                                                                                                                                                             |\n| `\\bWITHIN\\s+GROUP\\b`                                         | `LISTAGG(... WITHIN GROUP ...)`                                                    | Supported in DBR 16.4+ — leave as-is. On older runtimes, rewrite with `ARRAY_JOIN(ARRAY_SORT(...))`                                                                                                                                                                  |\n| `\\bCONCAT_WS\\s*\\(`                                           | `CONCAT_WS(sep, a, b, ...)` with any NULL argument                                 | Silent drift — Snowflake returns NULL, DBSQL returns concat. Wrap with null-check if source relies on it                                                                                                                                                             |\n| `\\bTO_CHAR\\s*\\(`                                             | `TO_CHAR(date, 'YYYY...')` with uppercase tokens                                   | Rewrite as `DATE_FORMAT(date, 'yyyy-MM-dd HH:mm:ss')` with lowercase year / minute tokens                                                                                                                                                                            |\n| `\\bTO_(?:DATE\\|TIMESTAMP)\\s*\\(`                              | `TO_DATE(str, 'MI')` / `TO_TIMESTAMP(str, 'MI')`                                   | Replace `MI` with `mm`; replace `HH24` with `HH`; see `09-functions.md`                                                                                                                                                                                              |\n| `\\bSYSTEM\\$`                                                 | `SYSTEM$*` function calls                                                          | Replace with DBSQL equivalents or remove                                                                                                                                                                                                                             |\n| `\\$\\$`                                                       | `$$...$$` as plain string delimiter (outside UDF body)                             | Replace with single-quoted strings                                                                                                                                                                                                                                   |\n| `\\bOBJECT_CONSTRUCT\\s*\\(\\|\\bARRAY_CONSTRUCT\\s*\\(`            | `OBJECT_CONSTRUCT(` / `ARRAY_CONSTRUCT(`                                           | Replace with `NAMED_STRUCT(` / `ARRAY(`                                                                                                                                                                                                                              |\n| `\\bMATCH_RECOGNIZE\\s*\\(`                                     | `MATCH_RECOGNIZE(`                                                                 | TODO — no DBSQL equivalent                                                                                                                                                                                                                                           |\n| `\\bRESULT_SCAN\\s*\\(`                                         | `RESULT_SCAN(`                                                                     | TODO — no DBSQL equivalent                                                                                                                                                                                                                                           |\n| `\\bFLATTEN\\s*\\(\\s*INPUT\\s*=>`                                | `FLATTEN(INPUT =>`                                                                 | Replace with `LATERAL VIEW EXPLODE()` or `EXPLODE()` inline                                                                                                                                                                                                          |\n| `\\bPARSE_JSON\\s*\\(`                                          | `PARSE_JSON(` returning VARIANT                                                    | Keep on DBR 15.3+; on older runtimes replace with `FROM_JSON(str, schema)` and an explicit schema                                                                                                                                                                    |\n| —                                                            | Unquoted identifiers that the Snowflake source folded to uppercase                 | Confirm downstream callers don't depend on UPPERCASE column names                                                                                                                                                                                                    |\n| `\\bSPLIT\\s*\\(`                                               | Delimiter metacharacters in `SPLIT(str, delim)` calls                              | Escape regex metachars (`.`, vertical-bar, `(`, `)`, etc.)                                                                                                                                                                                                           |\n| `CAST\\s*\\(\\s*'(?:on\\|off)'`                                  | `CAST('on' AS BOOLEAN)` / `CAST('off' AS BOOLEAN)`                                 | DBSQL does not coerce `'on'`/`'off'` — rewrite to `'true'`/`'false'` or `CASE WHEN`                                                                                                                                                                                  |\n| `::N?VARCHAR\\b\\|\\bAS\\s+N?VARCHAR\\b`                          | `CAST(... AS VARCHAR)` / `CAST(... AS NVARCHAR)` / `... ::VARCHAR`                 | Replace with `CAST(... AS STRING)` — `VARCHAR` is a Snowflake type alias not valid in DBSQL; check especially inside `MD5(...)` expressions (see `09-functions.md` MD5 anti-pattern)                                                                                 |\n| `\\bEXCLUDE\\s*\\(`                                             | `SELECT * EXCLUDE (col1, col2)`                                                    | Replace with `SELECT * EXCEPT (col1, col2)` — `EXCLUDE` is Snowflake-only syntax, not valid in Databricks SQL                                                                                                                                                        |\n| `\\)\\s+IS\\s+(?:NOT\\s+)?DISTINCT\\s+FROM`                       | `(a, b) IS DISTINCT FROM (c, d)` tuple form                                        | Decompose with OR: `a IS DISTINCT FROM c OR b IS DISTINCT FROM d` (true when ANY position differs)                                                                                                                                                                   |\n| `\\)\\s+IS\\s+(?:NOT\\s+)?DISTINCT\\s+FROM`                       | `(a, b) IS NOT DISTINCT FROM (c, d)` tuple form                                    | Decompose with AND: `a IS NOT DISTINCT FROM c AND b IS NOT DISTINCT FROM d` (true only when ALL positions are equal). DBSQL `IS [NOT] DISTINCT FROM` is scalar-only                                                                                                  |\n| `MD5\\s*\\(\\s*(?:CAST\\s*\\(\\s*'\\|')`                            | `MD5('col_name')` or `MD5(CONCAT('col_a', ...))` — column name as a string literal | Inside `MD5(...)`, `SHA(...)`, `SHA2(...)`, `xxhash64(...)`: arguments must be bare column identifiers or expressions, never quoted strings. `MD5(col_a)` hashes the column value; `MD5('col_a')` always hashes the constant string `\"col_a\"` regardless of row data |\n| `\\bGROUP\\s+BY\\s+ALL\\b`                                       | `GROUP BY ALL`                                                                     | Verify the migration target is DBR 12.2 LTS+ or SQL Serverless. If the target version is unknown or earlier than 12.2, expand to an explicit column list                                                                                                             |\n| —                                                            | Unqualified base table in FROM / JOIN / INTO                                       | Every base table must use the full three-part name `catalog.schema.table`. Check every FROM clause, every JOIN, and every INSERT INTO. The catalog and schema must match the migration target given in the session context                                           |\n| —                                                            | Schema-qualified CTE name — e.g. `catalog.schema.cte_name` in a WITH clause        | CTEs are SQL aliases, not catalog objects. Never apply `catalog.schema.` to a CTE name. Remove any such prefix found on a CTE alias                                                                                                                                  |\n"}}]);
//# sourceMappingURL=https://sourcemaps.dev.databricks.com/monolith/static/js/24159.83d9535fab.chunk.js.map