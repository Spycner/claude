"use strict";(self.webpackChunkdatabricks_monolith=self.webpackChunkdatabricks_monolith||[]).push([[27567],{27567:e=>{e.exports="# Schema & DDL Migration — Oracle\n\nOracle → DBSQL equivalents for schema objects, table definitions, indexes, constraints, and sequences.\n\n> **Databricks defaults**: All managed tables are **Delta format** by default — no `USING DELTA` clause is required unless overriding. Reference **every permanent object** using **Unity Catalog three-part names**: `catalog.schema.object_name`. This applies to **tables, views, materialized views, sequences, functions, and procedures** alike — not just tables. A bare `CREATE SEQUENCE orders_seq` or `CREATE VIEW v` is incorrect output; it must be `CREATE SEQUENCE my_catalog.my_schema.orders_seq` / `CREATE VIEW my_catalog.my_schema.v`. The same rule applies to every reference site: `NEXT VALUE FOR <seq>`, `DROP <object>`, `ALTER <object>`, `SELECT FROM <object>`, etc.\n\n## ORACLE\n\n### Identity Columns\n\nOracle offers two patterns for surrogate keys: pre-12c `SEQUENCE` + `BEFORE INSERT TRIGGER`, and 12c+ `GENERATED ALWAYS/BY DEFAULT AS IDENTITY`. Both map to DBSQL identity columns — with the same caveats.\n\n> DBSQL identity column DDL is shown inline below. Load `skills/writing-sql/table-optimization.md` if you need clustering or partitioning context alongside DDL changes.\n\n**WARNING: Identity columns disable parallel writes in Delta.** See alternatives below.\n\n```sql\n-- Oracle 12c+\nCREATE TABLE t (\n    id NUMBER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,\n    name VARCHAR2(100)\n);\n\n-- Oracle pre-12c (sequence + trigger, unconditional assignment)\nCREATE SEQUENCE t_seq START WITH 1 INCREMENT BY 1;\nCREATE TABLE t (id NUMBER PRIMARY KEY, name VARCHAR2(100));\nCREATE OR REPLACE TRIGGER t_bi BEFORE INSERT ON t FOR EACH ROW\nBEGIN :NEW.id := t_seq.NEXTVAL; END;\n/\n\n-- Oracle pre-12c (sequence + trigger, NULL-guarded assignment — caller may supply id)\nCREATE OR REPLACE TRIGGER t_bi BEFORE INSERT ON t FOR EACH ROW\nBEGIN\n    IF :NEW.id IS NULL THEN\n        SELECT t_seq.NEXTVAL INTO :NEW.id FROM dual;\n    END IF;\nEND;\n/\n\n-- DBSQL — preserve the Oracle PRIMARY KEY (informational, NOT ENFORCED)\n-- Pick ALWAYS vs BY DEFAULT from the source semantics:\n--   * Oracle 12c+ `GENERATED ALWAYS AS IDENTITY`            → DBSQL `GENERATED ALWAYS AS IDENTITY`\n--   * Oracle 12c+ `GENERATED BY DEFAULT AS IDENTITY`        → DBSQL `GENERATED BY DEFAULT AS IDENTITY`\n--   * Pre-12c trigger with unconditional assignment         → DBSQL `GENERATED ALWAYS AS IDENTITY`\n--   * Pre-12c trigger guarded by `IF :NEW.id IS NULL`       → DBSQL `GENERATED BY DEFAULT AS IDENTITY`\n--     (the guard means callers were allowed to supply their own id — `ALWAYS` would reject those inserts)\nCREATE TABLE my_catalog.my_schema.t (\n    id   BIGINT GENERATED BY DEFAULT AS IDENTITY,\n    name STRING,\n    CONSTRAINT pk_t PRIMARY KEY (id) NOT ENFORCED  -- Oracle PRIMARY KEY preserved as informational\n);\n```\n\n> **`ALWAYS` vs `BY DEFAULT` — pick deliberately.** `GENERATED ALWAYS` rejects any user-supplied value at runtime; `GENERATED BY DEFAULT` accepts a supplied value and only generates one when the column is omitted or `NULL`. When in doubt about which one the source used, default to `BY DEFAULT` — it is the strictly looser semantics and will not break existing writers. Use `ALWAYS` only when you have positive evidence (Oracle DDL says `GENERATED ALWAYS`, or the trigger assigns unconditionally and no caller passes a value).\n>\n> **Always preserve the Oracle `PRIMARY KEY`** as `CONSTRAINT pk_<table> PRIMARY KEY (col) NOT ENFORCED`. PK declarations are informational in DBSQL but are required for FK references, optimizer hints, Catalog Explorer metadata, and downstream BI tools. Dropping the PK silently is a translation bug, not a simplification.\n\n**Critical limitations:**\n\n1. **Disables parallel writes** — Delta tables with identity columns cannot be written concurrently. A single concurrent writer is required for the entire table; this is typically the dealbreaker for large ETL.\n2. **Values not guaranteed sequential** — May have gaps.\n3. **Order across rows is not guaranteed** — Identity values are assigned per-task and per-batch; rows ordered by `id` are **not** in insertion order. Code that relied on Oracle `IDENTITY` / sequence-based ids being monotonically increasing in arrival order will be wrong.\n4. **Cannot be added later** — Cannot add identity columns via `ALTER TABLE`.\n5. **No `CURRVAL` equivalent** — There is no post-insert function to retrieve the last generated value; use `INSERT ... RETURNING` or read from the Delta change feed if needed.\n\n**Backfill / re-run divergence (CRITICAL — call out to the user):**\n\nA fresh `CREATE TABLE ... AS SELECT` or full backfill against a `GENERATED ALWAYS AS IDENTITY` column will produce **different `id` values on every run**. There is no way to reproduce the exact Oracle-assigned ids in DBSQL — Oracle's sequence cache and insert order are not preserved. This breaks:\n\n- **Joins to downstream tables that captured the Oracle `id`** as a foreign key. After backfill, the foreign key no longer matches.\n- **Re-runnable / idempotent ETL** that expected `MERGE ON natural_key` to update the same row across runs. With `GENERATED ALWAYS AS IDENTITY` the surrogate key is rewritten each run, so downstream consumers see a new `id` even when the row's natural key is unchanged.\n\n**Mitigations (in priority order):**\n\n1. **Migrate the Oracle `id` values as data, not as a generated column.** Drop `GENERATED ALWAYS AS IDENTITY` from the schema and load the original ids verbatim. Use a regular `BIGINT` column with `NOT NULL`. New rows generated post-migration get ids from a `SEQUENCE` (see below) or from the application layer.\n2. **Use `MERGE ON natural_key` with `GENERATED BY DEFAULT AS IDENTITY`** (not `ALWAYS`). `BY DEFAULT` lets the `MERGE` skip the identity column when matched — only `WHEN NOT MATCHED THEN INSERT (cols_without_id)` advances the sequence. **Caveat**: `BY DEFAULT` still assigns a fresh id on full-table reload, so this only works for incremental upserts, not initial backfill.\n3. **Use a UUID surrogate key from the start** — see \"Preferred alternative\" below.\n\n**`MERGE ON natural_key` with an `IDENTITY` PK is fragile** — the identity column cannot be in the `ON` clause (it is generated post-row-creation, so no source-side value exists), and `WHEN MATCHED THEN UPDATE SET id = ...` is rejected by `GENERATED ALWAYS`. Lay the schema out so the PK in the `ON` clause is the natural business key and the identity is purely a surrogate, or skip identity entirely.\n\n**Preferred alternative — UUID (allows parallel writes):**\n\n```sql\n-- UUIDv4 (random) — default, supports parallel writes\nCREATE TABLE my_catalog.my_schema.t (id STRING DEFAULT UUID(), name STRING);\n```\n\n> Databricks `UUID()` returns UUIDv4 (random). UUIDv4 has poor index/cluster locality because successive ids are unrelated.\n>\n> **For k-sortability** (timestamp-prefixed ids, useful when callers query the most-recent N rows or cluster on `id`), use UUIDv7. Databricks does **not** ship a built-in `UUID7()` function — generate UUIDv7 in the application or ETL layer (most modern client libraries support it) and either store as `STRING`, or split the 128-bit value into two `BIGINT` columns (`id_hi BIGINT, id_lo BIGINT`) for tighter storage and faster equality joins. Document the choice — splitting is non-portable and consumers must reconstruct the UUID on read.\n\nIf the Oracle code uses `SEQ.NEXTVAL` as a **standalone generator** (not as an identity column — e.g. in application-level inserts), keep the sequence as a DBSQL sequence object. See the `SEQUENCE` section below.\n\n### Indexes\n\nOracle: `CREATE INDEX`, `CREATE UNIQUE INDEX`, `CREATE BITMAP INDEX`, function-based indexes, `ORGANIZATION INDEX` (IOT).\n\nDBSQL: Indexes are **not supported** (Delta uses data skipping + Z-ORDER + liquid clustering). Drop all `CREATE INDEX` / `CREATE BITMAP INDEX` statements. Translate IOT (index-organized tables) to a regular Delta table with liquid clustering on the former primary key.\n\n> **`CREATE UNIQUE INDEX` carries uniqueness intent — do not silently drop it.** A unique index in Oracle enforces both a query-acceleration role (which DBSQL handles via data skipping) **and** a uniqueness constraint. Lift the uniqueness intent to an informational `UNIQUE` constraint so it is preserved as schema metadata for the optimizer, Catalog Explorer, and downstream tools:\n>\n> ```sql\n> -- Oracle\n> CREATE UNIQUE INDEX uq_orders_natural_key ON orders (customer_id, order_date);\n>\n> -- DBSQL — drop the index, lift uniqueness to an ALTER TABLE constraint\n> ALTER TABLE my_catalog.my_schema.orders\n>   ADD CONSTRAINT uq_orders_natural_key UNIQUE (customer_id, order_date) NOT ENFORCED;\n> ```\n>\n> Application code must enforce uniqueness (e.g. `MERGE ON (customer_id, order_date)`). DBSQL does not validate `NOT ENFORCED UNIQUE` at write time.\n\n```sql\n-- Oracle IOT\nCREATE TABLE lookup (code VARCHAR2(10) PRIMARY KEY, desc VARCHAR2(100))\nORGANIZATION INDEX;\n\n-- DBSQL\nCREATE TABLE my_catalog.my_schema.lookup (code STRING, desc STRING)\nCLUSTER BY (code);\n```\n\nDrop `CREATE INDEX` statements with an inline comment explaining the removal. Performance-sensitive indexes may need replacement with liquid clustering or Z-ORDER — load `skills/writing-sql/table-optimization.md` if the user asks about performance.\n\n### Constraints\n\nOracle enforces `PRIMARY KEY`, `UNIQUE`, `FOREIGN KEY`, `NOT NULL`, and `CHECK` constraints. DBSQL enforces `NOT NULL` and `CHECK`; primary keys, unique constraints, and foreign keys are **informational only** (not enforced).\n\n> **CRITICAL — `CHECK` cannot be inline in `CREATE TABLE`.** Oracle commonly declares CHECKs at the column level (`status VARCHAR2(20) CHECK (status IN (...))`) or as a table-level clause inside `CREATE TABLE`. **DBSQL rejects both forms.** Every `CHECK` must be lifted to a separate `ALTER TABLE ... ADD CONSTRAINT name CHECK (...)` statement issued _after_ the table is created. This applies to both column-inline and table-level Oracle CHECKs. `PRIMARY KEY`, `FOREIGN KEY`, `UNIQUE`, and `NOT NULL` may stay inline in `CREATE TABLE` — only `CHECK` must be moved.\n\n> **CRITICAL — PK columns must be declared `NOT NULL`.** Oracle implicitly makes every column of a `PRIMARY KEY` (or inline `col TYPE PRIMARY KEY`) `NOT NULL`. DBSQL does **not** — `NOT NULL` must be written explicitly on every column listed in a `PRIMARY KEY` constraint, **even when the constraint is `NOT ENFORCED`**. Omitting it causes `CREATE TABLE` to fail with a constraint-validation error. Identity columns (`GENERATED ALWAYS/BY DEFAULT AS IDENTITY`) are implicitly `NOT NULL`, so they need no extra annotation; manual PKs always do.\n>\n> ```sql\n> -- Oracle: inline PRIMARY KEY implies NOT NULL on order_id\n> CREATE TABLE orders (\n>     order_id  NUMBER PRIMARY KEY,\n>     ...\n> );\n>\n> -- DBSQL: NOT NULL must be spelled out, then PK promoted to a named constraint\n> CREATE TABLE my_catalog.my_schema.orders (\n>     order_id  BIGINT NOT NULL,                                  -- required, even with NOT ENFORCED below\n>     ...,\n>     CONSTRAINT pk_orders PRIMARY KEY (order_id) NOT ENFORCED\n> );\n> ```\n>\n> The same rule applies to composite PKs: every column in `PRIMARY KEY (a, b)` must be declared `NOT NULL`.\n\n```sql\n-- Oracle: column-inline CHECK (common)\nCREATE TABLE orders (\n    status        VARCHAR2(20) CHECK (status IN ('NEW','SHIPPED')),\n    total_amount  NUMBER(12,2) CHECK (total_amount >= 0)\n);\n\n-- DBSQL: lift CHECKs out of CREATE TABLE into ALTER TABLE statements\nCREATE TABLE my_catalog.my_schema.orders (\n    status        STRING,\n    total_amount  DECIMAL(12,2)\n);\nALTER TABLE my_catalog.my_schema.orders\n  ADD CONSTRAINT chk_status CHECK (status IN ('NEW','SHIPPED'));\nALTER TABLE my_catalog.my_schema.orders\n  ADD CONSTRAINT chk_total_amount CHECK (total_amount >= 0);\n```\n\nWhen the Oracle CHECK is unnamed, synthesize a stable name (`chk_<table>_<column>` or `chk_<table>_<purpose>`) so the constraint can be referenced later by `ALTER TABLE ... DROP CONSTRAINT`.\n\n```sql\n-- Oracle: enforced foreign key\nALTER TABLE orders ADD CONSTRAINT fk_customer\n  FOREIGN KEY (customer_id) REFERENCES customers(id);\n\n-- DBSQL: informational only; use UC three-part names\nALTER TABLE my_catalog.my_schema.orders ADD CONSTRAINT fk_customer\n  FOREIGN KEY (customer_id) REFERENCES my_catalog.my_schema.customers(id) NOT ENFORCED;\n\n-- Oracle: named CHECK constraint added via ALTER (enforced)\nALTER TABLE orders ADD CONSTRAINT chk_amount CHECK (amount > 0);\n\n-- DBSQL: same shape — CHECK added via ALTER, also enforced\nALTER TABLE my_catalog.my_schema.orders ADD CONSTRAINT chk_amount CHECK (amount > 0);\n```\n\n**`ON DELETE CASCADE` / `ON UPDATE CASCADE`**: Oracle enforces referential actions. DBSQL does not enforce foreign keys at all, so these clauses are meaningless — strip them and flag that cascade behavior must be implemented in application code or `MERGE` statements.\n\n**Deferrable constraints (`DEFERRABLE INITIALLY DEFERRED`)**: No DBSQL equivalent — strip.\n\n### ALTER TABLE — column changes\n\n| Oracle                                              | DBSQL                                                           | Notes                                                                                                                                                                                                                                     |\n| --------------------------------------------------- | --------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `ALTER TABLE t ADD (col NUMBER)`                    | `ALTER TABLE t ADD COLUMN col DECIMAL(38,0)`                    | DBSQL requires `COLUMN` keyword; `ADD (..)` parenthesized Oracle syntax must be flattened                                                                                                                                                 |\n| `ALTER TABLE t ADD (col NUMBER DEFAULT 0 NOT NULL)` | `ALTER TABLE t ADD COLUMN col DECIMAL(38,0) NOT NULL DEFAULT 0` | Delta accepts NOT NULL with DEFAULT for new columns                                                                                                                                                                                       |\n| `ALTER TABLE t MODIFY (col NUMBER(18,4))`           | Not supported                                                   | Cannot change column type on a Delta table in-place. Drop/recreate or `CAST` in queries.                                                                                                                                                  |\n| `ALTER TABLE t MODIFY (col NOT NULL)`               | `ALTER TABLE t ALTER COLUMN col SET NOT NULL`                   | Supported                                                                                                                                                                                                                                 |\n| `ALTER TABLE t DROP COLUMN col`                     | `ALTER TABLE t DROP COLUMN col`                                 | Supported; Delta retains data until `REORG TABLE ... APPLY (PURGE)`                                                                                                                                                                       |\n| `ALTER TABLE t RENAME COLUMN old TO new`            | `ALTER TABLE t RENAME COLUMN old TO new`                        | Supported. **Caveat**: any `FOREIGN KEY ... REFERENCES t(old)` declared on another table will silently go stale — DBSQL FKs are `NOT ENFORCED`, so the rename is not rejected. Hunt for and update every referencing FK after the rename. |\n\n```sql\n-- Oracle: widen a column type\nALTER TABLE orders MODIFY (notes VARCHAR2(4000));\n\n-- DBSQL: no in-place type change\n-- Option 1: cast at query time (non-destructive)\nSELECT CAST(notes AS STRING) AS notes FROM my_catalog.my_schema.orders;\n-- Option 2: recreate with new type (destructive)\nCREATE TABLE my_catalog.my_schema.orders_new AS SELECT *, CAST(notes AS STRING) AS notes FROM my_catalog.my_schema.orders;\n```\n\n#### Table-level ALTER TABLE\n\nOracle exposes many table-level operations beyond column changes. Most map to a small set of DBSQL operations or are no-ops.\n\n| Oracle                                                                              | DBSQL                                                                         | Notes                                                                                                                                                                                 |\n| ----------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `ALTER TABLE t RENAME TO new_t`                                                     | `ALTER TABLE my_catalog.my_schema.t RENAME TO my_catalog.my_schema.new_t`     | Direct. Cross-schema rename is allowed in DBSQL.                                                                                                                                      |\n| `RENAME old_t TO new_t`                                                             | `ALTER TABLE my_catalog.my_schema.old_t RENAME TO my_catalog.my_schema.new_t` | Oracle's bare `RENAME` statement maps to `ALTER TABLE ... RENAME`.                                                                                                                    |\n| `ALTER TABLE t MOVE [TABLESPACE ts]`                                                | Drop                                                                          | Storage relocation — DBSQL storage is UC-managed.                                                                                                                                     |\n| `ALTER TABLE t MOVE COMPRESS [...]`                                                 | Drop                                                                          | Delta compression is automatic (per-Parquet codec); not user-tunable here.                                                                                                            |\n| `ALTER TABLE t SHRINK SPACE [CASCADE]` / `DEALLOCATE UNUSED`                        | Use `OPTIMIZE my_catalog.my_schema.t` and/or `VACUUM my_catalog.my_schema.t`  | Compaction is `OPTIMIZE`; reclaiming storage from deleted files is `VACUUM` (subject to retention). See `07-performance-tuning.md`.                                                   |\n| `ALTER TABLE t READ ONLY` / `READ WRITE`                                            | Express via permissions                                                       | Use `REVOKE MODIFY` / `GRANT MODIFY` to control writability. There is no per-table read-only flag.                                                                                    |\n| `ALTER TABLE t LOGGING` / `NOLOGGING`                                               | Drop                                                                          | Delta always writes to the transaction log — no NOLOGGING mode.                                                                                                                       |\n| `ALTER TABLE t [NO]CACHE` / `[NO]MONITORING`                                        | Drop                                                                          | Buffer-pool / monitoring hints — irrelevant in DBSQL.                                                                                                                                 |\n| `ALTER TABLE t COMPRESS [BASIC \\| OLTP \\| FOR QUERY/ARCHIVE \\| ADVANCED]`           | Drop                                                                          | Delta compression is automatic. For column encoding, see `07-performance-tuning.md` \xa7Z-ORDER and liquid clustering.                                                                   |\n| `ALTER TABLE t ENABLE/DISABLE TABLE LOCK`                                           | Drop                                                                          | Locking is MVCC in DBSQL — not user-controlled.                                                                                                                                       |\n| `ALTER TABLE t ENABLE/DISABLE ROW MOVEMENT`                                         | Drop                                                                          | Partition-key updates always allowed in Delta.                                                                                                                                        |\n| `ALTER TABLE t ENABLE/DISABLE NOVALIDATE CONSTRAINT c`                              | `ALTER TABLE ... { ALTER \\| DROP } CONSTRAINT c`                              | DBSQL has no per-constraint enable/disable flag. To skip validation on a `CHECK`, drop it; to suppress an unenforced informational PK/FK/UNIQUE, drop and recreate as `NOT ENFORCED`. |\n| `ALTER TABLE t MODIFY CONSTRAINT c ...`                                             | `ALTER TABLE ... { ALTER \\| DROP } CONSTRAINT c`                              | DBSQL has no in-place constraint modification — drop and re-add.                                                                                                                      |\n| `ALTER TABLE t SET UNUSED COLUMN c` / `DROP UNUSED COLUMNS`                         | `ALTER TABLE ... DROP COLUMN c`                                               | DBSQL has no two-phase logical-then-physical drop. Treat both Oracle steps as a single `DROP COLUMN`.                                                                                 |\n| `ALTER TABLE t RENAME CONSTRAINT old TO new`                                        | `ALTER TABLE ... DROP CONSTRAINT old; ALTER TABLE ... ADD CONSTRAINT new ...` | DBSQL has no constraint rename — drop and re-add with the new name. Same shape as `MODIFY CONSTRAINT` above.                                                                          |\n| `ALTER TABLE t ADD SUPPLEMENTAL LOG DATA (...) COLUMNS`                             | Drop, replace with Delta change data feed                                     | Set `delta.enableChangeDataFeed = true` on the table to surface row-level changes.                                                                                                    |\n| `ALTER TABLE t FLASHBACK ARCHIVE [name]` / `NO FLASHBACK ARCHIVE`                   | Drop                                                                          | Flashback retention is governed by `delta.logRetentionDuration` / `delta.deletedFileRetentionDuration` — see `02-select-dml.md` \xa7Flashback queries.                                   |\n| `COMMENT ON TABLE t IS '...'`                                                       | `COMMENT ON TABLE my_catalog.my_schema.t IS '...'`                            | Direct.                                                                                                                                                                               |\n| `COMMENT ON COLUMN t.c IS '...'`                                                    | `ALTER TABLE my_catalog.my_schema.t ALTER COLUMN c COMMENT '...'`             | Different syntactic shape, same intent.                                                                                                                                               |\n| `ALTER TABLE t SET TBLPROPERTIES (...)` (post-12c via `ALTER TABLE ... ATTRIBUTES`) | `ALTER TABLE my_catalog.my_schema.t SET TBLPROPERTIES (...)`                  | Use TBLPROPERTIES for Delta-specific knobs (`delta.enableChangeDataFeed`, `delta.columnMapping.mode`, `delta.minReaderVersion`, etc.).                                                |\n\n### DEFAULT Constraints\n\n| Oracle default expression | DBSQL equivalent                                                                                                                                                                                                                                                                                                       |\n| ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `DEFAULT SYSDATE`         | `DEFAULT CURRENT_TIMESTAMP`                                                                                                                                                                                                                                                                                            |\n| `DEFAULT SYSTIMESTAMP`    | `DEFAULT CURRENT_TIMESTAMP`                                                                                                                                                                                                                                                                                            |\n| `DEFAULT CURRENT_DATE`    | `DEFAULT CURRENT_DATE` (DATE) or `DEFAULT CURRENT_TIMESTAMP` (TIMESTAMP) — match column type                                                                                                                                                                                                                           |\n| `DEFAULT SYS_GUID()`      | `DEFAULT UUID()`                                                                                                                                                                                                                                                                                                       |\n| `DEFAULT USER`            | `DEFAULT CURRENT_USER()`                                                                                                                                                                                                                                                                                               |\n| `DEFAULT 0`               | `DEFAULT 0` — direct.                                                                                                                                                                                                                                                                                                  |\n| `DEFAULT ''`              | **Behavioral change — not a direct translation.** Oracle stores `''` as `NULL`; DBSQL stores it as an empty string. If the source relied on the Oracle behavior (e.g. `WHERE col IS NULL` matching empty-string inserts), translate to `DEFAULT NULL` and audit predicates — see `00-quirks.md` \xa7Empty string is NULL. |\n| `DEFAULT seq.NEXTVAL`     | Use `GENERATED AS IDENTITY` on the column instead — see Identity Columns section for the `ALWAYS` vs `BY DEFAULT` decision (default to `BY DEFAULT` when uncertain).                                                                                                                                                   |\n\n```sql\n-- Oracle\nCREATE TABLE orders (\n    order_id    NUMBER GENERATED ALWAYS AS IDENTITY,\n    created_at  DATE          DEFAULT SYSDATE,\n    status      VARCHAR2(20)  DEFAULT 'pending',\n    is_active   NUMBER(1)     DEFAULT 1\n);\n\n-- DBSQL\nCREATE TABLE my_catalog.my_schema.orders (\n    order_id    BIGINT     GENERATED ALWAYS AS IDENTITY,\n    created_at  TIMESTAMP  DEFAULT CURRENT_TIMESTAMP,  -- Oracle DATE includes time → TIMESTAMP\n    status      STRING     DEFAULT 'pending',\n    is_active   BOOLEAN    DEFAULT true                -- NUMBER(1) 0/1 → BOOLEAN\n);\n```\n\nNamed Oracle constraint names (`CONSTRAINT df_status DEFAULT 'pending'`) have no named-constraint equivalent for DEFAULT in DBSQL — use an inline `DEFAULT` expression and drop the constraint name.\n\n#### `DEFAULT ON NULL` (Oracle 12c+)\n\nOracle's `DEFAULT ON NULL` clause replaces explicit `NULL` writes with the default value (regular `DEFAULT` only fires when the column is absent from the `INSERT` list). DBSQL has no such modifier — `DEFAULT` only applies when the column is absent.\n\n```sql\n-- Oracle: ON NULL substitutes the default even when caller writes NULL\nCREATE TABLE t (status VARCHAR2(20) DEFAULT ON NULL 'pending');\nINSERT INTO t (status) VALUES (NULL);  -- stores 'pending'\n\n-- DBSQL: DEFAULT does NOT substitute on explicit NULL — caller writes NULL → stored NULL\n-- Workaround: COALESCE in the calling INSERT, or NOT NULL + DEFAULT to fail-fast\nCREATE TABLE my_catalog.my_schema.t (status STRING NOT NULL DEFAULT 'pending');\nINSERT INTO my_catalog.my_schema.t (status) VALUES (COALESCE(NULL, 'pending'));\n```\n\nIf the source uses `DEFAULT ON NULL` in many places, push the coalesce into a writer view or wrap the INSERT in a procedure — flag as a behavioral change to the user.\n\n#### `INVISIBLE` columns\n\nOracle 12c+ supports `col TYPE INVISIBLE` (not returned by `SELECT *`, not bound by positional `INSERT`). DBSQL has no equivalent. Translation:\n\n- Drop the `INVISIBLE` keyword.\n- The column becomes visible in `SELECT *` and bound by positional `INSERT`. **Audit every `SELECT *` and bare-`VALUES` `INSERT` against the table** — both will now see the formerly-hidden column.\n- If hiding is essential for downstream consumers, expose a view with the column omitted instead.\n\n### Virtual Columns\n\nOracle `col type GENERATED ALWAYS AS (expression) VIRTUAL` has **no direct DBSQL equivalent**. The expression is evaluated on read in Oracle.\n\n**Migration options:**\n\n1. **View** — create a view that adds the computed expression. Callers reference the view instead of the table.\n2. **Write-time computation** — compute the expression in the INSERT/MERGE and store as a regular column. Requires updating all write paths.\n3. **Generated column via CTAS** — if the table is rebuilt from a SELECT, include the expression in the `AS SELECT` list.\n\n```sql\n-- Oracle\nCREATE TABLE order_lines (\n    quantity   NUMBER,\n    unit_price NUMBER(10,2),\n    line_total NUMBER GENERATED ALWAYS AS (quantity * unit_price) VIRTUAL\n);\n\n-- DBSQL option 1: view (read-transparent)\nCREATE TABLE my_catalog.my_schema.order_lines (\n    quantity   INT,\n    unit_price DECIMAL(10,2)\n);\n\nCREATE OR REPLACE VIEW my_catalog.my_schema.order_lines_v AS\nSELECT *, quantity * unit_price AS line_total\nFROM my_catalog.my_schema.order_lines;\n\n-- DBSQL option 2: store computed value at write time\nINSERT INTO my_catalog.my_schema.order_lines (quantity, unit_price, line_total)\nVALUES (5, 9.99, 5 * 9.99);\n```\n\n### SEQUENCE objects\n\nOracle `CREATE SEQUENCE` / `.NEXTVAL` / `.CURRVAL` has a direct DBSQL parallel, with one key difference: DBSQL does **not** support `.CURRVAL`.\n\n```sql\n-- Oracle\nCREATE SEQUENCE order_seq\n    START WITH 1000\n    INCREMENT BY 1\n    MINVALUE 1000\n    MAXVALUE 9999999\n    NOCACHE\n    NOCYCLE;\n\nSELECT order_seq.NEXTVAL FROM DUAL;\nSELECT order_seq.CURRVAL FROM DUAL;  -- Oracle only: last value issued in current session\n\n-- DBSQL\nCREATE SEQUENCE my_catalog.my_schema.order_seq\n    START WITH 1000\n    INCREMENT BY 1\n    MINVALUE 1000\n    MAXVALUE 9999999;  -- NOCYCLE is the default; NOCACHE is not needed (no CACHE option in DBSQL)\n\nSELECT NEXT VALUE FOR my_catalog.my_schema.order_seq;  -- .NEXTVAL → NEXT VALUE FOR\n-- CURRVAL: no equivalent — capture the NEXTVAL into a variable, or use a RETURNING clause\n```\n\n**Inline use in `INSERT ... VALUES` (most common pattern):**\n\n```sql\n-- Oracle\nINSERT INTO orders (order_id, customer_id, amount)\nVALUES (order_seq.NEXTVAL, 42, 99.95);\n\n-- DBSQL — same rewrite rule, just inlined into the VALUES list\nINSERT INTO my_catalog.my_schema.orders (order_id, customer_id, amount)\nVALUES (NEXT VALUE FOR my_catalog.my_schema.order_seq, 42, 99.95);\n```\n\nThe same rewrite applies in `SELECT` lists, `INSERT ... SELECT`, and procedure bodies — anywhere `seq.NEXTVAL` appears as an expression, replace it with `NEXT VALUE FOR seq` using the fully-qualified sequence name.\n\n> ⚠️ **CRITICAL — Do not silently substitute a different generation strategy.** When the Oracle source body calls `seq.NEXTVAL`, the only correct DBSQL translation at the call site is `NEXT VALUE FOR <seq>`. The following common substitutions are **wrong** because they change observable behavior:\n>\n> | Wrong substitution                                                                    | Why it's wrong                                                                                                                                                                                                                                                                                               |\n> | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n> | `UUID()` (or `uuid()` / `RANDOM_UUID()` / any random string generator)                | Returns a `STRING`, not a numeric. Type-mismatches the existing `BIGINT` column, breaks any caller assuming numeric / sortable / monotonic ids, and changes id cardinality from a dense sequence to a 128-bit random space. **This is the single most common mis-translation observed.**                     |\n> | A new `IDENTITY` column on the target table, dropping the explicit `INSERT (id, ...)` | This is a **schema** change, not a body change. It only applies if the table's `CREATE TABLE` was simultaneously rewritten to use `GENERATED AS IDENTITY` (see Identity Columns section). Doing it at the procedure-body level alone leaves the column without a generator and the `INSERT` without a value. |\n> | `(SELECT MAX(id) + 1 FROM t)`                                                         | Race-condition prone under any concurrency. Oracle sequences are atomic; this read-then-insert is not.                                                                                                                                                                                                       |\n> | A hardcoded counter / global variable                                                 | DBSQL has no per-session global state. See `oracle/11-user-packages.md` \xa7Package-level state for why this never works.                                                                                                                                                                                       |\n>\n> If you genuinely need to switch from sequence to identity / UUID, that is a **schema migration**: change the column DDL, regenerate or remap existing ids, and update every caller. Don't disguise it as a procedure-body translation.\n\n> ⚠️ **`NEXT VALUE FOR` is not allowed inside `BEGIN ATOMIC` blocks.** `NEXT VALUE FOR` is a non-deterministic expression, and DBSQL `BEGIN ATOMIC` compound statements / atomic stored procedures reject non-deterministic expressions. This means an Oracle procedure that wraps `INSERT ... VALUES (seq.NEXTVAL, ...)` in a transaction (i.e. expects atomic behavior with the surrounding statements) **cannot** be translated by simply wrapping the body in `BEGIN ATOMIC`. The same restriction applies to `BEGIN ATOMIC` blocks at the script level.\n\n**When the source needed atomicity, pick one of these escape hatches (in priority order):**\n\n1. **Switch the target column to an `IDENTITY` column and drop the sequence reference from the `INSERT`.** This is almost always the right answer — DBSQL identity columns are deterministic from the INSERT's perspective and are fully `BEGIN ATOMIC`–compatible.\n\n   ```sql\n   -- Schema change: identity columns CANNOT be added via ALTER TABLE in DBSQL.\n   -- The table must be recreated. For an empty / new table, declare it directly:\n   CREATE TABLE my_catalog.my_schema.orders (\n       order_id    BIGINT GENERATED ALWAYS AS IDENTITY,\n       customer_id INT,\n       amount      DECIMAL(10,2)\n   );\n\n   -- For a populated table, copy the data into a new table and swap names\n   -- (CREATE OR REPLACE TABLE would silently destroy the existing rows):\n   CREATE TABLE my_catalog.my_schema.orders_new (\n       order_id    BIGINT GENERATED BY DEFAULT AS IDENTITY,  -- BY DEFAULT preserves existing ids\n       customer_id INT,\n       amount      DECIMAL(10,2)\n   );\n   INSERT INTO my_catalog.my_schema.orders_new (order_id, customer_id, amount)\n       SELECT order_id, customer_id, amount FROM my_catalog.my_schema.orders;\n   ALTER TABLE my_catalog.my_schema.orders     RENAME TO my_catalog.my_schema.orders_old;\n   ALTER TABLE my_catalog.my_schema.orders_new RENAME TO my_catalog.my_schema.orders;\n   -- DROP TABLE my_catalog.my_schema.orders_old; -- after verifying\n\n   -- Procedure body: drop order_id from the column list entirely\n   BEGIN ATOMIC\n       INSERT INTO my_catalog.my_schema.orders (customer_id, amount)\n       VALUES (42, 99.95);\n       -- ... other statements that need to be atomic with this INSERT ...\n   END;\n   ```\n\n   **Pick `ALWAYS` vs `BY DEFAULT` deliberately.** `GENERATED ALWAYS AS IDENTITY` rejects any user-supplied value for `order_id` — use it on greenfield tables where no writer ever passes the surrogate key. `GENERATED BY DEFAULT AS IDENTITY` lets callers supply a value (which is required when seeding the new table from the old one, and required if any existing writer still passes `order_id`).\n\n   Drop the corresponding `CREATE SEQUENCE` statement once no other code path consumes it. If other code paths (reports, exports, downstream Oracle compatibility) need the sequence retained, leave it in place but stop calling `NEXT VALUE FOR` from procedures that need `BEGIN ATOMIC`.\n\n2. **Pre-compute the sequence value outside the atomic block, then use a variable inside.** Use this when you must keep an explicit sequence (e.g. callers depend on its specific number space) but the procedure body still needs atomic semantics across multiple statements.\n\n   ```sql\n   CREATE OR REPLACE PROCEDURE my_catalog.my_schema.create_order(\n       IN p_customer_id INT, IN p_amount DECIMAL(10,2)\n   )\n   LANGUAGE SQL\n   AS BEGIN\n       DECLARE v_order_id BIGINT;\n       SET v_order_id = (SELECT NEXT VALUE FOR my_catalog.my_schema.order_seq); -- outside ATOMIC\n       BEGIN ATOMIC\n           INSERT INTO my_catalog.my_schema.orders (order_id, customer_id, amount)\n           VALUES (v_order_id, p_customer_id, p_amount);\n           INSERT INTO my_catalog.my_schema.order_audit (order_id, action)\n           VALUES (v_order_id, 'created');\n       END;\n   END;\n   ```\n\n   Note: the `SET ... NEXT VALUE FOR` lives in the outer non-atomic `AS BEGIN ... END` block. The inner `BEGIN ATOMIC` only sees the deterministic variable. Be aware that if the inner block fails after the sequence has been advanced, the burned sequence number is **not** reclaimed — sequence values are not rolled back even by atomic blocks. This matches Oracle's behavior.\n\n3. **Drop atomicity and accept per-statement auto-commit.** If callers don't actually depend on the sequence-bumping `INSERT` being atomic with the surrounding statements (very common — most Oracle procedures used `COMMIT` for durability boundaries, not atomicity), just translate the procedure as a plain `AS BEGIN ... END` and leave `NEXT VALUE FOR` inline in the `INSERT`. Add a TODO calling out the loss of atomicity if you are unsure.\n\n**Differences:**\n\n- `.NEXTVAL` → `NEXT VALUE FOR sequence_name`\n- `.CURRVAL` → **no equivalent**. If code needs the most recently issued value, store `NEXT VALUE FOR ...` into a variable: `SET v = (SELECT NEXT VALUE FOR my_catalog.my_schema.order_seq)`.\n- `CYCLE` is supported; `NOCYCLE` is the default — omit\n- `CACHE n` is not supported — remove\n- `ORDER` / `NOORDER` are not applicable — remove\n- Available on SQL warehouses and Databricks Runtime 13.0+\n\n#### `ALTER SEQUENCE` / `DROP SEQUENCE` / rename\n\n| Oracle                                             | DBSQL                                                                                | Notes                                                                                                                   |\n| -------------------------------------------------- | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------- |\n| `ALTER SEQUENCE seq INCREMENT BY n`                | `ALTER SEQUENCE my_catalog.my_schema.seq INCREMENT BY n`                             | Direct.                                                                                                                 |\n| `ALTER SEQUENCE seq MINVALUE n` / `MAXVALUE n`     | `ALTER SEQUENCE ... MINVALUE n` / `MAXVALUE n`                                       | Direct.                                                                                                                 |\n| `ALTER SEQUENCE seq RESTART [START WITH n]`        | `ALTER SEQUENCE ... RESTART [WITH n]`                                                | Direct. Without `WITH n`, restarts at the original `START WITH`.                                                        |\n| `ALTER SEQUENCE seq CACHE n` / `NOCACHE`           | Drop the clause                                                                      | DBSQL has no `CACHE` knob — sequences allocate values on demand.                                                        |\n| `ALTER SEQUENCE seq CYCLE` / `NOCYCLE`             | `ALTER SEQUENCE ... CYCLE` / `NO CYCLE`                                              | Direct (note `NO CYCLE` is two words in DBSQL; `NOCYCLE` is one word in Oracle).                                        |\n| `ALTER SEQUENCE seq ORDER` / `NOORDER`             | Drop the clause                                                                      | Oracle RAC ordering knob — meaningless in DBSQL.                                                                        |\n| `ALTER SEQUENCE seq SCALE` / `NOSCALE` / `EXTEND`  | Drop the clause                                                                      | Scalable sequences (Oracle 18c+) — no DBSQL equivalent. Pre-existing scaled values must remain in their existing space. |\n| `ALTER SEQUENCE seq KEEP` / `NOKEEP` (Oracle 21c+) | Drop the clause                                                                      | Application-continuity hint — meaningless in DBSQL.                                                                     |\n| `RENAME old_seq TO new_seq`                        | `ALTER SEQUENCE my_catalog.my_schema.old_seq RENAME TO my_catalog.my_schema.new_seq` | Direct. Update all `NEXT VALUE FOR` call sites.                                                                         |\n| `DROP SEQUENCE seq`                                | `DROP SEQUENCE my_catalog.my_schema.seq`                                             | Direct. Use `IF EXISTS` for idempotent migration scripts.                                                               |\n\n> **Sequence values are not transactional.** Both Oracle and DBSQL advance a sequence even if the surrounding statement rolls back. `ALTER SEQUENCE ... RESTART` does not reclaim previously-issued values — it just resets the next-value pointer. If the migrated workload relies on contiguous, non-skipped sequence values (a known anti-pattern in Oracle too), flag it: neither dialect can guarantee this.\n\n### Partitioning → Liquid Clustering\n\nOracle supports range, list, hash, and composite partitioning. DBSQL uses **liquid clustering** (preferred) or partitioning on a low-cardinality column — but the migration target is usually liquid clustering, which automatically reorganizes data.\n\n```sql\n-- Oracle: range partition on order_date\nCREATE TABLE orders (...)\nPARTITION BY RANGE (order_date) (\n    PARTITION p2023 VALUES LESS THAN (DATE '2024-01-01'),\n    PARTITION p2024 VALUES LESS THAN (DATE '2025-01-01')\n);\n\n-- DBSQL: use liquid clustering (no per-partition thresholds needed)\nCREATE TABLE my_catalog.my_schema.orders (...) CLUSTER BY (order_date);\n```\n\nFor hash/list partitioning, translate to `CLUSTER BY` on the same column. Drop the partition definitions and flag that retention/drop-partition behavior needs to be re-implemented via `DELETE` on the clustered column.\n\n#### Partitioning variants\n\n| Oracle partitioning style                                         | DBSQL translation                                                                                                                          |\n| ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |\n| `PARTITION BY RANGE (col)`                                        | `CLUSTER BY (col)` — drop the per-partition value boundaries.                                                                              |\n| `PARTITION BY LIST (col)`                                         | `CLUSTER BY (col)`.                                                                                                                        |\n| `PARTITION BY HASH (col) PARTITIONS n`                            | `CLUSTER BY (col)` — Delta's clustering layout is automatic; don't try to fix the bucket count.                                            |\n| `PARTITION BY RANGE (col) INTERVAL (NUMTOYMINTERVAL(1, 'MONTH'))` | `CLUSTER BY (col)` — interval partitioning becomes implicit. Delta auto-grows; nothing to maintain.                                        |\n| `PARTITION BY REFERENCE (fk)`                                     | TODO — DBSQL has no reference partitioning. Cluster the child table on the same column the parent clusters on (typically the foreign key). |\n| Composite (`PARTITION BY RANGE (a) SUBPARTITION BY HASH (b)`)     | `CLUSTER BY (a, b)`.                                                                                                                       |\n| `PARTITION BY SYSTEM`                                             | TODO — system partitioning has no equivalent. Flatten to a single Delta table.                                                             |\n\n#### Partition-management DDL\n\nOracle exposes a wide surface for partition lifecycle management. Most of it has **no DBSQL equivalent** because Delta partitions are file-layout artifacts, not first-class metadata. Below is the migration mapping.\n\n| Oracle                                                                                     | DBSQL action                                                                                                                                                  |\n| ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `ALTER TABLE t ADD PARTITION p VALUES (...)`                                               | Drop — Delta partitions/clusters are created on demand by writes.                                                                                             |\n| `ALTER TABLE t DROP PARTITION p` / `DROP PARTITION FOR (val)`                              | `DELETE FROM my_catalog.my_schema.t WHERE col = val` (or a range predicate). Follow with `OPTIMIZE` + `VACUUM` if disk reclaim matters.                       |\n| `ALTER TABLE t TRUNCATE PARTITION p`                                                       | `DELETE FROM ... WHERE ...` — DBSQL has no per-partition `TRUNCATE`.                                                                                          |\n| `ALTER TABLE t SPLIT PARTITION p AT (val) INTO (...)`                                      | Drop — Delta auto-manages file layout.                                                                                                                        |\n| `ALTER TABLE t MERGE PARTITIONS p1, p2 INTO p3`                                            | Drop — `OPTIMIZE` consolidates files automatically.                                                                                                           |\n| `ALTER TABLE t MOVE PARTITION p TABLESPACE ts`                                             | Drop — no tablespaces in DBSQL.                                                                                                                               |\n| `ALTER TABLE t EXCHANGE PARTITION p WITH TABLE staging_t`                                  | Use `INSERT OVERWRITE my_catalog.my_schema.t REPLACE WHERE col = val SELECT * FROM staging_t` for partition-replace semantics, or a `DELETE` + `INSERT` pair. |\n| `ALTER TABLE t COALESCE PARTITION` (hash)                                                  | Drop — Delta clustering is auto.                                                                                                                              |\n| `ALTER TABLE t MODIFY PARTITION p {REBUILD UNUSABLE LOCAL INDEXES \\| SHRINK SPACE \\| ...}` | Drop — index-management directives have no DBSQL equivalent. For compaction, run `OPTIMIZE` over the whole table.                                             |\n| `ALTER TABLE t MODIFY DEFAULT ATTRIBUTES FOR PARTITION ...`                                | Drop — Delta has no per-partition default attributes.                                                                                                         |\n| `ALTER TABLE t ADD/DROP/MODIFY SUBPARTITION ...`                                           | Drop — same as PARTITION rules above. Composite partitioning collapses to multi-column `CLUSTER BY`.                                                          |\n| `ALTER TABLE t {ENABLE \\| DISABLE} ROW MOVEMENT`                                           | Drop — partition-key updates are always allowed in Delta.                                                                                                     |\n| `SELECT ... FROM t PARTITION (p)` / `SUBPARTITION (sp)`                                    | Replace with a `WHERE` predicate on the partition column: `SELECT ... FROM ... WHERE col = val`. (Cross-references `02-select-dml.md` \xa7SELECT patterns.)      |\n| `LOCK TABLE t PARTITION (p) IN EXCLUSIVE MODE`                                             | Remove — DBSQL has no row/partition locks. See `02-select-dml.md` \xa7FOR UPDATE.                                                                                |\n\n> **Partition-name dependencies leak.** Oracle code often references partition names directly (`t PARTITION (p_2024_q1)`) for partition-pruning hints, partition-exchange ETL, or operational scripts. After migrating to liquid clustering, **partition names no longer exist** — every such reference is a broken call site that the agent must rewrite as a value-predicate. Scan the source for the patterns `PARTITION (` and `SUBPARTITION (` and rewrite each one explicitly; do not silently delete them.\n\n### Index-Organized Tables (IOT)\n\nOracle `ORGANIZATION INDEX` stores the table in primary-key order. DBSQL: use liquid clustering on the primary key columns (see IOT example in Indexes section above).\n\n### External Tables\n\nOracle `ORGANIZATION EXTERNAL (TYPE ORACLE_LOADER ... LOCATION (...))` has no direct equivalent. Migrate to a managed Delta table loaded via `COPY INTO` from a Unity Catalog external location or volume.\n\n```sql\n-- Oracle external table\nCREATE TABLE ext_sales (...) ORGANIZATION EXTERNAL (\n    TYPE ORACLE_LOADER\n    DEFAULT DIRECTORY data_dir\n    LOCATION ('sales.csv')\n);\n\n-- DBSQL: managed Delta table + COPY INTO from UC external location\nCREATE TABLE my_catalog.my_schema.ext_sales (...);\n\nCOPY INTO my_catalog.my_schema.ext_sales\nFROM 'abfss://container@account.dfs.core.windows.net/data/sales.csv'\nFILEFORMAT = CSV\nFORMAT_OPTIONS ('header' = 'true');\n```\n\n> For full `COPY INTO` and UC external location syntax, load `skills/writing-sql/foreign-tables.md`.\n\n### Triggers\n\nOracle `CREATE TRIGGER` (`BEFORE`/`AFTER` `INSERT`/`UPDATE`/`DELETE`, statement-level or row-level) has **no direct DBSQL equivalent**. Options:\n\n1. **Move logic to the application or ETL layer** — the trigger body becomes part of the INSERT/UPDATE statement\n2. **Use Delta Live Tables with expectations** for data-quality triggers\n3. **TODO** for audit triggers that cannot be refactored\n\n```sql\n-- Oracle audit trigger\nCREATE OR REPLACE TRIGGER audit_orders\nAFTER INSERT OR UPDATE OR DELETE ON orders\nFOR EACH ROW\nBEGIN\n    INSERT INTO audit_log (table_name, action, changed_at)\n    VALUES ('orders', ORA_SYSEVENT, SYSDATE);\nEND;\n/\n\n-- DBSQL: no trigger support\n-- TODO: Oracle trigger 'audit_orders' must be refactored — move audit logic into the\n-- INSERT/UPDATE/DELETE pipeline or enable Delta change data feed + a downstream job.\n```\n\nIdentity-generation triggers (`BEFORE INSERT ... :NEW.id := seq.NEXTVAL`) should be replaced by `GENERATED AS IDENTITY` on the column — see Identity Columns section. Pick `ALWAYS` vs `BY DEFAULT` from the trigger body: unconditional assignment → `ALWAYS`; assignment guarded by `IF :NEW.id IS NULL` → `BY DEFAULT` (the guard means the caller may supply an id, which `ALWAYS` would reject). When uncertain, default to `BY DEFAULT`. Preserve the table's Oracle `PRIMARY KEY` as `CONSTRAINT pk_<table> PRIMARY KEY (col) NOT ENFORCED` — do not drop it.\n\n#### Trigger variants and management DDL\n\nMost Oracle trigger variants share the same outcome — **no DBSQL equivalent, refactor into the write pipeline or a downstream job**. The variant matters because the recommended refactor differs and because the agent's TODO/blocker output should name the variant explicitly.\n\n| Oracle pattern                                                                                                         | Refactor target / TODO note                                                                                                                                                                                                                    |\n| ---------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `BEFORE INSERT … :NEW.id := seq.NEXTVAL`                                                                               | Replace column with `GENERATED AS IDENTITY` (preserve the table's Oracle `PRIMARY KEY` as `NOT ENFORCED`) — see Identity Columns. Pick `ALWAYS` (unconditional assignment) vs `BY DEFAULT` (`IF :NEW.id IS NULL` guard) from the trigger body. |\n| `BEFORE INSERT/UPDATE … :NEW.col := <expr>` (default-value or normalization)                                           | Move into the calling `INSERT`/`UPDATE` (compute the value at write time) **or** define a generated/virtual column via a view (see Virtual Columns).                                                                                           |\n| `BEFORE UPDATE … RAISE_APPLICATION_ERROR` (validation)                                                                 | Move to a `CHECK` constraint when expressible; otherwise validate in the writer (notebook/job/app) or a downstream `expectations` test in DLT.                                                                                                 |\n| `AFTER INSERT/UPDATE/DELETE` row-level (audit / mirror table)                                                          | Two options: (a) mirror via Delta change data feed + a downstream MERGE; (b) write the audit row from the same `INSERT`/`MERGE` statement explicitly.                                                                                          |\n| `AFTER INSERT/UPDATE/DELETE` statement-level                                                                           | Move post-statement work into the calling job. Statement-level granularity has no effect on the rewrite — it just affects how often the body fires.                                                                                            |\n| `INSTEAD OF INSERT/UPDATE/DELETE ON view`                                                                              | TODO — make the view's underlying tables the write targets, or refactor to a stored procedure that fans out to base tables.                                                                                                                    |\n| `COMPOUND TRIGGER` (BEFORE STATEMENT / BEFORE EACH ROW / AFTER EACH ROW / AFTER STATEMENT timing points sharing state) | TODO — merge all timing points into a single set-based pipeline; per-row mutable state cannot be preserved. Often signals a hand-rolled mutating-table workaround that is no longer needed.                                                    |\n| `FOLLOWS other_trig` / `PRECEDES other_trig`                                                                           | Drop ordering clauses — irrelevant when triggers are removed. Capture the implied execution order in the rewritten pipeline.                                                                                                                   |\n| `WHEN (predicate)` clause on a trigger                                                                                 | Inline the predicate into the calling statement's `WHERE` (for AFTER triggers, into the downstream MERGE/INSERT).                                                                                                                              |\n| `REFERENCING NEW AS n OLD AS o`                                                                                        | Map to `n.col` / `o.col` references in the rewritten statement; `OLD` columns come from the source side of a `MERGE` or from `change_data_feed`.                                                                                               |\n| Database-level system triggers (`AFTER LOGON`, `AFTER STARTUP`, `BEFORE SHUTDOWN`, `AFTER SERVERERROR`)                | TODO — out of scope. These are operational hooks, not data logic. Replace with cluster init scripts, audit logs, or workspace-level config.                                                                                                    |\n| Schema/DDL triggers (`BEFORE/AFTER CREATE/ALTER/DROP/GRANT/REVOKE`)                                                    | TODO — out of scope. Use Unity Catalog audit logs and account-level system tables for change tracking.                                                                                                                                         |\n\n#### Trigger management DDL\n\n| Oracle                                           | DBSQL action                                                                                                                                          |\n| ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `ALTER TRIGGER trg DISABLE`                      | **Critical**: a disabled trigger has no runtime effect — its body **must not be migrated**. Drop the entire trigger from the migration scope.         |\n| `ALTER TRIGGER trg ENABLE`                       | No-op — DBSQL has no triggers. Treat as documentation that the body **was** active.                                                                   |\n| `ALTER TRIGGER trg COMPILE`                      | No-op — drop entirely.                                                                                                                                |\n| `ALTER TRIGGER trg RENAME TO new_trg`            | No-op once the trigger is removed; if migration is staged, just rename the refactored job/pipeline component.                                         |\n| `ALTER TABLE t {ENABLE \\| DISABLE} ALL TRIGGERS` | Same as per-trigger ENABLE/DISABLE — DISABLE means the bodies should not be migrated.                                                                 |\n| `DROP TRIGGER trg`                               | No-op once the trigger is removed. In a migration script that aims to be re-runnable, emit a comment noting the original trigger has been refactored. |\n\n### Views\n\nOracle views map closely to DBSQL views — most differences are in optional clauses that have no DBSQL equivalent.\n\n| Oracle                                                                                                 | DBSQL                                                                                                                                                        | Notes                                                                                                                                                                                                                                                         |\n| ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `CREATE VIEW v AS SELECT ...`                                                                          | `CREATE VIEW my_catalog.my_schema.v AS SELECT ...`                                                                                                           | Direct.                                                                                                                                                                                                                                                       |\n| `CREATE OR REPLACE VIEW v AS SELECT ...`                                                               | `CREATE OR REPLACE VIEW my_catalog.my_schema.v AS SELECT ...`                                                                                                | Direct.                                                                                                                                                                                                                                                       |\n| `CREATE FORCE VIEW v AS SELECT ...` (allow creation when base objects missing)                         | Drop `FORCE` — DBSQL parses the view body at creation and will reject unresolved references. If the base table is created later in the same script, reorder. |\n| `CREATE NO FORCE VIEW`                                                                                 | Drop `NO FORCE` — that is the DBSQL default behavior.                                                                                                        |                                                                                                                                                                                                                                                               |\n| `CREATE VIEW ... WITH READ ONLY`                                                                       | Drop the clause                                                                                                                                              | DBSQL views are read-only by default; DML against a view is rejected unless an `INSTEAD OF` trigger exists (which DBSQL does not support).                                                                                                                    |\n| `CREATE VIEW ... WITH CHECK OPTION`                                                                    | TODO                                                                                                                                                         | DBSQL has no `WITH CHECK OPTION`. If the source allowed `INSERT`/`UPDATE` through the view with predicate validation, push the validation into the writer.                                                                                                    |\n| `CREATE EDITIONING VIEW`                                                                               | TODO                                                                                                                                                         | Edition-based redefinition is Oracle-only. Use UC schema versioning or a parallel-write strategy instead.                                                                                                                                                     |\n| `CREATE MATERIALIZED VIEW mv AS SELECT ...`                                                            | `CREATE MATERIALIZED VIEW my_catalog.my_schema.mv AS SELECT ...`                                                                                             | DBSQL supports MVs on serverless SQL warehouses. `REFRESH FAST/COMPLETE/FORCE`, `ON COMMIT/ON DEMAND`, `BUILD IMMEDIATE/DEFERRED`, query rewrite, log/prebuilt-table options have no direct equivalents — see `07-performance-tuning.md` \xa7Materialized views. |\n| `CREATE OR REPLACE MATERIALIZED VIEW mv ...`                                                           | Same — DBSQL supports `OR REPLACE`.                                                                                                                          |                                                                                                                                                                                                                                                               |\n| `ALTER VIEW v COMPILE`                                                                                 | No-op                                                                                                                                                        | DBSQL has no compile-time validation hook to invoke; the view is recompiled on the next read.                                                                                                                                                                 |\n| `ALTER VIEW v RENAME TO new_v`                                                                         | `ALTER VIEW my_catalog.my_schema.v RENAME TO my_catalog.my_schema.new_v`                                                                                     | Direct.                                                                                                                                                                                                                                                       |\n| `ALTER VIEW v ADD/MODIFY/DROP CONSTRAINT ...`                                                          | Drop                                                                                                                                                         | DBSQL views do not carry constraints.                                                                                                                                                                                                                         |\n| `ALTER MATERIALIZED VIEW mv {COMPILE \\| ENABLE QUERY REWRITE \\| DISABLE QUERY REWRITE \\| REFRESH ...}` | `REFRESH MATERIALIZED VIEW my_catalog.my_schema.mv`                                                                                                          | Refresh maps to `REFRESH MATERIALIZED VIEW`. Query rewrite is automatic in DBSQL — drop the clause.                                                                                                                                                           |\n| `DROP VIEW v` / `DROP VIEW v CASCADE CONSTRAINTS`                                                      | `DROP VIEW my_catalog.my_schema.v`                                                                                                                           | Drop `CASCADE CONSTRAINTS` — DBSQL views have no constraints. Use `IF EXISTS` for idempotent scripts.                                                                                                                                                         |\n| `DROP MATERIALIZED VIEW mv`                                                                            | `DROP MATERIALIZED VIEW my_catalog.my_schema.mv`                                                                                                             | Direct.                                                                                                                                                                                                                                                       |\n| `RENAME old_view TO new_view`                                                                          | `ALTER VIEW my_catalog.my_schema.old_view RENAME TO my_catalog.my_schema.new_view`                                                                           | Same as `ALTER VIEW ... RENAME`.                                                                                                                                                                                                                              |\n| `COMMENT ON COLUMN v.col IS '...'`                                                                     | `ALTER VIEW my_catalog.my_schema.v ALTER COLUMN col COMMENT '...'`                                                                                           | Direct, different syntax shape. `COMMENT ON TABLE`/`COMMENT ON VIEW` work in both dialects.                                                                                                                                                                   |\n\n### Schemas\n\nOracle conflates \"schema\" with \"user\" — every Oracle user has a schema of the same name, created by `CREATE USER`. DBSQL has Unity Catalog schemas (databases) inside catalogs, with users/groups/principals managed separately at the account level.\n\n| Oracle                                                                                                                          | DBSQL                                                                                                                                              | Notes                                                                                                                                                     |\n| ------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `CREATE USER alice IDENTIFIED BY pwd`                                                                                           | TODO — out of scope                                                                                                                                | User/principal management is account-level in Databricks (SCIM, OAuth). The migration should not create users.                                            |\n| `CREATE USER alice ... DEFAULT TABLESPACE ts QUOTA ...`                                                                         | TODO — drop                                                                                                                                        | Tablespaces and quotas have no Unity Catalog equivalent. Storage is managed via UC managed/external locations.                                            |\n| `CREATE SCHEMA AUTHORIZATION user GRANT ... CREATE TABLE ... CREATE VIEW ...` (Oracle's multi-statement schema-creation bundle) | Split into separate statements: one `CREATE SCHEMA my_catalog.my_schema`, then each `CREATE TABLE`/`CREATE VIEW`/`GRANT` as a top-level statement. | The Oracle form is a single atomic DDL bundle that no longer has a parallel — DBSQL has no atomic multi-object schema bundle, just sequential statements. |\n| `CREATE SCHEMA my_schema`                                                                                                       | `CREATE SCHEMA my_catalog.my_schema`                                                                                                               | Direct. Use `IF NOT EXISTS` for idempotent scripts. Three-part naming requires the catalog component.                                                     |\n| `ALTER USER alice {QUOTA ... \\| DEFAULT TABLESPACE ... \\| TEMPORARY TABLESPACE ...}`                                            | TODO                                                                                                                                               | Storage-management metadata — drop entirely. Storage routing is handled by UC managed/external locations on the schema.                                   |\n| `ALTER SCHEMA s {RENAME \\| OWNER ...}`                                                                                          | `ALTER SCHEMA my_catalog.my_schema RENAME TO my_catalog.new_name` / `ALTER SCHEMA ... OWNER TO `principal``                                        | Both supported in DBSQL.                                                                                                                                  |\n| `DROP USER alice [CASCADE]`                                                                                                     | TODO — out of scope                                                                                                                                | Account-level user removal — does not happen in a SQL migration.                                                                                          |\n| `DROP SCHEMA s` / `DROP SCHEMA s CASCADE`                                                                                       | `DROP SCHEMA my_catalog.my_schema [CASCADE]`                                                                                                       | Direct — `CASCADE` drops contained objects in both dialects.                                                                                              |\n| `COMMENT ON SCHEMA s IS '...'`                                                                                                  | `COMMENT ON SCHEMA my_catalog.my_schema IS '...'`                                                                                                  | Direct.                                                                                                                                                   |\n\n> **No tablespaces, no rollback segments, no temporary tablespaces, no UNDO.** Strip every storage-clause that references these in `CREATE`/`ALTER` DDL. Delta storage is managed by Unity Catalog — there is no per-object placement directive in DBSQL.\n\n### Synonyms\n\nOracle `CREATE SYNONYM` / `CREATE PUBLIC SYNONYM` have no DBSQL equivalent. Options:\n\n1. Replace all references to the synonym with the fully-qualified target name\n2. Create a view that selects from the target: `CREATE VIEW my_catalog.my_schema.syn_name AS SELECT * FROM my_catalog.other_schema.target`\n\nViews do not support DML pass-through in all cases — if the synonym is used for writes, flag it and rewrite the write statements to target the underlying table directly.\n"}}]);
//# sourceMappingURL=https://sourcemaps.dev.databricks.com/monolith/static/js/27567.ff5d19445a.chunk.js.map