"use strict";(self.webpackChunkdatabricks_monolith=self.webpackChunkdatabricks_monolith||[]).push([[30998],{330998:e=>{e.exports="# Oracle Built-in PL/SQL Packages (`DBMS_*`, `UTL_*`)\n\nMaster map for Oracle-supplied PL/SQL packages. Use this file when the source references any `DBMS_*` or `UTL_*` package member that is not already covered by a topic file.\n\nThe Oracle PL/SQL Packages and Types Reference defines roughly 250 supplied packages with thousands of members. The vast majority have **no DBSQL equivalent** — they expose database-engine internals (background jobs, advanced queuing, file I/O, network sockets, online redefinition, AWR) that Databricks Lakehouse architecture does not have. This file catalogs the high-traffic packages with per-member translation rules, and groups the obscure / out-of-scope packages in an appendix with a single TODO recommendation.\n\n## ORACLE\n\n> Already covered elsewhere — do **not** duplicate the translation here:\n>\n> - `04-procedures-variables.md` \xa7`DBMS_OUTPUT.PUT_LINE` → driver logs\n> - `04-procedures-variables.md` \xa7`DBMS_SQL.*` (Method 4 dynamic SQL — `OPEN_CURSOR`, `PARSE`, `BIND_VARIABLE`, `DEFINE_COLUMN`, `EXECUTE`, `FETCH_ROWS`, `COLUMN_VALUE`, `CLOSE_CURSOR`, `RETURN_RESULT`, `GET_NEXT_RESULT`)\n> - `04b-error-handling.md` \xa7Error-stack helpers (`DBMS_UTILITY.FORMAT_ERROR_STACK` / `FORMAT_CALL_STACK` / `FORMAT_ERROR_BACKTRACE`, `UTL_CALL_STACK.*`)\n>\n> The tables below pick up everything else.\n\n### `DBMS_RANDOM` — random numbers and strings\n\n| Oracle                                                   | DBSQL                                                            | Notes                                                                                                                                |\n| -------------------------------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |\n| `DBMS_RANDOM.VALUE`                                      | `RAND()`                                                         | Both return a `[0, 1)` double.                                                                                                       |\n| `DBMS_RANDOM.VALUE(lo, hi)`                              | `lo + (hi - lo) * RAND()`                                        | Inclusive lower, exclusive upper in both dialects.                                                                                   |\n| `DBMS_RANDOM.NORMAL`                                     | `RANDN()`                                                        | Standard-normal sample; same statistical shape.                                                                                      |\n| `DBMS_RANDOM.STRING('U' \\| 'L' \\| 'A' \\| 'X' \\| 'P', n)` | manual rewrite                                                   | No direct DBSQL function. Build with `array_join(transform(sequence(1, n), i -> chr(...)), '')` against the desired character class. |\n| `DBMS_RANDOM.SEED(seed)`                                 | `RAND(seed)` (per-call seeded form)                              | DBSQL `RAND` accepts an optional `BIGINT` seed at the call site; there is no global seed setter.                                     |\n| `DBMS_RANDOM.RANDOM`                                     | `CAST(RAND() * POWER(2, 31) AS INT) - CAST(POWER(2, 30) AS INT)` | Returns a signed 32-bit integer; rewrite at the call site.                                                                           |\n| `DBMS_RANDOM.INITIALIZE(n)` / `DBMS_RANDOM.TERMINATE`    | drop                                                             | Pre-Oracle-11g session-level initialization. No DBSQL counterpart, no semantic effect after migration.                               |\n\n### `DBMS_LOB` — LOB manipulation\n\nDBSQL has no `CLOB` / `BLOB` / `BFILE` types. Translate `CLOB` → `STRING`, `BLOB` → `BINARY`, and the LOB-locator API collapses to ordinary string/binary functions (see `03-data-types.md`).\n\n| Oracle                                                            | DBSQL                                                                     | Notes                                                                                                                |\n| ----------------------------------------------------------------- | ------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |\n| `DBMS_LOB.GETLENGTH(lob)`                                         | `LENGTH(s)` (chars) / `OCTET_LENGTH(b)` (bytes)                           | Pick the function that matches the LOB type the original code was using.                                             |\n| `DBMS_LOB.SUBSTR(lob, n, p)`                                      | `SUBSTR(s, p, n)`                                                         | **Argument-order swap** — Oracle is `(lob, length, offset)`, DBSQL is `(string, offset, length)`. Easy to get wrong. |\n| `DBMS_LOB.INSTR(lob, pat, p, k)`                                  | `INSTR(s, pat, p, k)` or `LOCATE(pat, s, p)`                              | DBSQL has no `k`-th-occurrence form on `LOCATE`; if `k > 1`, use a regex with `regexp_extract_all` then index.       |\n| `DBMS_LOB.APPEND(dest, src)`                                      | `SET dest = CONCAT(dest, src)` (in scripting) or `dest \\|\\| src` (in SQL) | Plain concatenation.                                                                                                 |\n| `DBMS_LOB.WRITE` / `WRITEAPPEND`                                  | `SET s = CONCAT(s, value)` / column update                                | LOB locators do not exist; assign or update the column directly.                                                     |\n| `DBMS_LOB.READ`                                                   | `SUBSTR` / `SUBSTRING_INDEX`                                              | Same — no streaming locator API in DBSQL.                                                                            |\n| `DBMS_LOB.COMPARE(l1, l2)`                                        | `CASE WHEN l1 = l2 THEN 0 WHEN l1 < l2 THEN -1 ELSE 1 END`                | Plain comparison; DBSQL has no signed-compare scalar.                                                                |\n| `DBMS_LOB.COPY`                                                   | `SET dest = src`                                                          | Locator copy collapses to assignment.                                                                                |\n| `DBMS_LOB.CREATETEMPORARY` / `FREETEMPORARY`                      | drop                                                                      | No temporary-LOB concept in DBSQL.                                                                                   |\n| `DBMS_LOB.OPEN` / `CLOSE` / `ISOPEN`                              | drop                                                                      | No LOB-locator lifecycle in DBSQL.                                                                                   |\n| `DBMS_LOB.LOADFROMFILE` / `LOADBLOBFROMFILE` / `LOADCLOBFROMFILE` | TODO                                                                      | File ingestion — use `COPY INTO` from a Unity Catalog Volume or Auto Loader. Not a body-level translation.           |\n| `DBMS_LOB.FILEOPEN` / `FILECLOSE` / `FILEEXISTS` / `FILEGETNAME`  | TODO                                                                      | `BFILE` operations — see `UTL_FILE` row below for the same recommendation.                                           |\n| `DBMS_LOB.CONVERTTOBLOB` / `CONVERTTOCLOB`                        | `CAST(s AS BINARY)` / `CAST(b AS STRING)`                                 | Encoding-aware conversions — confirm the destination charset; DBSQL is UTF-8 only.                                   |\n\n### `DBMS_UTILITY` — process / hashing helpers (subset)\n\nThe error-stack helpers (`FORMAT_ERROR_STACK`, `FORMAT_CALL_STACK`, `FORMAT_ERROR_BACKTRACE`) are covered in `04b-error-handling.md` \xa7Error-stack helpers. The remaining commonly used members:\n\n| Oracle                                                                     | DBSQL                                            | Notes                                                                                                                                      |\n| -------------------------------------------------------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------ |\n| `DBMS_UTILITY.GET_TIME`                                                    | `UNIX_MILLIS(CURRENT_TIMESTAMP)`                 | Oracle returns hundredths of a second; DBSQL milliseconds. Adjust the math at every difference site.                                       |\n| `DBMS_UTILITY.GET_CPU_TIME`                                                | drop or `UNIX_MILLIS(CURRENT_TIMESTAMP)` proxy   | No DBSQL equivalent for CPU time. Wall-clock is usually a fine substitute for crude profiling — flag with a TODO if precise CPU is needed. |\n| `DBMS_UTILITY.GET_HASH_VALUE(s, base, size)`                               | `pmod(hash(s), size) + base`                     | Oracle returns a `NUMBER` in `[base, base + size - 1]`. `hash()` in DBSQL returns a 32-bit `INT`; use `pmod` to clamp.                     |\n| `DBMS_UTILITY.GET_PARAMETER_VALUE`                                         | TODO                                             | Reads init-parameter values; no Lakehouse equivalent.                                                                                      |\n| `DBMS_UTILITY.NAME_RESOLVE` / `NAME_TOKENIZE`                              | manual                                           | Identifier introspection. Replace with explicit string parsing if needed.                                                                  |\n| `DBMS_UTILITY.COMPILE_SCHEMA`                                              | drop                                             | No separate compile step in DBSQL.                                                                                                         |\n| `DBMS_UTILITY.ANALYZE_SCHEMA` / `ANALYZE_DATABASE` / `ANALYZE_PART_OBJECT` | `ANALYZE TABLE ... COMPUTE STATISTICS`           | Per-table only in DBSQL; loop over tables if a schema-wide refresh was the intent.                                                         |\n| `DBMS_UTILITY.EXEC_DDL_STATEMENT(stmt)`                                    | `EXECUTE IMMEDIATE stmt` (in scripting)          | Direct dynamic-DDL substitute.                                                                                                             |\n| `DBMS_UTILITY.DB_VERSION(:v, :compat)`                                     | `SELECT current_version()` (Photon) or hard-code | DBSQL has no compatibility-version concept.                                                                                                |\n\n### `DBMS_OUTPUT` (overflow rules)\n\n`DBMS_OUTPUT.PUT_LINE` is covered in `04-procedures-variables.md`. Edge cases:\n\n| Oracle                                        | DBSQL                  | Notes                                                                                                  |\n| --------------------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------ |\n| `DBMS_OUTPUT.PUT(s)` (no newline)             | drop or `RAISE NOTICE` | DBSQL has no put-without-newline form. Concatenate before logging.                                     |\n| `DBMS_OUTPUT.NEW_LINE`                        | drop                   | Same — concat the newline yourself if it actually matters.                                             |\n| `DBMS_OUTPUT.ENABLE(buffer_size)` / `DISABLE` | drop                   | SQL\\*Plus / SQL Developer notion; no DBSQL counterpart.                                                |\n| `DBMS_OUTPUT.GET_LINE` / `GET_LINES`          | TODO                   | Reading back the output buffer. The migration-side equivalent is to `INSERT INTO log` and `SELECT` it. |\n\n### `DBMS_APPLICATION_INFO` — module / action / client info tagging\n\nOracle uses these to tag sessions for V$SESSION introspection. DBSQL has no V$SESSION; the closest substitutes are query tags via cluster tags, JDBC properties, or `SET` statements that surface in the query history.\n\n| Oracle                                                   | DBSQL                                                               | Notes                                                                                                                           |\n| -------------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |\n| `DBMS_APPLICATION_INFO.SET_MODULE(m, a)`                 | drop or `SET STATEMENT TIMEOUT` (unrelated) — log into a side table | No DBSQL equivalent. If observability matters, write `(module, action, query_id)` rows into a logging table at procedure entry. |\n| `DBMS_APPLICATION_INFO.SET_ACTION(a)`                    | drop                                                                | Same.                                                                                                                           |\n| `DBMS_APPLICATION_INFO.SET_CLIENT_INFO(c)`               | drop                                                                | Same.                                                                                                                           |\n| `DBMS_APPLICATION_INFO.READ_MODULE` / `READ_CLIENT_INFO` | drop                                                                | The values were set by the same session — track them in a local variable instead.                                               |\n| `DBMS_APPLICATION_INFO.SET_SESSION_LONGOPS`              | drop                                                                | Long-op progress reporting; no DBSQL equivalent.                                                                                |\n\n### `DBMS_SESSION` — session-level settings\n\n| Oracle                                                               | DBSQL                                                                    | Notes                                                                                                         |\n| -------------------------------------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- |\n| `DBMS_SESSION.SET_NLS('NLS_DATE_FORMAT', 'YYYY-MM-DD')`              | `SET TIME ZONE ...` (for time zone) — date formats are per-call in DBSQL | DBSQL has no session-level format string; pass the format model to every `to_date` / `date_format` call site. |\n| `DBMS_SESSION.SET_TIME_ZONE('UTC')`                                  | `SET TIME ZONE 'UTC'`                                                    | Direct.                                                                                                       |\n| `DBMS_SESSION.SET_CONTEXT('ns', 'attr', 'value')`                    | `SET VARIABLE` (Databricks SQL session variables)                        | DBSQL session variables are flat (no namespace). Concat `ns_attr` if the namespace mattered.                  |\n| `DBMS_SESSION.CLEAR_CONTEXT` / `CLEAR_ALL_CONTEXT`                   | `RESET VARIABLE`                                                         | Clears one or all session variables.                                                                          |\n| `DBMS_SESSION.SET_IDENTIFIER` / `CLEAR_IDENTIFIER`                   | drop                                                                     | Tags the session for VPD policies — see `DBMS_RLS` row.                                                       |\n| `DBMS_SESSION.SWITCH_CURRENT_CONSUMER_GROUP`                         | TODO                                                                     | Resource Manager — no DBSQL equivalent at the SQL layer; use SQL warehouse sizing or compute policies.        |\n| `DBMS_SESSION.RESET_PACKAGE`                                         | drop                                                                     | Resets per-session package state. With packages flattened, package state is gone — no-op after migration.     |\n| `DBMS_SESSION.MODIFY_PACKAGE_STATE(DBMS_SESSION.FREE_ALL_RESOURCES)` | drop                                                                     | Same.                                                                                                         |\n\n### `DBMS_LOCK` — sleep and named locks\n\n| Oracle                                              | DBSQL           | Notes                                                                                                                                                                     |\n| --------------------------------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `DBMS_LOCK.SLEEP(seconds)`                          | TODO — refactor | DBSQL has no sleep primitive in SQL scripting. If the caller was polling, redesign the loop to fetch on a timer from the orchestrator (Workflows / Lakeflow).             |\n| `DBMS_LOCK.ALLOCATE_UNIQUE` / `REQUEST` / `RELEASE` | TODO            | Named user-locks have no DBSQL equivalent. Refactor concurrency control to an idempotent `MERGE` with a guard predicate, or to a Workflow with `max_concurrent_runs = 1`. |\n| `DBMS_LOCK.CONVERT`                                 | TODO            | Same.                                                                                                                                                                     |\n\n### `DBMS_CRYPTO` — encryption and hashing\n\n| Oracle                                                                      | DBSQL                                                                    | Notes                                                                                                                                  |\n| --------------------------------------------------------------------------- | ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- |\n| `DBMS_CRYPTO.HASH(src, DBMS_CRYPTO.HASH_MD5)`                               | `unhex(md5(src))`                                                        | DBSQL `md5(string)` returns hex; wrap with `unhex` to get raw bytes if Oracle's binary `RAW` was the consumed form.                    |\n| `DBMS_CRYPTO.HASH(src, DBMS_CRYPTO.HASH_SH1)`                               | `sha1(src)` (hex) or `unhex(sha1(src))` (binary)                         | Same.                                                                                                                                  |\n| `DBMS_CRYPTO.HASH(src, DBMS_CRYPTO.HASH_SH256 \\| HASH_SH384 \\| HASH_SH512)` | `sha2(src, 256 \\| 384 \\| 512)`                                           | Direct.                                                                                                                                |\n| `DBMS_CRYPTO.MAC(src, typ, key)`                                            | `hmac(key, src, 'SHA-256')` (or other digest)                            | DBSQL `hmac` covers the common MAC variants.                                                                                           |\n| `DBMS_CRYPTO.ENCRYPT(src, typ, key, iv)` / `DECRYPT`                        | `aes_encrypt(src, key, mode, padding, iv)` / `aes_decrypt`               | Map Oracle's `ENCRYPT_AES256 + CHAIN_CBC + PAD_PKCS5` to DBSQL `aes_encrypt(s, k, 'CBC', 'PKCS', iv)`. Confirm key derivation matches. |\n| `DBMS_CRYPTO.RANDOMBYTES(n)`                                                | manual: `cast(rand_bytes(n) as binary)` (Photon) or stage from `randn()` | DBSQL has no built-in cryptographic RNG; for non-security uses, `rand_bytes` is fine. For real key generation, generate outside SQL.   |\n\n### `DBMS_OBFUSCATION_TOOLKIT` — deprecated\n\nPre-10g hashing/encryption package. Oracle deprecates it in favor of `DBMS_CRYPTO`. Map every call as if it were the equivalent `DBMS_CRYPTO` call; flag the deprecation with a TODO so the migration confirms the algorithm choice was intentional.\n\n### `DBMS_ASSERT` — input sanitization\n\n| Oracle                                              | DBSQL                                                          | Notes                                                                                                                            |\n| --------------------------------------------------- | -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |\n| `DBMS_ASSERT.SIMPLE_SQL_NAME(s)`                    | `regexp_replace(s, '[^a-zA-Z0-9_]', '')` + length check        | Returns the input if it is a valid simple identifier; raises `INVALID_SQL_NAME` otherwise. DBSQL validation is manual.           |\n| `DBMS_ASSERT.QUALIFIED_SQL_NAME(s)`                 | manual regex + dot-split                                       | Same idea for `schema.object` patterns.                                                                                          |\n| `DBMS_ASSERT.SCHEMA_NAME(s)` / `SQL_OBJECT_NAME(s)` | `IDENTIFIER(:s)` with a pre-check                              | DBSQL `IDENTIFIER(:bind)` performs the safe-identifier substitution; pre-validate against a regex if the input is user-supplied. |\n| `DBMS_ASSERT.ENQUOTE_LITERAL(s)`                    | `replace(s, '''', '''''')` + wrap                              | Standard quote-doubling.                                                                                                         |\n| `DBMS_ASSERT.ENQUOTE_NAME(s)`                       | <code>concat('\\\\`', replace(s, '\\\\`', '\\\\`\\\\`'), '\\\\`')</code> | Backtick-quoted identifier in DBSQL.                                                                                             |\n| `DBMS_ASSERT.NOOP(s)`                               | drop — return `s`                                              | Identity function.                                                                                                               |\n\n### `DBMS_METADATA` — DDL extraction\n\n| Oracle                                                                                                 | DBSQL                                                                                | Notes                                                                                                                   |\n| ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------- |\n| `DBMS_METADATA.GET_DDL('TABLE', name, owner)`                                                          | `SHOW CREATE TABLE catalog.schema.name`                                              | Direct equivalent for tables and views. Output dialect differs — flag if downstream tooling parsed the Oracle DDL form. |\n| `DBMS_METADATA.GET_DDL('VIEW' \\| 'INDEX' \\| 'TRIGGER' \\| 'PROCEDURE' \\| 'FUNCTION' \\| 'PACKAGE', ...)` | `SHOW CREATE TABLE` (views) / `SHOW CREATE PROCEDURE` / `DESCRIBE FUNCTION EXTENDED` | Triggers and packages have no DBSQL equivalent — DDL extraction is moot.                                                |\n| `DBMS_METADATA.GET_GRANTED_DDL(...)` / `GET_DEPENDENT_DDL(...)`                                        | `SHOW GRANTS ON ...` / `DESCRIBE TABLE EXTENDED`                                     | Use UC information schema views (`system.information_schema.table_privileges`) for programmatic enumeration.            |\n| `DBMS_METADATA.SET_TRANSFORM_PARAM` / `OPEN` / `FETCH` / `CLOSE`                                       | TODO                                                                                 | Streaming-DDL fetch API has no DBSQL equivalent.                                                                        |\n\n### `DBMS_DDL` — DDL helpers\n\n| Oracle                                                          | DBSQL                                                 | Notes                                                                                                                          |\n| --------------------------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |\n| `DBMS_DDL.ALTER_COMPILE`                                        | drop                                                  | No separate compile step.                                                                                                      |\n| `DBMS_DDL.ANALYZE_OBJECT(typ, sch, obj, method)`                | `ANALYZE TABLE catalog.schema.obj COMPUTE STATISTICS` | Direct for tables; no equivalent for indexes (DBSQL has no traditional index — Liquid Clustering / data skipping replaces it). |\n| `DBMS_DDL.SET_TRIGGER_FIRING_PROPERTY` / `IS_TRIGGER_FIRE_ONCE` | drop                                                  | Triggers have no DBSQL counterpart.                                                                                            |\n\n### `DBMS_STATS` — optimizer statistics (selected)\n\nDBSQL collects statistics automatically (predictive / Photon) and via explicit `ANALYZE TABLE`. Most `DBMS_STATS` members map to either a single `ANALYZE TABLE` or a TODO.\n\n| Oracle                                                                          | DBSQL                                                                                  | Notes                                                                                               |\n| ------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |\n| `DBMS_STATS.GATHER_TABLE_STATS(ownname, tabname, ...)`                          | `ANALYZE TABLE catalog.schema.tabname COMPUTE STATISTICS [FOR COLUMNS col1, col2]`     | Direct. Drop `estimate_percent`, `method_opt`, `granularity`, `cascade` — DBSQL handles internally. |\n| `DBMS_STATS.GATHER_SCHEMA_STATS` / `GATHER_DATABASE_STATS`                      | loop over tables and call `ANALYZE TABLE`                                              | DBSQL has no schema/database-wide form. Generate the loop in the orchestrator.                      |\n| `DBMS_STATS.GATHER_INDEX_STATS`                                                 | drop                                                                                   | No traditional indexes in DBSQL — Liquid Clustering and bloom filters do not need explicit stats.   |\n| `DBMS_STATS.DELETE_TABLE_STATS` / `DELETE_COLUMN_STATS` / `DELETE_SCHEMA_STATS` | `ANALYZE TABLE ... DROP STATISTICS`                                                    | Per-table only.                                                                                     |\n| `DBMS_STATS.SET_TABLE_PREFS` / `SET_GLOBAL_PREFS`                               | drop                                                                                   | DBSQL has no preference layer.                                                                      |\n| `DBMS_STATS.LOCK_TABLE_STATS` / `UNLOCK_TABLE_STATS`                            | drop                                                                                   | No lock-stats concept in DBSQL.                                                                     |\n| `DBMS_STATS.IMPORT_TABLE_STATS` / `EXPORT_TABLE_STATS`                          | TODO                                                                                   | Stats portability has no DBSQL equivalent.                                                          |\n| `DBMS_STATS.GET_TABLE_STATS(...)` (OUT params)                                  | `DESCRIBE TABLE EXTENDED catalog.schema.tabname` or `system.information_schema.tables` | Read stats from the metastore views.                                                                |\n\n### `DBMS_XPLAN` — explain output\n\n| Oracle                                                   | DBSQL                                     | Notes                                                                                                                  |\n| -------------------------------------------------------- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |\n| `DBMS_XPLAN.DISPLAY` / `DISPLAY_PLAN` / `DISPLAY_CURSOR` | `EXPLAIN [FORMATTED \\| EXTENDED] <query>` | Different output shape; if the original code parsed columns out of `DBMS_XPLAN.DISPLAY`, the parser must change.       |\n| `DBMS_XPLAN.DISPLAY_AWR(sql_id, ...)`                    | TODO                                      | AWR has no Lakehouse equivalent — Databricks query history / system tables (`system.query.history`) is the substitute. |\n\n### `DBMS_SCHEDULER` and `DBMS_JOB` — scheduling\n\nBoth are out of SQL-migration scope. Replace the schedule with a Databricks **Workflow** (or DLT/Lakeflow pipeline) that calls the migrated procedure. Emit a single TODO per `CREATE_JOB` / `SUBMIT` call and document the schedule cron string for the migration consumer.\n\n| Oracle                                                                                    | DBSQL                      | Notes                                                                                                                                                  |\n| ----------------------------------------------------------------------------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `DBMS_SCHEDULER.CREATE_JOB` / `RUN_JOB` / `STOP_JOB` / `DROP_JOB` / `ENABLE` / `DISABLE`  | TODO — Databricks Workflow | Capture the `job_action`, `repeat_interval`, `start_date` / `end_date`, `enabled` flag in the TODO so the migration consumer can rebuild the workflow. |\n| `DBMS_SCHEDULER.CREATE_PROGRAM` / `CREATE_SCHEDULE` / `CREATE_JOB_CLASS` / `CREATE_CHAIN` | TODO                       | Each Oracle scheduler concept maps to a Workflow primitive.                                                                                            |\n| `DBMS_JOB.SUBMIT(:n, 'plsql_block', sysdate, 'sysdate + 1/24')`                           | TODO — Databricks Workflow | Legacy Oracle 8i scheduling API; same recommendation.                                                                                                  |\n| `DBMS_JOB.REMOVE` / `BROKEN` / `RUN`                                                      | TODO                       | Same.                                                                                                                                                  |\n\n### `DBMS_AQ`, `DBMS_AQADM` — Advanced Queuing\n\nOut of scope for SQL migration. Replace with a streaming source (Kafka, Kinesis) and a Structured Streaming consumer, or with Delta tables + CDF for table-as-queue patterns.\n\n| Oracle                                                                                         | DBSQL                               | Notes                                                                                |\n| ---------------------------------------------------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------ |\n| `DBMS_AQ.ENQUEUE` / `DEQUEUE` / `LISTEN` / `BIND_AGENT`                                        | TODO — streaming source / Delta CDF | Capture the queue name, payload type, and consumer subscription pattern in the TODO. |\n| `DBMS_AQADM.CREATE_QUEUE_TABLE` / `CREATE_QUEUE` / `START_QUEUE` / `STOP_QUEUE` / `DROP_QUEUE` | TODO                                | Replace with a Delta table with CDF enabled.                                         |\n\n### `DBMS_PIPE`, `DBMS_ALERT` — inter-session messaging\n\nNo DBSQL equivalent. Sessions in Databricks are isolated; cross-session messaging belongs in the orchestrator (Workflows triggers, webhooks, notifications).\n\n| Oracle                                                                                                           | DBSQL                         | Notes                                                                                                        |\n| ---------------------------------------------------------------------------------------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------ |\n| `DBMS_PIPE.SEND_MESSAGE` / `RECEIVE_MESSAGE` / `PACK_MESSAGE` / `UNPACK_MESSAGE` / `CREATE_PIPE` / `REMOVE_PIPE` | TODO — orchestrator messaging | Replace with Workflow `run_job_task` chaining or webhooks.                                                   |\n| `DBMS_ALERT.REGISTER` / `WAITONE` / `WAITANY` / `SIGNAL`                                                         | TODO                          | Same recommendation. For \"rerun this query when t changes\", use Delta CDF + a Structured Streaming consumer. |\n\n### `DBMS_MVIEW`, `DBMS_REFRESH` — materialized view refresh\n\n| Oracle                                              | DBSQL                                                     | Notes                                                                                                                                    |\n| --------------------------------------------------- | --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |\n| `DBMS_MVIEW.REFRESH('schema.mv', 'C')`              | `REFRESH MATERIALIZED VIEW catalog.schema.mv`             | Direct. The `'C'` (complete) / `'F'` (fast) / `'P'` (partition) / `'?'` (force) modes do not exist — DBSQL chooses the optimal strategy. |\n| `DBMS_MVIEW.REFRESH_DEPENDENT`                      | loop over dependents and call `REFRESH MATERIALIZED VIEW` | Use `system.information_schema.tables` to enumerate dependents.                                                                          |\n| `DBMS_MVIEW.REFRESH_ALL_MVIEWS`                     | loop over MVs and call `REFRESH MATERIALIZED VIEW`        | Same.                                                                                                                                    |\n| `DBMS_REFRESH.MAKE` / `ADD` / `REFRESH` / `DESTROY` | drop                                                      | Refresh-group concept; collapse to direct `REFRESH MATERIALIZED VIEW` calls.                                                             |\n\n### `DBMS_PARALLEL_EXECUTE` — chunked DML\n\nOracle splits a large DML into rowid-range chunks and parallelizes them. DBSQL does not need this — Spark partitions DML automatically.\n\n| Oracle                                                                                                                    | DBSQL                                  | Notes                                                                                                              |\n| ------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |\n| `DBMS_PARALLEL_EXECUTE.CREATE_TASK` / `CREATE_CHUNKS_BY_ROWID` / `CREATE_CHUNKS_BY_NUMBER_COL` / `RUN_TASK` / `DROP_TASK` | replace with the underlying single DML | Issue the original `UPDATE` / `MERGE` / `DELETE` directly. Spark parallelizes by file. Drop the chunking entirely. |\n\n### `DBMS_RLS` — Virtual Private Database (row-level security)\n\n| Oracle                                                                                             | DBSQL                                                            | Notes                                                                                                                                                                                                  |\n| -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `DBMS_RLS.ADD_POLICY('sch', 't', 'policy_name', 'sch', 'pol_func', 'select,insert,update,delete')` | `ALTER TABLE catalog.schema.t SET ROW FILTER my_filter ON (...)` | Translate the Oracle policy function into a DBSQL row-filter UDF and bind it via Unity Catalog `SET ROW FILTER`. Per-statement-type policies are not supported — DBSQL row filters apply to all reads. |\n| `DBMS_RLS.DROP_POLICY` / `ENABLE_POLICY`                                                           | `ALTER TABLE ... DROP ROW FILTER`                                | Direct.                                                                                                                                                                                                |\n| `DBMS_RLS.ADD_GROUPED_POLICY` / `CREATE_POLICY_GROUP`                                              | TODO                                                             | Policy groups have no DBSQL equivalent — flatten to one filter per table.                                                                                                                              |\n| `SYS_CONTEXT('USERENV', 'CURRENT_USER')` (frequently inside policy functions)                      | `current_user()`                                                 | Direct.                                                                                                                                                                                                |\n\n### `DBMS_NETWORK_ACL_ADMIN` — outbound network ACLs\n\nOut of scope. Databricks egress is governed by the network policy of the workspace / cluster, not by SQL-level ACLs.\n\n| Oracle                                                                            | DBSQL | Notes                                                         |\n| --------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------- |\n| `DBMS_NETWORK_ACL_ADMIN.CREATE_ACL` / `ADD_PRIVILEGE` / `ASSIGN_ACL` / `DROP_ACL` | drop  | Workspace network policy / IP access lists handle this layer. |\n\n### `DBMS_RESOURCE_MANAGER` / `DBMS_RESOURCE_MANAGER_PRIVS`\n\nOut of scope. Use SQL warehouse sizing / serverless compute policies / cluster policies instead. Drop every call and emit a single TODO at the procedure level naming the original consumer group.\n\n### `DBMS_TRANSACTION`\n\n| Oracle                                                    | DBSQL        | Notes                                                                                                         |\n| --------------------------------------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------- |\n| `DBMS_TRANSACTION.READ_ONLY` / `READ_WRITE`               | drop         | DBSQL has no session-level read-only mode. A read-only intent is enforced by `GRANT SELECT` only.             |\n| `DBMS_TRANSACTION.SAVEPOINT(name)` / `ROLLBACK_SAVEPOINT` | drop or TODO | DBSQL has no `SAVEPOINT`; see `00-quirks.md` validation row.                                                  |\n| `DBMS_TRANSACTION.COMMIT` / `ROLLBACK`                    | drop         | Statement-level commit/rollback are removed in DBSQL — see `04-procedures-variables.md` \xa7Transaction control. |\n| `DBMS_TRANSACTION.LOCAL_TRANSACTION_ID`                   | TODO         | No equivalent; surface a UUID at procedure entry if a correlation id is needed.                               |\n\n### `DBMS_XMLGEN`, `DBMS_XMLQUERY`, `DBMS_XMLDOM`, `DBMS_XSLPROCESSOR`\n\nDBSQL has no native XML. The recommendation in `02-select-dml.md` \xa7XML construction applies — emit JSON via `to_json` + `named_struct` + `collect_list`. If the consumer truly requires XML on the wire, format as a string with manual escaping; flag with a TODO.\n\n### `UTL_FILE` — file I/O on the database server\n\nOut of scope. Database-server-side file I/O does not exist in Lakehouse architecture. Replace with:\n\n- **Read inputs:** Auto Loader / `COPY INTO` from a Unity Catalog Volume.\n- **Write outputs:** `COPY TO` (DBSQL streaming) or `df.write` from a notebook task.\n\n| Oracle                                                | DBSQL                          | Notes                                                                  |\n| ----------------------------------------------------- | ------------------------------ | ---------------------------------------------------------------------- |\n| `UTL_FILE.FOPEN(dir, filename, 'r' \\| 'w' \\| 'a')`    | TODO — UC Volume + Auto Loader | The Oracle `directory` object maps to a UC external location / volume. |\n| `UTL_FILE.GET_LINE` / `PUT_LINE` / `PUTF` / `PUT`     | TODO                           | Same.                                                                  |\n| `UTL_FILE.FCLOSE` / `FCLOSE_ALL` / `FFLUSH`           | TODO                           | Same.                                                                  |\n| `UTL_FILE.FREMOVE` / `FRENAME` / `FCOPY` / `FGETATTR` | TODO                           | Same — file-management operations belong in the orchestrator.          |\n\n### `UTL_HTTP`, `UTL_SMTP`, `UTL_MAIL`, `UTL_TCP` — outbound network\n\nOut of scope. SQL-engine-initiated network I/O has no DBSQL counterpart. Move HTTP / SMTP / TCP calls to a notebook (Python `requests`, `smtplib`) or a webhook task in a Workflow.\n\n| Oracle                                                                      | DBSQL                              | Notes                                                                            |\n| --------------------------------------------------------------------------- | ---------------------------------- | -------------------------------------------------------------------------------- |\n| `UTL_HTTP.BEGIN_REQUEST` / `GET_RESPONSE` / `READ_TEXT` / `END_RESPONSE`    | TODO — notebook / Workflow webhook | Capture the URL, headers, and method in the TODO.                                |\n| `UTL_SMTP.OPEN_CONNECTION` / `MAIL` / `RCPT` / `DATA` / `QUIT`              | TODO                               | Email belongs in the orchestrator (Workflows email-on-failure) or a Python task. |\n| `UTL_MAIL.SEND` / `SEND_ATTACH_RAW` / `SEND_ATTACH_VARCHAR2`                | TODO                               | Higher-level wrapper around `UTL_SMTP`; same recommendation.                     |\n| `UTL_TCP.OPEN_CONNECTION` / `WRITE_TEXT` / `READ_TEXT` / `CLOSE_CONNECTION` | TODO                               | Raw socket I/O — Python notebook task.                                           |\n\n### `UTL_RAW` — RAW byte manipulation\n\n| Oracle                                                      | DBSQL                                                            | Notes                                                                                                                               |\n| ----------------------------------------------------------- | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |\n| `UTL_RAW.CAST_TO_VARCHAR2(r)`                               | `cast(r as string)` (UTF-8) or `decode(r, 'UTF-8')`              | Confirm the source charset.                                                                                                         |\n| `UTL_RAW.CAST_TO_RAW(s)`                                    | `cast(s as binary)` or `encode(s, 'UTF-8')`                      | Same.                                                                                                                               |\n| `UTL_RAW.CAST_TO_NUMBER(r)` / `CAST_FROM_NUMBER(n)`         | `conv(hex(r), 16, 10)::DECIMAL` (manual)                         | Oracle's RAW-as-NUMBER format is proprietary; if the original code persisted these, the values are not portable — flag with a TODO. |\n| `UTL_RAW.CONCAT(r1, r2, ...)`                               | `concat(r1, r2)` (works on `BINARY`)                             | Direct.                                                                                                                             |\n| `UTL_RAW.SUBSTR(r, p, n)` / `LENGTH(r)`                     | `substring(r, p, n)` / `octet_length(r)`                         | Direct.                                                                                                                             |\n| `UTL_RAW.BIT_AND` / `BIT_OR` / `BIT_XOR` / `BIT_COMPLEMENT` | manual: cast to integer, apply `&` / `\\|` / `^` / `~`, cast back | DBSQL has no byte-array bitwise functions; round-trip via integer for short payloads.                                               |\n| `UTL_RAW.REVERSE(r)`                                        | manual                                                           | No direct binary-reverse in DBSQL; transform via `array(...)` + `reverse` then re-cast.                                             |\n| `UTL_RAW.COMPARE(r1, r2)`                                   | `CASE WHEN r1 = r2 THEN 0 WHEN r1 < r2 THEN -1 ELSE 1 END`       | Same as the `DBMS_LOB.COMPARE` row.                                                                                                 |\n\n### `UTL_ENCODE` / `UTL_COMPRESS` — encoding and compression\n\n| Oracle                                                           | DBSQL                            | Notes                                                                                                                      |\n| ---------------------------------------------------------------- | -------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |\n| `UTL_ENCODE.BASE64_ENCODE(r)` / `BASE64_DECODE(s)`               | `base64(r)` / `unbase64(s)`      | Direct.                                                                                                                    |\n| `UTL_ENCODE.MIMEHEADER_ENCODE` / `MIMEHEADER_DECODE`             | manual                           | RFC-2047 encoded-word — manual regex translation.                                                                          |\n| `UTL_ENCODE.QUOTED_PRINTABLE_ENCODE` / `QUOTED_PRINTABLE_DECODE` | manual                           | Quoted-printable encoding — manual.                                                                                        |\n| `UTL_ENCODE.UUENCODE` / `UUDECODE`                               | manual or TODO                   | uuencode is rare; flag with a TODO if the consumer truly needs it.                                                         |\n| `UTL_COMPRESS.LZ_COMPRESS(r)` / `LZ_UNCOMPRESS(r)`               | `gzip` outside SQL, or store raw | DBSQL has no in-SQL gzip primitives; compress in the writer (Spark `option('compression', 'gzip')`) and store as `BINARY`. |\n\n### `UTL_URL` / `UTL_I18N`\n\n| Oracle                                                           | DBSQL                                       | Notes                                                                                            |\n| ---------------------------------------------------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------ |\n| `UTL_URL.ESCAPE(s)` / `UNESCAPE(s)`                              | `url_encode(s)` / `url_decode(s)`           | DBSQL `url_encode` / `url_decode` cover the common case (RFC 3986).                              |\n| `UTL_I18N.STRING_TO_RAW(s, charset)` / `RAW_TO_CHAR(r, charset)` | `encode(s, charset)` / `decode(r, charset)` | Direct. Confirm the charset name matches DBSQL's accepted set (`'UTF-8'`, `'ISO-8859-1'`, etc.). |\n| `UTL_I18N.MAP_CHARSET(s, ...)` / `GET_LOCAL_LANGUAGES`           | drop                                        | Charset-mapping helpers — no DBSQL equivalent and no semantic effect after migration.            |\n| `UTL_I18N.UNESCAPE_REFERENCE(s)` / `ESCAPE_REFERENCE`            | manual                                      | HTML/XML entity escapes — small UDF if needed.                                                   |\n\n---\n\n## Out of scope (Oracle-only feature areas)\n\nThese packages have **no DBSQL counterpart** and do not warrant a per-member translation. When the source uses any of them, emit one TODO per call, name the package and member, and recommend the Databricks alternative listed below.\n\n| Oracle package(s)                                                                       | What it does                                                | Databricks alternative                                                                          |\n| --------------------------------------------------------------------------------------- | ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |\n| `DBMS_REDEFINITION`                                                                     | Online table redefinition.                                  | Use Delta `ALTER TABLE` + `OPTIMIZE` / `CLONE`. No online-redefinition concept needed.          |\n| `DBMS_SPACE`, `DBMS_SPACE_ADMIN`, `DBMS_STORAGE_MAP`                                    | Tablespace / segment / extent management.                   | Lakehouse storage is fully managed; drop all calls.                                             |\n| `DBMS_FLASHBACK`, `DBMS_FLASHBACK_ARCHIVE`                                              | Flashback Data Archive admin.                               | Use Delta time travel (`VERSION AS OF` / `TIMESTAMP AS OF`) — see `02-select-dml.md`.           |\n| `DBMS_LOGSTDBY`, `DBMS_LOGMNR`, `DBMS_STREAMS_*`, `DBMS_XSTREAM_*`, `DBMS_GOLDENGATE_*` | Replication / log mining / GoldenGate.                      | Delta CDF + Auto Loader + Workflows; or a dedicated CDC product (Fivetran / Debezium → Delta).  |\n| `DBMS_HM`, `DBMS_REPAIR`, `DBMS_REPCAT_*`                                               | Health monitor / block corruption / replication catalog.    | No equivalent — flag with TODO.                                                                 |\n| `DBMS_RESUMABLE`                                                                        | Suspendable statements.                                     | No equivalent — failures are immediate. Refactor for retry in the orchestrator.                 |\n| `DBMS_AUDIT_MGMT`, `DBMS_FGA`                                                           | Audit policies, fine-grained auditing.                      | UC audit logs (`system.access.audit`) cover this layer — drop SQL-level config.                 |\n| `DBMS_AUTO_TASK_*`, `DBMS_HPROF`, `DBMS_PROFILER`, `DBMS_TRACE`                         | Auto-tasks, hierarchical profiler, classic profiler, trace. | Databricks query profile / Photon profile.                                                      |\n| `DBMS_WORKLOAD_REPOSITORY`, `DBMS_AWR_*`, `DBMS_ADDM`, `DBMS_SQLDIAG`, `DBMS_SQL_TUNE`  | AWR, ADDM, SQL Tuning Advisor.                              | Databricks query history (`system.query.history`) + SQL warehouse monitoring.                   |\n| `DBMS_SERVICE`, `DBMS_HA_ALERTS`                                                        | Database services / HA alerts.                              | Cluster / SQL-warehouse policies + Workflow alerts.                                             |\n| `DBMS_DG`, `DBMS_DBFS_*`                                                                | Data Guard, DBFS in-database file system.                   | Lakehouse storage is the file system; drop.                                                     |\n| `DBMS_LDAP`                                                                             | LDAP queries from PL/SQL.                                   | Identity federation is configured at the workspace / account level; drop.                       |\n| `DBMS_PREPROCESSOR`                                                                     | Conditional-compilation preprocessor introspection.         | Resolve at translation time — see `09-scripting-control-flow.md` \xa7Conditional compilation.      |\n| `DBMS_DEBUG`, `DBMS_DEBUG_JDWP`                                                         | PL/SQL debugger.                                            | No equivalent — use notebook-driven manual testing.                                             |\n| `DBMS_TYPES`                                                                            | Type-id constants for `ANYDATA`.                            | DBSQL has no `ANYDATA` — drop the type-id discrimination.                                       |\n| `DBMS_OBFUSCATION_TOOLKIT`                                                              | Deprecated crypto.                                          | Map to `DBMS_CRYPTO` row above.                                                                 |\n| `DBMS_WARNING`                                                                          | Compile-time warning configuration.                         | Drop (no compiler warnings concept in DBSQL).                                                   |\n| `OWA_*` (`OWA_UTIL`, `OWA_COOKIE`, `HTP`, `HTF`)                                        | Oracle PL/SQL web toolkit.                                  | Out of scope — rebuild as a Databricks App or external service.                                 |\n| `WPG_DOCLOAD`                                                                           | Web Toolkit document loading.                               | Same.                                                                                           |\n| `CTX_*` (Oracle Text)                                                                   | Full-text search.                                           | Databricks Vector Search + `ai_query` for semantic search; `regexp_*` for naive lexical search. |\n| `SDO_*`, `MDSYS.*` (Oracle Spatial)                                                     | Spatial / geospatial.                                       | Mosaic (`com.databricks.labs.mosaic`) or `H3` / `ST_*` Photon spatial functions.                |\n| `ORD_*` (Oracle Multimedia)                                                             | Multimedia / image / audio.                                 | No equivalent in SQL — handle in a notebook.                                                    |\n"}}]);
//# sourceMappingURL=https://sourcemaps.dev.databricks.com/monolith/static/js/30998.a8581657d0.chunk.js.map