"use strict";(self.webpackChunkdatabricks_monolith=self.webpackChunkdatabricks_monolith||[]).push([[70559],{770559:e=>{e.exports="# Performance Tuning — Hints, Indexes, and Statistics\n\nOracle optimizer hints, index hints, statistics operations, and locking constructs that have no DBSQL equivalent, plus what to drop vs. translate.\n\n> This doc is dialect-agnostic for the \"what to drop\" rules. The ORACLE section covers what to substitute.\n\n## What to drop unconditionally\n\nThese hints/options have no equivalent in DBSQL and must be removed without substitution. **Rule of thumb:** strip every `/*+ ... */` comment in the source as a first pass — DBSQL ignores them as comments anyway, but cleaning them up makes the diff readable. The table below catalogs what each hint _meant_ in Oracle so you can evaluate whether the underlying intent needs to be re-expressed (e.g. `/*+ MATERIALIZE */` → restructure as a temp view).\n\n### Optimizer hints — comprehensive catalog\n\n| Source hint family                                                                                                                                                                                                                                                                                                                     | Remove                                                                                                                                                                                                       |\n| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `/*+ ... */` optimizer hint comment (any hint)                                                                                                                                                                                                                                                                                         | DBSQL has no per-query optimizer hint mechanism — strip the entire `/*+ ... */` block. Hints inside `--+ ...` single-line form and inside `<COMMENT_KW>` SQL-comment hints are equivalent and also stripped. |\n| **Optimizer mode**: `RULE`, `CHOOSE`, `FIRST_ROWS`, `FIRST_ROWS(n)`, `ALL_ROWS`, `OPTIMIZER_FEATURES_ENABLE('21.1.0')`                                                                                                                                                                                                                 | No session/per-query optimizer mode. Spark cost-based optimizer always.                                                                                                                                      |\n| **Access path — index**: `INDEX(t idx)`, `INDEX_ASC(t idx)`, `INDEX_DESC(t idx)`, `INDEX_RS_ASC` / `INDEX_RS_DESC`, `INDEX_FFS(t idx)`, `INDEX_SS(t idx)`, `INDEX_SS_ASC` / `INDEX_SS_DESC`, `INDEX_JOIN(t i1 i2)`, `INDEX_COMBINE(t i1 i2)`, `AND_EQUAL(t i1 i2)`, `NO_INDEX(t)`, `NO_INDEX_FFS(t)`, `NO_INDEX_SS(t)`                 | No index hints. Delta uses data skipping + Z-Order / Liquid Clustering instead. Translate the _underlying intent_ (selective lookup column) by adding the column to `CLUSTER BY` — see `01-schema-ddl.md`.   |\n| **Access path — full / rowid / cluster**: `FULL(t)`, `ROWID(t)`, `CLUSTER(t)`, `HASH(t)`                                                                                                                                                                                                                                               | No equivalent — the planner picks. `CLUSTER`/`HASH` referred to single-table cluster / hash-cluster storage, neither of which migrates.                                                                      |\n| **Join method**: `USE_NL(t1 t2)`, `USE_NL_WITH_INDEX(...)`, `USE_HASH(t1 t2)`, `USE_MERGE(t1 t2)`, `USE_BAND(...)`, `USE_CUBE(...)`, `NO_USE_NL`, `NO_USE_HASH`, `NO_USE_MERGE`, `NL_AJ`, `NL_SJ`, `HASH_AJ`, `HASH_SJ`, `MERGE_AJ`, `MERGE_SJ`                                                                                        | No join-method hints. Spark adaptive query execution chooses the operator.                                                                                                                                   |\n| **Join order**: `LEADING(t1 t2 t3)`, `ORDERED`, `ORDERED_PREDICATES`                                                                                                                                                                                                                                                                   | No join-order hints. Adaptive query execution reorders.                                                                                                                                                      |\n| **Star / fact**: `STAR_TRANSFORMATION`, `NO_STAR_TRANSFORMATION`, `FACT(t)`, `NO_FACT(t)`                                                                                                                                                                                                                                              | No star-transformation hint. Spark handles dimension/fact joins through standard cost-based planning.                                                                                                        |\n| **Query transformation**: `MERGE(v)`, `NO_MERGE(v)`, `UNNEST`, `NO_UNNEST`, `PUSH_PRED(v)`, `NO_PUSH_PRED(v)`, `PUSH_SUBQ`, `NO_PUSH_SUBQ`, `USE_CONCAT`, `NO_EXPAND`, `EXPAND_GSET_TO_UNION`, `NO_QUERY_TRANSFORMATION`, `OUTLINE_LEAF(...)`, `OPT_ESTIMATE(...)`, `ELIMINATE_JOIN(t)`, `NO_ELIMINATE_JOIN(t)`, `OUTER_JOIN_TO_INNER` | No transformation hints — Spark optimizer applies its own predicate pushdown, view merging, subquery unnesting, OR-expansion, and join-elimination passes.                                                   |\n| **Materialized view rewrite**: `REWRITE(mv)`, `NO_REWRITE`, `REWRITE_OR_ERROR`                                                                                                                                                                                                                                                         | DBSQL MVs are queried by name; there is no implicit MV-rewrite mechanism. If callers relied on transparent rewrite, redirect the query to `SELECT FROM mv` explicitly.                                       |\n| **Subquery flattening / view**: `MATERIALIZE`, `INLINE` (on CTEs), `NO_QUERY_TRANSFORMATION`                                                                                                                                                                                                                                           | No CTE materialization control. The Spark optimizer decides; if you actually need to force materialization, write the CTE result to a temporary view (`CREATE OR REPLACE TEMPORARY VIEW v AS ...`).          |\n| **Parallelism**: `PARALLEL(t, n)`, `PARALLEL`, `PARALLEL_INDEX(t idx, n)`, `NO_PARALLEL`, `NO_PARALLEL_INDEX`, `PQ_DISTRIBUTE(t HASH HASH)`, `PQ_FILTER`, `PQ_SKEW`, `STATEMENT_QUEUING`, `NO_STATEMENT_QUEUING`                                                                                                                       | Parallelism is managed by the Spark engine. Warehouse / cluster sizing controls it, not query-level hints.                                                                                                   |\n| **Direct-path insert**: `APPEND`, `APPEND_VALUES`, `NOAPPEND`                                                                                                                                                                                                                                                                          | All Delta writes use bulk paths; remove. (Note: `APPEND_VALUES` enables direct-path on `INSERT ... VALUES` — DBSQL has no toggle.)                                                                           |\n| **Result cache**: `RESULT_CACHE`, `NO_RESULT_CACHE`                                                                                                                                                                                                                                                                                    | SQL Warehouse query-result cache is automatic. There is no statement-level toggle.                                                                                                                           |\n| **Dynamic sampling / runtime stats**: `DYNAMIC_SAMPLING(t n)`, `GATHER_PLAN_STATISTICS`, `MONITOR`, `NO_MONITOR`                                                                                                                                                                                                                       | Sampling is automatic. SQL monitoring lives in the Databricks UI query history, not as a hint.                                                                                                               |\n| **In-Memory Column Store**: `INMEMORY(t)`, `NO_INMEMORY(t)`, `INMEMORY_PRUNING`, `NO_INMEMORY_PRUNING`                                                                                                                                                                                                                                 | Drop. Photon + Delta are always in-memory-ready; no per-query toggle.                                                                                                                                        |\n| **Approximation (19c+/23ai)**: `APPROX_FOR_AGGREGATION`, `APPROX_FOR_COUNT_DISTINCT`, `APPROX_FOR_PERCENTILE`                                                                                                                                                                                                                          | Drop the hint. If the source intended approximation, replace each aggregate explicitly with the matching DBSQL function (`approx_count_distinct`, `approx_percentile`, `approx_top_k`).                      |\n| **PL/SQL function context**: `WITH_PLSQL`                                                                                                                                                                                                                                                                                              | Drop — DBSQL has no equivalent of declaring a query-local PL/SQL function from a hint.                                                                                                                       |\n| **Cursor sharing / bind peeking**: `CURSOR_SHARING_EXACT`, `BIND_AWARE`, `NO_BIND_AWARE`                                                                                                                                                                                                                                               | Drop. DBSQL plans are query-shape-keyed; there is no bind-peeking concept.                                                                                                                                   |\n| **Distributed**: `DRIVING_SITE(t)`                                                                                                                                                                                                                                                                                                     | Database links are not migrated; the hint has no DBSQL counterpart.                                                                                                                                          |\n| **Generic option override**: `OPT_PARAM('PARAM_NAME', value)`                                                                                                                                                                                                                                                                          | Drop. There is no per-statement parameter override.                                                                                                                                                          |\n| **Tracing / instrumentation**: `GATHER_PLAN_STATISTICS`, `TRACE_PUBLISH`, `OPTIMIZER_FEATURES_ENABLE(...)`                                                                                                                                                                                                                             | Drop. Use Spark UI / Photon UI / query profile in the Databricks UI.                                                                                                                                         |\n\n### Statistics, plan management, and session settings to drop\n\n| Source                                                                                                                                                                                                                            | Remove / replace                                                                                                                                                                              |\n| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `ANALYZE TABLE ... COMPUTE STATISTICS`, `ANALYZE INDEX ...`, `ANALYZE TABLE ... ESTIMATE STATISTICS SAMPLE n PERCENT`                                                                                                             | Delta handles statistics automatically. DBSQL exposes `ANALYZE TABLE ... COMPUTE STATISTICS [FOR COLUMNS ...]` for explicit refresh — see \"Statistics\" below.                                 |\n| `DBMS_STATS.GATHER_TABLE_STATS`, `DBMS_STATS.GATHER_SCHEMA_STATS`, `DBMS_STATS.GATHER_DATABASE_STATS`                                                                                                                             | Drop entirely; or rewrite as `ANALYZE TABLE ... COMPUTE STATISTICS [FOR COLUMNS ...]` only when callers were tuning a specific table. See `12-builtin-packages.md` \xa7`DBMS_STATS`.             |\n| `DBMS_STATS.SET_TABLE_STATS`, `DBMS_STATS.SET_COLUMN_STATS`, `DBMS_STATS.SET_INDEX_STATS`                                                                                                                                         | No equivalent — Delta does not allow injecting hand-tuned cardinality.                                                                                                                        |\n| `DBMS_STATS.GATHER_FIXED_OBJECTS_STATS`, `GATHER_DICTIONARY_STATS`, `GATHER_SYSTEM_STATS`                                                                                                                                         | No equivalent — engine self-tunes.                                                                                                                                                            |\n| `DBMS_STATS.CREATE_EXTENDED_STATS(...)` (function-based / column-group extended stats)                                                                                                                                            | Drop. DBSQL does not have extended stats. If a function-based stat mattered, materialize the expression as a generated column and `ANALYZE TABLE ... COMPUTE STATISTICS FOR COLUMNS gen_col`. |\n| `DBMS_STATS.SET_TABLE_PREFS('PUBLISH', 'FALSE')` and the `PENDING` / pending-stats workflow                                                                                                                                       | Drop entirely.                                                                                                                                                                                |\n| Histogram options on stats (`METHOD_OPT => 'FOR ALL COLUMNS SIZE 254'`, `'FOR COLUMNS ... SIZE AUTO/REPEAT/SKEWONLY'`)                                                                                                            | Drop. DBSQL stats are min/max/null-count/ndv only — there are no histograms.                                                                                                                  |\n| `DBMS_STATS.LOCK_TABLE_STATS` / `UNLOCK_TABLE_STATS` / `RESTORE_TABLE_STATS`                                                                                                                                                      | Drop. No locked-stats workflow.                                                                                                                                                               |\n| `DBMS_STATS.IMPORT_TABLE_STATS` / `EXPORT_TABLE_STATS`                                                                                                                                                                            | Drop. No portable stats interchange.                                                                                                                                                          |\n| `DBMS_SPM.LOAD_PLANS_FROM_*`, SQL Plan Baselines (`DBA_SQL_PLAN_BASELINES`), `ALTER SESSION SET OPTIMIZER_USE_SQL_PLAN_BASELINES`                                                                                                 | Out of scope — DBSQL has no plan baselines / capture / evolve workflow. See `12-builtin-packages.md` Out-of-scope appendix.                                                                   |\n| `DBMS_SQLTUNE.IMPORT_SQL_PROFILE`, SQL Profiles, SQL Patches, Outlines (`DBA_OUTLINES`)                                                                                                                                           | Out of scope — DBSQL has no profile / outline mechanism.                                                                                                                                      |\n| `DBMS_SQLTUNE.CREATE_TUNING_TASK` / `EXECUTE_TUNING_TASK`, SQL Tuning Sets, SQL Access Advisor                                                                                                                                    | Out of scope — use Databricks Query Profile + Lakehouse Monitoring.                                                                                                                           |\n| `DBMS_ADVISOR.*` / Automatic SQL Tuning / Automatic Plan Management                                                                                                                                                               | Out of scope.                                                                                                                                                                                 |\n| `ALTER SYSTEM FLUSH SHARED_POOL`, `ALTER SYSTEM FLUSH BUFFER_CACHE`, `ALTER SYSTEM SET ...`                                                                                                                                       | No equivalent — engine-managed.                                                                                                                                                               |\n| `ALTER SESSION SET OPTIMIZER_MODE = ...`                                                                                                                                                                                          | Drop.                                                                                                                                                                                         |\n| `ALTER SESSION SET CURSOR_SHARING = {EXACT \\| FORCE \\| SIMILAR}`                                                                                                                                                                  | Drop.                                                                                                                                                                                         |\n| `ALTER SESSION SET STATISTICS_LEVEL = {ALL \\| TYPICAL \\| BASIC}`                                                                                                                                                                  | Drop.                                                                                                                                                                                         |\n| `ALTER SESSION SET STAR_TRANSFORMATION_ENABLED = ...`, `OPTIMIZER_INDEX_CACHING`, `OPTIMIZER_INDEX_COST_ADJ`, `DB_FILE_MULTIBLOCK_READ_COUNT`, `HASH_AREA_SIZE`, `SORT_AREA_SIZE`, `WORKAREA_SIZE_POLICY`, `PGA_AGGREGATE_TARGET` | Drop. Engine-tuned in DBSQL.                                                                                                                                                                  |\n| `ALTER SESSION ENABLE PARALLEL {DML \\| DDL \\| QUERY}`, `ALTER SESSION FORCE PARALLEL ...`, `ALTER SESSION DISABLE PARALLEL ...`                                                                                                   | Drop.                                                                                                                                                                                         |\n| `ALTER SESSION SET SQL_TRACE = TRUE`, `ALTER SESSION SET EVENTS '...'`, `ALTER SESSION SET TIMED_STATISTICS = TRUE`, `ALTER SESSION SET TRACEFILE_IDENTIFIER = '...'`                                                             | Drop. Use the SQL Warehouse query-profile / events stream in the Databricks UI.                                                                                                               |\n| `ALTER SESSION SET RESULT_CACHE_MODE = {MANUAL \\| FORCE}`                                                                                                                                                                         | Drop. Result caching is automatic.                                                                                                                                                            |\n| `ALTER SESSION SET PARALLEL_MIN_PERCENT`, `PARALLEL_DEGREE_POLICY`, `PARALLEL_FORCE_LOCAL`                                                                                                                                        | Drop.                                                                                                                                                                                         |\n| `ALTER SESSION SET INMEMORY_QUERY = ENABLE/DISABLE`                                                                                                                                                                               | Drop.                                                                                                                                                                                         |\n| `ALTER SESSION SET CONTAINER = pdb_name` / `ALTER SESSION SET CURRENT_SCHEMA = ...`                                                                                                                                               | DBSQL has no PDB. For schema switch, use three-part-name fully qualified references everywhere; `USE CATALOG` / `USE SCHEMA` work in interactive sessions but not in batch DDL.               |\n| `ALTER DATABASE ...`, `ALTER SYSTEM ...`                                                                                                                                                                                          | Out of scope — engine / cluster management lives in the workspace admin UI.                                                                                                                   |\n\n### Locking, transaction control, and read-only modifiers\n\n| Source                                                                                                              | Remove / replace                                                                                                                          |\n| ------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |\n| `LOCK TABLE t IN {EXCLUSIVE \\| SHARE \\| ROW SHARE \\| ROW EXCLUSIVE \\| SHARE ROW EXCLUSIVE} MODE [NOWAIT \\| WAIT n]` | No table-level locks. Delta uses optimistic concurrency — concurrent writers conflict at commit time and the loser retries.               |\n| `SELECT ... FOR UPDATE [OF col] [NOWAIT \\| SKIP LOCKED \\| WAIT n]`                                                  | No row-level pessimistic locking — see `02-select-dml.md` and the \"Locking and `FOR UPDATE`\" section below.                               |\n| `SELECT ... FOR UPDATE OF c1, c2` (column-level lock projection)                                                    | Drop. Column-level locking has no DBSQL equivalent.                                                                                       |\n| `WITH READ ONLY` / `WITH CHECK OPTION` on `CREATE VIEW`                                                             | DBSQL views are read-only by default — drop `WITH READ ONLY`. `WITH CHECK OPTION` requires moving validation into the writer.             |\n| `SET TRANSACTION READ ONLY` / `SET TRANSACTION ISOLATION LEVEL SERIALIZABLE`                                        | Drop. DBSQL transactions in notebook context are auto-commit; `BEGIN ATOMIC` blocks default to a snapshot isolation that cannot be tuned. |\n| `COMMIT WRITE [BATCH \\| IMMEDIATE] [WAIT \\| NOWAIT]`                                                                | Drop the modifiers; bare `COMMIT;` itself is dropped inside procedure bodies (see `04-procedures-variables.md`).                          |\n\n## What to translate\n\n| Source                                                                                            | DBSQL equivalent                                                                                                                                                                                                                              |\n| ------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| B-tree index on lookup column                                                                     | `OPTIMIZE ... ZORDER BY (lookup_col)` or `CLUSTER BY (lookup_col)` (liquid)                                                                                                                                                                   |\n| Composite B-tree index on `(a, b, c)`                                                             | `CLUSTER BY (a, b, c)` (liquid clustering) or `OPTIMIZE ... ZORDER BY (a, b, c)`                                                                                                                                                              |\n| Unique B-tree index                                                                               | `CLUSTER BY (col)` + `CONSTRAINT u_col UNIQUE (col)`. Note Delta unique constraints are validated on write but not enforced concurrently — flag if multi-writer correctness depended on the index.                                            |\n| Function-based index (`CREATE INDEX ... ON t(UPPER(col))`)                                        | Generated column + cluster on it: `col_upper STRING GENERATED ALWAYS AS (UPPER(col))` then `CLUSTER BY (col_upper)`                                                                                                                           |\n| Bitmap index                                                                                      | Drop — Delta data skipping handles low-cardinality filters; consider Z-Order                                                                                                                                                                  |\n| Bitmap join index (`CREATE BITMAP INDEX ... ON t1, t2 ...`)                                       | Drop. The \"pre-joined\" lookup is provided by Photon's broadcast hash join + data skipping; if the lookup latency mattered, materialize the join into a Delta table (`CREATE TABLE ... AS SELECT ...` with `CLUSTER BY` on the dimension key). |\n| Reverse-key index                                                                                 | Drop — Delta has no hot-block contention                                                                                                                                                                                                      |\n| Domain index (`INDEXTYPE IS CTXSYS.CONTEXT`, `MDSYS.SPATIAL_INDEX`, `LUCENE`)                     | TODO — full-text / spatial indexes have no DBSQL equivalent. Use Vector Search + `array_contains` for text, or external tooling for spatial. See `12-builtin-packages.md` Out-of-scope (`CTX_*`, `SDO_*`, `MDSYS.*`).                         |\n| Invisible index (`CREATE INDEX ... INVISIBLE`)                                                    | Drop — Delta has no concept of invisible indexes (clustering / Z-Order are always considered).                                                                                                                                                |\n| Compressed index (`CREATE INDEX ... COMPRESS [n]`)                                                | Drop the `COMPRESS` clause; Delta storage is compressed by default.                                                                                                                                                                           |\n| Index-Organized Table (`ORGANIZATION INDEX`)                                                      | `CLUSTER BY (pk_col)` on regular Delta table                                                                                                                                                                                                  |\n| Hash cluster / index cluster (`CREATE CLUSTER ...`, `CREATE TABLE ... CLUSTER c(col)`)            | Drop the cluster object. Migrate the underlying tables as standalone Delta tables; if the cluster co-located rows for join performance, `CLUSTER BY (join_key)` on both tables.                                                               |\n| Hash-partitioned table                                                                            | `CLUSTER BY (col)` (preferred) — bucket-style partitioning is anti-pattern in Delta                                                                                                                                                           |\n| Range-partitioned table on date                                                                   | `PARTITIONED BY (date_col)` in CREATE TABLE — see `01-schema-ddl.md`                                                                                                                                                                          |\n| List-partitioned table                                                                            | `PARTITIONED BY (col)` — Delta auto-discovers distinct values                                                                                                                                                                                 |\n| Materialized view (`CREATE MATERIALIZED VIEW`)                                                    | `CREATE MATERIALIZED VIEW` (DBSQL native) or DLT pipeline                                                                                                                                                                                     |\n| `MATERIALIZED VIEW LOG ON t WITH ...`                                                             | Drop entirely. Delta CDF (`ALTER TABLE t SET TBLPROPERTIES (delta.enableChangeDataFeed = true)`) provides the same incremental-source mechanism — DBSQL MVs consume CDF transparently. See \"Materialized view extras\" below.                  |\n| `EXPLAIN PLAN FOR ...; SELECT * FROM TABLE(DBMS_XPLAN.DISPLAY)`                                   | `EXPLAIN <query>` or `EXPLAIN FORMATTED <query>`                                                                                                                                                                                              |\n| `DBMS_XPLAN.DISPLAY_PLAN`, `DBMS_XPLAN.DISPLAY_CURSOR`, `DBMS_XPLAN.DISPLAY_AWR`                  | `EXPLAIN [FORMATTED \\| EXTENDED] <query>` for cached/stored plans. AWR-style historic plans live in the Databricks query-history UI; no programmatic equivalent.                                                                              |\n| `DBMS_HPROF`, `DBMS_PROFILER` (PL/SQL profiler)                                                   | Use the SQL Warehouse query profiler in the Databricks UI. See `12-builtin-packages.md` \xa7`DBMS_HPROF`/`DBMS_PROFILER`.                                                                                                                        |\n| `DBMS_MONITOR.SESSION_TRACE_ENABLE`, `DBMS_MONITOR.SERV_MOD_ACT_TRACE_ENABLE`                     | Use the SQL Warehouse query-profile UI; no programmatic trace toggle.                                                                                                                                                                         |\n| `V$SQL`, `V$SQL_PLAN`, `V$SQL_PLAN_STATISTICS_ALL`, `V$SESSION_LONGOPS`, `V$SQLAREA`, `GV$*`      | Use the Databricks **query history** (UI + system tables `system.query.history`); no programmatic plan view at the SQL level.                                                                                                                 |\n| `V$LOCK`, `V$SESSION`, `V$SESSION_WAIT`, `V$SESSION_EVENT`, `V$SESSION_LONGOPS`                   | TODO — no DBSQL session/lock observability surface in SQL. The information is in `system.compute.warehouse_events` and the Spark UI.                                                                                                          |\n| `DBA_HIST_*` (AWR), `DBA_ADVISOR_*`, `DBA_SQL_PLAN_BASELINES`, `DBA_OUTLINES`, `DBA_SQL_PROFILES` | Out of scope — see `12-builtin-packages.md` Out-of-scope appendix.                                                                                                                                                                            |\n| `V$RESULT_CACHE_*`                                                                                | Out of scope — Databricks query-result cache is observable via query history.                                                                                                                                                                 |\n\n## ORACLE\n\n```sql\n-- Oracle with hints → DBSQL equivalents (strip all /*+ ... */ blocks)\n\n-- Oracle\nSELECT /*+ INDEX(o orders_idx_customer) USE_NL(o c) PARALLEL(o, 8) */\n       o.id, c.name\nFROM   orders o, customers c\nWHERE  o.customer_id = c.id (+);\n\n-- DBSQL — hints removed, (+) outer join rewritten as ANSI LEFT JOIN\nSELECT o.id, c.name\nFROM   my_catalog.my_schema.orders o\nLEFT JOIN my_catalog.my_schema.customers c\n       ON o.customer_id = c.id;\n```\n\n```sql\n-- Oracle: direct-path bulk insert with APPEND hint\nINSERT /*+ APPEND */ INTO target_table\nSELECT * FROM source_table;\n\n-- DBSQL: hint removed; Delta handles bulk inserts efficiently\nINSERT INTO my_catalog.my_schema.target_table\nSELECT * FROM my_catalog.my_schema.source_table;\n```\n\n```sql\n-- Oracle: CTE with MATERIALIZE hint\nWITH high_value AS (\n    SELECT /*+ MATERIALIZE */ customer_id, SUM(amount) AS total\n    FROM   orders\n    GROUP BY customer_id\n    HAVING SUM(amount) > 10000\n)\nSELECT * FROM high_value;\n\n-- DBSQL: hint removed; the planner decides\nWITH high_value AS (\n    SELECT customer_id, SUM(amount) AS total\n    FROM   my_catalog.my_schema.orders\n    GROUP BY customer_id\n    HAVING SUM(amount) > 10000\n)\nSELECT * FROM high_value;\n```\n\n### Indexes → Z-Order / Liquid Clustering\n\nOracle indexes do not migrate one-to-one. Delta's data skipping makes secondary indexes unnecessary; replace high-selectivity lookup indexes with **Liquid Clustering** (preferred) or `OPTIMIZE ... ZORDER BY`.\n\n```sql\n-- Oracle\nCREATE INDEX idx_orders_customer ON orders(customer_id);\nCREATE INDEX idx_orders_date_status ON orders(order_date, status);\n\n-- DBSQL — preferred: Liquid Clustering at table creation\nCREATE TABLE my_catalog.my_schema.orders (\n    id          BIGINT,\n    customer_id BIGINT,\n    order_date  DATE,\n    status      STRING,\n    amount      DECIMAL(18, 2)\n)\nCLUSTER BY (customer_id, order_date);\n\n-- DBSQL — alternative: existing tables, use OPTIMIZE + ZORDER\nOPTIMIZE my_catalog.my_schema.orders ZORDER BY (customer_id, order_date);\n```\n\nLiquid Clustering is preferred over `ZORDER BY` for new tables — it adapts to changing query patterns without requiring `OPTIMIZE` reruns. See `01-schema-ddl.md → Indexes` for the full mapping table.\n\n### Function-based indexes → generated columns\n\nOracle function-based indexes (`CREATE INDEX ... ON t(UPPER(col))`) have no DBSQL equivalent. Promote the expression to a generated column and cluster on it:\n\n```sql\n-- Oracle\nCREATE INDEX idx_emp_upper_email ON employees(UPPER(email));\nSELECT * FROM employees WHERE UPPER(email) = 'ALICE@CORP.COM';\n\n-- DBSQL\nCREATE TABLE my_catalog.my_schema.employees (\n    id         BIGINT,\n    email      STRING,\n    email_upper STRING GENERATED ALWAYS AS (UPPER(email))\n)\nCLUSTER BY (email_upper);\n\nSELECT * FROM my_catalog.my_schema.employees WHERE email_upper = 'ALICE@CORP.COM';\n-- The original predicate UPPER(email) = '...' will also benefit from data skipping on email_upper.\n```\n\n### Bitmap indexes — drop\n\nOracle bitmap indexes are designed for low-cardinality filter columns. Delta's per-file min/max + bloom filter data skipping covers this case automatically — drop bitmap indexes without replacement.\n\n```sql\n-- Oracle (drop the bitmap index entirely; do not translate)\nCREATE BITMAP INDEX idx_orders_status ON orders(status);\n```\n\nIf queries filter heavily on the column, add it to the existing `CLUSTER BY` clause or run `OPTIMIZE ... ZORDER BY (status)` once.\n\n### Index-Organized Tables → CLUSTER BY on PK\n\n```sql\n-- Oracle\nCREATE TABLE lookup (\n    code   VARCHAR2(10),\n    label  VARCHAR2(100),\n    CONSTRAINT pk_lookup PRIMARY KEY (code)\n) ORGANIZATION INDEX;\n\n-- DBSQL: regular Delta table clustered by the primary key\nCREATE TABLE my_catalog.my_schema.lookup (\n    code  STRING,\n    label STRING\n)\nCLUSTER BY (code);\n```\n\nSee `01-schema-ddl.md → IOTs` for the full pattern.\n\n### Statistics\n\nOracle requires explicit statistics gathering for accurate plans. Delta gathers and maintains file-level and column-level statistics automatically — every `OPTIMIZE`, `INSERT`, `MERGE`, and `UPDATE` keeps them current.\n\n```sql\n-- Oracle (drop entirely)\nEXEC DBMS_STATS.GATHER_TABLE_STATS('MY_SCHEMA', 'ORDERS');\nEXEC DBMS_STATS.GATHER_SCHEMA_STATS('MY_SCHEMA');\nANALYZE TABLE orders COMPUTE STATISTICS;\nANALYZE INDEX idx_orders_customer COMPUTE STATISTICS;\n\n-- DBSQL: nothing to translate\n-- For column-level stats on selective columns, optionally:\nANALYZE TABLE my_catalog.my_schema.orders COMPUTE STATISTICS FOR COLUMNS customer_id, order_date;\n```\n\n`ANALYZE TABLE ... COMPUTE STATISTICS` exists in DBSQL but is rarely needed — only useful when the planner is choosing a poor join order on a stable table.\n\n### Locking and `FOR UPDATE`\n\nOracle `SELECT ... FOR UPDATE` and `LOCK TABLE` provide pessimistic row/table locking. Delta uses **optimistic concurrency** — concurrent writers are detected at commit time and the loser retries. There is no DBSQL equivalent for pessimistic locking inside a query.\n\n```sql\n-- Oracle\nSELECT * FROM orders WHERE id = 42 FOR UPDATE;\nSELECT * FROM orders WHERE status = 'pending' FOR UPDATE SKIP LOCKED;\nLOCK TABLE orders IN EXCLUSIVE MODE;\n\n-- DBSQL: no direct equivalent\n-- Pattern 1: read-modify-write inside a single MERGE statement (atomic)\nMERGE INTO my_catalog.my_schema.orders t\nUSING (SELECT 42 AS id, 'shipped' AS new_status) s\n   ON t.id = s.id\nWHEN MATCHED THEN UPDATE SET status = s.new_status;\n\n-- Pattern 2: if SKIP LOCKED was used to implement a work queue, redesign as a\n-- claim-and-update MERGE with an idempotent worker_id, or use a Databricks Workflow.\n-- TODO: manual review required — Oracle SELECT FOR UPDATE SKIP LOCKED queue pattern needs redesign.\n```\n\nIf the original code uses `FOR UPDATE` purely to block concurrent readers from a stale view (a read-then-update sequence), replace it with a single atomic `MERGE` — Delta's snapshot isolation already prevents the lost-update anomaly at the statement level.\n\n### Materialized views\n\nOracle materialized views (`CREATE MATERIALIZED VIEW ... REFRESH ON COMMIT/DEMAND/SCHEDULE`) map to DBSQL native materialized views (manual or scheduled refresh) or to Delta Live Tables / Lakeflow Declarative Pipelines for streaming/incremental refresh.\n\n```sql\n-- Oracle\nCREATE MATERIALIZED VIEW mv_daily_sales\nREFRESH FAST ON COMMIT\nAS\nSELECT order_date, SUM(amount) AS total\nFROM   orders\nGROUP BY order_date;\n\n-- DBSQL — native MV (refresh on demand or via schedule)\nCREATE MATERIALIZED VIEW my_catalog.my_schema.mv_daily_sales AS\nSELECT order_date, SUM(amount) AS total\nFROM   my_catalog.my_schema.orders\nGROUP BY order_date;\n\n-- Then schedule refresh:\nREFRESH MATERIALIZED VIEW my_catalog.my_schema.mv_daily_sales;\n```\n\n`REFRESH FAST ON COMMIT` (Oracle's incremental, transaction-synchronous refresh) has no exact DBSQL equivalent. Options:\n\n- DBSQL Materialized Views support incremental refresh for many patterns; the engine picks the strategy automatically.\n- For low-latency continuous incremental updates, redesign as a Lakeflow Declarative Pipeline (DLT). Mark with a TODO if redesign is required.\n\n### `EXPLAIN PLAN`\n\n```sql\n-- Oracle\nEXPLAIN PLAN FOR\nSELECT * FROM orders WHERE customer_id = 42;\nSELECT * FROM TABLE(DBMS_XPLAN.DISPLAY);\n\n-- DBSQL\nEXPLAIN\nSELECT * FROM my_catalog.my_schema.orders WHERE customer_id = 42;\n\n-- For a richer plan with cost and physical operators:\nEXPLAIN FORMATTED\nSELECT * FROM my_catalog.my_schema.orders WHERE customer_id = 42;\n```\n\n`EXPLAIN PLAN FOR ...; SELECT ... FROM TABLE(DBMS_XPLAN.DISPLAY)` is a two-statement Oracle idiom — collapse it into a single `EXPLAIN` statement.\n\n### Hints inside `CREATE TABLE AS SELECT`\n\n```sql\n-- Oracle\nCREATE TABLE orders_summary AS\nSELECT /*+ PARALLEL(o, 8) */ customer_id, COUNT(*) AS cnt\nFROM   orders o\nGROUP BY customer_id;\n\n-- DBSQL — hints stripped; add CLUSTER BY for downstream lookup performance\nCREATE TABLE my_catalog.my_schema.orders_summary\nCLUSTER BY (customer_id) AS\nSELECT customer_id, COUNT(*) AS cnt\nFROM   my_catalog.my_schema.orders\nGROUP BY customer_id;\n```\n\n### Hints inside DML — `INSERT` / `UPDATE` / `DELETE` / `MERGE`\n\nOracle hints attach to any DML keyword, not just `SELECT`. Strip them all; the table below shows where they appear:\n\n```sql\n-- Oracle\nINSERT /*+ APPEND PARALLEL(t, 8) */ INTO target t SELECT * FROM source;\nUPDATE /*+ INDEX(t pk_t) */ target t SET status = 'X' WHERE id = 42;\nDELETE /*+ FULL(t) */ FROM target t WHERE created < SYSDATE - 30;\nMERGE /*+ APPEND USE_NL(t s) */ INTO target t USING source s ON (t.id = s.id) ...;\n\n-- DBSQL — hints stripped from every DML site\nINSERT INTO my_catalog.my_schema.target SELECT * FROM my_catalog.my_schema.source;\nUPDATE my_catalog.my_schema.target SET status = 'X' WHERE id = 42;\nDELETE FROM my_catalog.my_schema.target WHERE created < CURRENT_TIMESTAMP - INTERVAL '30' DAY;\nMERGE INTO my_catalog.my_schema.target t USING my_catalog.my_schema.source s ON (t.id = s.id) ...;\n```\n\n`MERGE` is the most common offender — Oracle ETL code routinely puts `/*+ APPEND PARALLEL(t, 8) */` on `MERGE` to force direct-path. Drop both hints; Delta's `MERGE` is bulk-friendly and parallelizes by file.\n\n### Storage and physical-attribute clauses on DDL\n\nOracle DDL is full of physical-attribute clauses controlling block layout, transaction concurrency, tablespace, compression tier, in-memory column store, flash cache, etc. **All of these have no DBSQL counterpart and must be dropped wholesale** from `CREATE TABLE`, `CREATE INDEX`, `CREATE MATERIALIZED VIEW`, `LOB ... STORE AS`, partition definitions, and `ALTER TABLE`/`ALTER INDEX` statements.\n\n| Oracle clause family                                                                                                                                                                                                      | Action                                                                                                                                                                    |\n| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `PCTFREE n`, `PCTUSED n`, `INITRANS n`, `MAXTRANS n`                                                                                                                                                                      | Drop. Delta has no per-block transaction concurrency parameters.                                                                                                          |\n| `STORAGE (INITIAL n NEXT n MINEXTENTS n MAXEXTENTS n PCTINCREASE n FREELISTS n FREELIST GROUPS n BUFFER_POOL ... FLASH_CACHE ... CELL_FLASH_CACHE ...)`                                                                   | Drop the entire `STORAGE (...)` block.                                                                                                                                    |\n| `TABLESPACE name` / `DEFAULT TABLESPACE` / `LOGGING` / `NOLOGGING`                                                                                                                                                        | Drop. Unity Catalog has no tablespace; logging is engine-managed.                                                                                                         |\n| `COMPRESS`, `NOCOMPRESS`, `COMPRESS BASIC`, `COMPRESS FOR OLTP`, `COMPRESS FOR QUERY {LOW \\| HIGH}`, `COMPRESS FOR ARCHIVE {LOW \\| HIGH}`, `ROW STORE COMPRESS`, `COLUMN STORE COMPRESS FOR ...`                          | Drop. Delta files use Snappy/ZSTD compression by default; the `delta.parquet.compression.codec` table property tunes it if needed.                                        |\n| `INMEMORY [PRIORITY {LOW \\| MEDIUM \\| HIGH \\| CRITICAL}] [MEMCOMPRESS FOR ...] [DISTRIBUTE ...] [DUPLICATE ...]`, `NO INMEMORY`                                                                                           | Drop. Photon + Delta caching are always available; no per-table opt-in.                                                                                                   |\n| `MEMOPTIMIZE FOR READ`, `MEMOPTIMIZE FOR WRITE`                                                                                                                                                                           | Drop. No DBSQL counterpart.                                                                                                                                               |\n| `CACHE` / `NOCACHE` (table-level), `CACHE READS` (LOB), `CELL_OFFLOAD_PROCESSING TRUE/FALSE`                                                                                                                              | Drop.                                                                                                                                                                     |\n| `RESULT_CACHE (MODE {DEFAULT \\| FORCE})` clause on `CREATE TABLE` / `CREATE FUNCTION`                                                                                                                                     | Drop. SQL Warehouse query-result cache is automatic.                                                                                                                      |\n| `PARALLEL n` / `PARALLEL` / `NOPARALLEL` (DDL clause, not hint)                                                                                                                                                           | Drop.                                                                                                                                                                     |\n| `MONITORING` / `NOMONITORING`                                                                                                                                                                                             | Drop. Deprecated in Oracle and unused.                                                                                                                                    |\n| `ENABLE ROW MOVEMENT` / `DISABLE ROW MOVEMENT`                                                                                                                                                                            | Drop. Delta updates always rewrite files; no row-movement concept.                                                                                                        |\n| `FLASHBACK ARCHIVE archive_name` / `NO FLASHBACK ARCHIVE`                                                                                                                                                                 | Drop the clause. Long-term retention via Delta time travel + `ALTER TABLE ... SET TBLPROPERTIES ('delta.logRetentionDuration' = ...)`. See `02-select-dml.md` \xa7Flashback. |\n| `LOB (col) STORE AS [SECUREFILE \\| BASICFILE] (...)` and all sub-clauses (`CHUNK n`, `RETENTION ...`, `PCTVERSION n`, `FREEPOOLS n`, `DEDUPLICATE`, `KEEP_DUPLICATES`, `COMPRESS HIGH/MEDIUM/LOW`, `ENCRYPT USING '...'`) | Drop entirely. See `03-data-types.md` \xa7LOB storage clauses.                                                                                                               |\n| `OVERFLOW [TABLESPACE ...] [INCLUDING col]` (IOT overflow)                                                                                                                                                                | Drop. IOTs migrate to a `CLUSTER BY (pk)` Delta table — see \"Index-Organized Tables\" above.                                                                               |\n| `PCTTHRESHOLD n` (IOT)                                                                                                                                                                                                    | Drop.                                                                                                                                                                     |\n| `KEY_COMPRESSION n`, `PREFIX COMPRESSION` (on indexes)                                                                                                                                                                    | Drop.                                                                                                                                                                     |\n| Partition-level storage: every per-partition `TABLESPACE`, `STORAGE`, `PCTFREE`, `COMPRESS` clause inside a `PARTITION p VALUES LESS THAN (...)` definition                                                               | Drop. After replacing the partitioning scheme with `PARTITIONED BY` or `CLUSTER BY` (see `01-schema-ddl.md`), no per-partition physical attributes survive.               |\n\n```sql\n-- Oracle: full physical-attribute soup\nCREATE TABLE orders (\n    id          NUMBER,\n    customer_id NUMBER,\n    order_date  DATE,\n    body        CLOB\n)\nPCTFREE 10 PCTUSED 40 INITRANS 2 MAXTRANS 255\nTABLESPACE users\nSTORAGE (INITIAL 64K NEXT 1M MINEXTENTS 1 MAXEXTENTS UNLIMITED)\nLOGGING\nCOMPRESS FOR OLTP\nINMEMORY PRIORITY HIGH\nLOB (body) STORE AS SECUREFILE (\n    TABLESPACE lob_data\n    ENABLE STORAGE IN ROW\n    CHUNK 8192\n    NOCACHE\n    COMPRESS HIGH\n    DEDUPLICATE\n)\nPARALLEL 8\nENABLE ROW MOVEMENT\nPARTITION BY RANGE (order_date) (\n    PARTITION p_2024_01 VALUES LESS THAN (DATE '2024-02-01') TABLESPACE ts_2024 STORAGE (INITIAL 1M),\n    PARTITION p_2024_02 VALUES LESS THAN (DATE '2024-03-01') TABLESPACE ts_2024\n);\n\n-- DBSQL: every physical-attribute / partition-storage clause is dropped\nCREATE TABLE my_catalog.my_schema.orders (\n    id          BIGINT,\n    customer_id BIGINT,\n    order_date  DATE,\n    body        STRING\n)\nPARTITIONED BY (order_date);\n```\n\n### Index DDL — `CREATE INDEX` options and `ALTER INDEX` operations\n\nMost Oracle index DDL has no DBSQL counterpart. The \"intent\" of a B-tree / function-based index transfers to `CLUSTER BY` or a generated column, but every Oracle-specific option is dropped.\n\n| Oracle DDL                                                                                                    | Action                                                                                                                                                                                                           |\n| ------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `CREATE [UNIQUE] INDEX idx ON t(col[, ...])`                                                                  | Drop. Replace with `CLUSTER BY (col[, ...])` on the table or `OPTIMIZE t ZORDER BY (col[, ...])` for existing tables. `UNIQUE` becomes `CONSTRAINT u_col UNIQUE (col)` on the table — not enforced concurrently. |\n| `CREATE BITMAP INDEX idx ON t(col)`                                                                           | Drop entirely. Delta data skipping covers low-cardinality filters.                                                                                                                                               |\n| `CREATE BITMAP INDEX idx ON t(t1.col, t2.col) FROM t1, t2 WHERE ...` (bitmap join index)                      | Drop. If the join was performance-critical, materialize as a Delta table via `CTAS`.                                                                                                                             |\n| `CREATE INDEX idx ON t(UPPER(col))` (function-based)                                                          | Generated column: `email_upper STRING GENERATED ALWAYS AS (UPPER(email))` + `CLUSTER BY (email_upper)`. See \"Function-based indexes\" above.                                                                      |\n| `CREATE INDEX idx ON t(col) REVERSE`                                                                          | Drop. Delta has no hot-block contention.                                                                                                                                                                         |\n| `CREATE INDEX idx ON t(col) INVISIBLE` / `ALTER INDEX idx INVISIBLE` / `VISIBLE`                              | Drop the keyword and the entire ALTER.                                                                                                                                                                           |\n| `CREATE INDEX idx ON t(col) COMPRESS [n]` / `KEY_COMPRESSION`                                                 | Drop the option.                                                                                                                                                                                                 |\n| `CREATE INDEX idx ON t(col) NOLOGGING` / `LOGGING`                                                            | Drop.                                                                                                                                                                                                            |\n| `CREATE INDEX idx ON t(col) PARALLEL n` / `NOPARALLEL`                                                        | Drop.                                                                                                                                                                                                            |\n| `CREATE INDEX idx ON t(col) ONLINE`                                                                           | Drop. DBSQL DDL is online by default (no read/write blocking on schema mutation).                                                                                                                                |\n| `CREATE INDEX idx ON t(col) LOCAL [PARTITION ...]` / `GLOBAL`                                                 | Drop. Per-partition / global index distinctions don't apply to Delta.                                                                                                                                            |\n| `CREATE INDEX idx ON t(col) TABLESPACE ts` / `STORAGE (...)` / `PCTFREE n` / `INITRANS n`                     | Drop the storage block.                                                                                                                                                                                          |\n| `CREATE INDEX idx ON t(col) INDEXTYPE IS CTXSYS.CONTEXT` (Oracle Text), `MDSYS.SPATIAL_INDEX`, `LUCENE`, etc. | TODO — domain indexes have no DBSQL counterpart. See `12-builtin-packages.md` Out-of-scope (`CTX_*`, `SDO_*`, `MDSYS.*`).                                                                                        |\n| `ALTER INDEX idx REBUILD [ONLINE] [PARALLEL n] [TABLESPACE ts] [PARTITION p]`                                 | Drop entirely. Run `OPTIMIZE catalog.schema.t` (or `OPTIMIZE ... FULL` to force) when file layout drifts.                                                                                                        |\n| `ALTER INDEX idx COALESCE` / `SHRINK SPACE [COMPACT \\| CASCADE]` / `MONITORING USAGE` / `NOMONITORING USAGE`  | Drop.                                                                                                                                                                                                            |\n| `ALTER INDEX idx UNUSABLE` / `ALTER INDEX idx LOGGING`/`NOLOGGING`                                            | Drop. There is no per-index disable in Delta.                                                                                                                                                                    |\n| `ALTER INDEX idx RENAME TO new_name`                                                                          | Drop. Without indexes, there is nothing to rename. If a constraint shared the index name, see `01-schema-ddl.md` \xa7`ALTER TABLE ... RENAME CONSTRAINT`.                                                           |\n| `DROP INDEX idx [FORCE]`                                                                                      | Drop the statement entirely.                                                                                                                                                                                     |\n| `ANALYZE INDEX idx VALIDATE STRUCTURE` / `ESTIMATE STATISTICS`                                                | Drop. Delta has no per-index validity / stat refresh.                                                                                                                                                            |\n\n### Materialized view extras — refresh modes, MV logs, query rewrite, build modes\n\nThe basic `CREATE MATERIALIZED VIEW` story is in the previous section. The peripheral options below all need translation or removal:\n\n| Oracle clause                                                                                                       | DBSQL                                                                                                                                                                       |\n| ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `BUILD IMMEDIATE` / `BUILD DEFERRED`                                                                                | Drop. DBSQL MVs always materialize on first refresh; explicit `REFRESH MATERIALIZED VIEW ...` triggers it.                                                                  |\n| `REFRESH FAST [ON COMMIT \\| ON DEMAND \\| ON STATEMENT]`                                                             | Drop the `FAST` modifier. DBSQL MVs are always incremental when the engine can prove safety; otherwise they fall back to full refresh transparently.                        |\n| `REFRESH COMPLETE [ON DEMAND \\| ON COMMIT]`                                                                         | `REFRESH MATERIALIZED VIEW catalog.schema.mv` (manual). For scheduled refresh, attach the MV to a Databricks SQL schedule or a Workflow task.                               |\n| `REFRESH FORCE`                                                                                                     | Drop. DBSQL chooses incremental vs full automatically.                                                                                                                      |\n| `REFRESH ON COMMIT`                                                                                                 | No exact equivalent. For low-latency refresh, use a Lakeflow Declarative Pipeline (DLT) or change the underlying writer to invoke `REFRESH MATERIALIZED VIEW` after commit. |\n| `REFRESH ON STATEMENT` (12.2+)                                                                                      | No exact equivalent. Same DLT redesign as above.                                                                                                                            |\n| `START WITH ... NEXT ...` (refresh schedule embedded in DDL)                                                        | Move the schedule out of the DDL. Use a Databricks SQL **schedule** on the MV or a Workflow task that runs `REFRESH MATERIALIZED VIEW`.                                     |\n| `WITH PRIMARY KEY` / `WITH ROWID`                                                                                   | Drop. Delta MVs do not need to declare a refresh-primary-key.                                                                                                               |\n| `ENABLE QUERY REWRITE` / `DISABLE QUERY REWRITE`                                                                    | Drop. DBSQL has no transparent MV rewrite — callers must `SELECT FROM mv` explicitly.                                                                                       |\n| `USING INDEX (...)` / `USING NO INDEX`                                                                              | Drop.                                                                                                                                                                       |\n| `MATERIALIZED VIEW LOG ON t WITH (PRIMARY KEY \\| ROWID \\| SEQUENCE \\| col_list) [INCLUDING NEW VALUES] [PURGE ...]` | Drop entirely. Delta CDF (`ALTER TABLE t SET TBLPROPERTIES (delta.enableChangeDataFeed = true)`) provides the equivalent incremental-source feed for downstream MVs.        |\n| `ALTER MATERIALIZED VIEW LOG ...`, `DROP MATERIALIZED VIEW LOG ON t`                                                | Drop.                                                                                                                                                                       |\n| `DBMS_MVIEW.REFRESH` / `DBMS_MVIEW.REFRESH_DEPENDENT` / `DBMS_REFRESH.*`                                            | `REFRESH MATERIALIZED VIEW catalog.schema.mv;` per MV. See `12-builtin-packages.md` \xa7`DBMS_MVIEW`/`DBMS_REFRESH`.                                                           |\n| `ALTER MATERIALIZED VIEW mv COMPILE`                                                                                | Drop. DBSQL has no compile step.                                                                                                                                            |\n| `ALTER MATERIALIZED VIEW mv {ENABLE \\| DISABLE} QUERY REWRITE`                                                      | Drop.                                                                                                                                                                       |\n| `ALTER MATERIALIZED VIEW mv CONSIDER FRESH`                                                                         | Drop.                                                                                                                                                                       |\n\n### Statistics — column stats, histograms, extended stats\n\nDelta keeps the following statistics automatically per file: row count, min/max per column, null count, NDV (approximate). It does **not** keep histograms or column-correlation stats. Patterns:\n\n```sql\n-- Oracle: gather histograms on selective columns\nEXEC DBMS_STATS.GATHER_TABLE_STATS(\n    ownname          => 'MY_SCHEMA',\n    tabname          => 'ORDERS',\n    method_opt       => 'FOR COLUMNS SIZE 254 status, country',\n    estimate_percent => DBMS_STATS.AUTO_SAMPLE_SIZE,\n    granularity      => 'AUTO',\n    cascade          => TRUE\n);\n\n-- DBSQL: drop the call. For a one-off boost on a stable lookup table:\nANALYZE TABLE my_catalog.my_schema.orders COMPUTE STATISTICS FOR COLUMNS status, country;\n```\n\n| Oracle stat operation                                                                                     | DBSQL                                                                                                                                                                        |\n| --------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `DBMS_STATS.GATHER_TABLE_STATS(... estimate_percent ..., method_opt => 'FOR ALL COLUMNS SIZE n')`         | Drop entirely (preferred), or `ANALYZE TABLE ... COMPUTE STATISTICS [FOR COLUMNS ...]` for a stable hot table. Drop `estimate_percent`/`method_opt`/`granularity`/`cascade`. |\n| `DBMS_STATS.GATHER_TABLE_STATS` with `method_opt => 'FOR COLUMNS SIZE AUTO/REPEAT/SKEWONLY'` (histograms) | Drop. DBSQL stats are min/max/NDV only — no histograms. If skew was driving the plan choice, audit the slow query and consider `CLUSTER BY` on the skew column.              |\n| `DBMS_STATS.CREATE_EXTENDED_STATS(... '(col_a, col_b)')` or `'(UPPER(col))'`                              | Drop. For function-based stats, materialize as a generated column and `ANALYZE ... FOR COLUMNS gen_col`. For column-group correlation stats, no equivalent.                  |\n| `DBMS_STATS.SET_TABLE_PREFS('PUBLISH', 'FALSE')` / pending stats workflow                                 | Drop.                                                                                                                                                                        |\n| `DBMS_STATS.LOCK_TABLE_STATS` / `UNLOCK_TABLE_STATS` / `RESTORE_TABLE_STATS`                              | Drop.                                                                                                                                                                        |\n| `DBMS_STATS.IMPORT_TABLE_STATS` / `EXPORT_TABLE_STATS` (cross-environment stats portability)              | Drop. Run `ANALYZE` on the target environment after migration.                                                                                                               |\n| `DBMS_STATS.SET_GLOBAL_PREFS` / `SET_SCHEMA_PREFS` / `SET_TABLE_PREFS`                                    | Drop entirely.                                                                                                                                                               |\n| `DBMS_STATS.GATHER_FIXED_OBJECTS_STATS` / `GATHER_DICTIONARY_STATS` / `GATHER_SYSTEM_STATS`               | Drop. Engine-level.                                                                                                                                                          |\n| `ANALYZE TABLE t VALIDATE STRUCTURE [CASCADE]`                                                            | Drop. No equivalent — Delta block validity is engine-managed.                                                                                                                |\n| `ANALYZE TABLE t {COMPUTE \\| ESTIMATE} STATISTICS SAMPLE n PERCENT`                                       | `ANALYZE TABLE t COMPUTE STATISTICS` (DBSQL does not expose a sample-percent knob).                                                                                          |\n| `ANALYZE TABLE t LIST CHAINED ROWS INTO chained_rows`                                                     | Drop. No row-chaining concept in Delta.                                                                                                                                      |\n\n### SQL Plan Management, profiles, outlines, tuning advisors — out of scope\n\nOracle's SQL Plan Management (SPM), SQL Profiles, SQL Patches, Outlines, and the Tuning / Access Advisor families have no DBSQL counterpart. Drop everything — the migration agent should emit one TODO per call.\n\n| Oracle construct                                                                                                                                       | Action                                                                                                                                                                                  |\n| ------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `DBMS_SPM.LOAD_PLANS_FROM_CURSOR_CACHE` / `LOAD_PLANS_FROM_SQLSET` / `EVOLVE_SQL_PLAN_BASELINE` / `ALTER_SQL_PLAN_BASELINE` / `DROP_SQL_PLAN_BASELINE` | TODO — no DBSQL plan baselines. Capture the _intent_ (a known-good plan shape) by encoding it as `CLUSTER BY` / pre-aggregated MVs / explicit `BROADCAST` table hints in Spark configs. |\n| `DBA_SQL_PLAN_BASELINES`, `ALTER SESSION SET OPTIMIZER_USE_SQL_PLAN_BASELINES = TRUE`                                                                  | Drop.                                                                                                                                                                                   |\n| SQL Profiles: `DBMS_SQLTUNE.IMPORT_SQL_PROFILE` / `ACCEPT_SQL_PROFILE` / `ALTER_SQL_PROFILE` / `DROP_SQL_PROFILE`                                      | TODO. Same intent re-encoding as above.                                                                                                                                                 |\n| SQL Patches: `DBMS_SQLDIAG.CREATE_SQL_PATCH` / `ALTER_SQL_PATCH` / `DROP_SQL_PATCH`                                                                    | TODO.                                                                                                                                                                                   |\n| Stored Outlines: `CREATE OUTLINE`, `DBA_OUTLINES`, `ALTER SESSION SET USE_STORED_OUTLINES = TRUE`                                                      | Drop. Deprecated in Oracle 11.2+.                                                                                                                                                       |\n| `DBMS_SQLTUNE.CREATE_TUNING_TASK` / `EXECUTE_TUNING_TASK` / `REPORT_TUNING_TASK` / `ACCEPT_SQL_PROFILE`                                                | TODO — use the Databricks Query Profile (UI) to find the slow stage; encode improvements in DDL (`CLUSTER BY`, MV).                                                                     |\n| SQL Tuning Sets: `DBMS_SQLTUNE.CREATE_SQLSET` / `LOAD_SQLSET` / `SELECT_SQLSET`                                                                        | Drop. Use the `system.query.history` table to bulk-inspect query patterns.                                                                                                              |\n| SQL Access Advisor: `DBMS_ADVISOR.CREATE_TASK('SQL Access Advisor', ...)` / `EXECUTE_TASK` / `GET_TASK_REPORT`                                         | TODO. The advisor's recommendations (indexes / MVs / partitioning) translate to `CLUSTER BY` / `OPTIMIZE` / Delta MV decisions you make manually.                                       |\n| Automatic SQL Tuning, Automatic Plan Management, Real-Time SQL Monitoring (`V$SQL_MONITOR`)                                                            | TODO. Use the Databricks Query Profile UI.                                                                                                                                              |\n\n### Performance views and AWR — out of scope\n\nOracle's `V$*`, `GV$*`, `X$*`, `DBA_HIST_*`, and `WRH$_*` performance views have no DBSQL parallel at the SQL level. Code that queried these views must move to:\n\n- **`system.query.history`** (Databricks system table) — historic query metadata, durations, status, user, warehouse.\n- **`system.compute.warehouse_events`** — warehouse start/stop/scale events.\n- **`system.access.audit`** — query / DDL audit log.\n- **Databricks Query Profile UI** — per-query plan + per-stage timing for live debugging.\n- **Spark UI** (per cluster / per warehouse) — task / stage / executor breakdown.\n\n| Oracle view family                                                                                               | Replace with                                                                                                  |\n| ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |\n| `V$SQL`, `V$SQLAREA`, `V$SQL_PLAN`, `V$SQL_PLAN_STATISTICS_ALL`, `V$SQL_BIND_CAPTURE`                            | `system.query.history` + Databricks Query Profile UI. No programmatic plan view.                              |\n| `V$SESSION`, `V$SESSION_LONGOPS`, `V$SESSION_WAIT`, `V$SESSION_EVENT`, `V$ACTIVE_SESSION_HISTORY`                | `system.query.history` joined with `system.compute.warehouse_events`. No live-session view.                   |\n| `V$LOCK`, `DBA_BLOCKERS`, `DBA_WAITERS`                                                                          | TODO — no DBSQL lock observability surface. Delta optimistic concurrency conflicts surface as failed commits. |\n| `V$INSTANCE`, `V$DATABASE`, `V$VERSION`                                                                          | `current_version()`, `current_metastore()`, workspace metadata APIs.                                          |\n| `V$RESULT_CACHE_*`, `V$BUFFER_POOL_STATISTICS`, `V$BH`                                                           | Out of scope.                                                                                                 |\n| `DBA_HIST_*` (AWR) — `DBA_HIST_SQLSTAT`, `DBA_HIST_SQL_PLAN`, `DBA_HIST_ACTIVE_SESS_HISTORY`, `DBA_HIST_SYSSTAT` | `system.query.history` (12-month retention) + Databricks Query Profile.                                       |\n| `DBA_ADVISOR_*` (Tuning Advisor / ADDM / Access Advisor history)                                                 | Out of scope.                                                                                                 |\n| `DBA_AUTOTASK_*` / `DBA_SCHEDULER_RUNNING_JOBS`                                                                  | `system.workflow.*` (Workflow runs / tasks).                                                                  |\n| `V$TEMPSTAT`, `V$TEMP_EXTENT_POOL`, `DBA_TEMP_FILES`                                                             | Out of scope. Spark spill is observable in Spark UI per-stage.                                                |\n\n### `EXPLAIN` — full DBSQL surface\n\n`EXPLAIN` in DBSQL is richer than the simple two-statement Oracle idiom; the formats are:\n\n| DBSQL form                  | Output                                                                                                                                        |\n| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |\n| `EXPLAIN <query>`           | Compact physical plan (Spark stages and operators). Use as the default replacement for `EXPLAIN PLAN FOR ... DBMS_XPLAN.DISPLAY`.             |\n| `EXPLAIN FORMATTED <query>` | Plan + per-operator details (output schema, predicates, partition counts, statistics). The closest analog to `DBMS_XPLAN.DISPLAY('TYPICAL')`. |\n| `EXPLAIN EXTENDED <query>`  | Plan + parsed logical / analyzed logical / optimized logical / physical plans (all four). The closest analog to `DBMS_XPLAN.DISPLAY('ALL')`.  |\n| `EXPLAIN COST <query>`      | Plan + cost estimates per operator.                                                                                                           |\n| `EXPLAIN CODEGEN <query>`   | Generated whole-stage codegen Java (debug-only).                                                                                              |\n\nCode that parsed `DBMS_XPLAN.DISPLAY` output will need a different parser — DBSQL plans are not plan-table-row tuples but free-form text. Consider switching to the **Query Profile JSON API** (`/api/2.0/sql/queries/{id}/profile`) for programmatic access.\n\n### Resource Manager / consumer groups — out of scope\n\nOracle's Database Resource Manager (`DBMS_RESOURCE_MANAGER`) maps loosely to Databricks SQL Warehouses (size + concurrency) and Spark scheduler pools. There is no SQL-level migration path:\n\n| Oracle                                                                                    | DBSQL                                                                                                                                                                   |\n| ----------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `DBMS_RESOURCE_MANAGER.CREATE_PLAN` / `CREATE_CONSUMER_GROUP` / `CREATE_PLAN_DIRECTIVE`   | TODO — provision a separate **SQL Warehouse** per consumer group at the workspace-admin level. Mapping principals to warehouses lives in workspace IAM, not in SQL DDL. |\n| `DBMS_RESOURCE_MANAGER.SWITCH_CONSUMER_GROUP_FOR_USER` / `SWITCH_CONSUMER_GROUP_FOR_SESS` | TODO — no in-session reassignment.                                                                                                                                      |\n| `ALTER USER ... PROFILE ...` / `CREATE PROFILE p LIMIT ...`                               | Out of scope.                                                                                                                                                           |\n| Per-statement `RESOURCE_MANAGER_CPU_ALLOCATION` hint                                      | Drop.                                                                                                                                                                   |\n"}}]);
//# sourceMappingURL=https://sourcemaps.dev.databricks.com/monolith/static/js/70559.5c56e9659f.chunk.js.map