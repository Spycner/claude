"use strict";(self.webpackChunkdatabricks_monolith=self.webpackChunkdatabricks_monolith||[]).push([[72770],{72770:e=>{e.exports="# Functions, Procedures & PL/SQL\n\nOracle PL/SQL → Databricks SQL scripting. Includes packages, cursors, exceptions, and variable handling.\n\n> For DBSQL stored procedure syntax, load `skills/writing-sql/stored-procedures.md`.\n> For general scripting constructs (variables, IF/ELSE, LOOP, WHILE, error handlers), load `skills/writing-sql/sql-scripting.md`.\n\n## ORACLE\n\n### Stored Procedures\n\n```sql\n-- Oracle (PL/SQL)\nCREATE OR REPLACE PROCEDURE my_proc(\n    param1 IN  NUMBER,\n    param2 IN  VARCHAR2 DEFAULT 'default',\n    output OUT NUMBER\n) AS BEGIN\n    output := param1 * 2;\nEND my_proc;\n/\n\n-- DBSQL (Unity Catalog three-part name: catalog.schema.procedure)\nCREATE OR REPLACE PROCEDURE my_catalog.my_schema.my_proc(\n    IN  param1 INT,\n    IN  param2 STRING DEFAULT 'default',\n    OUT output_param INT\n)\nLANGUAGE SQL\nSQL SECURITY INVOKER\nAS BEGIN\n    SET output_param = param1 * 2;\nEND;\n```\n\n**Terminator removal**: Strip the trailing `/` and `show errors` — they are SQL\\*Plus directives, not PL/SQL syntax. See `00-quirks.md`.\n\n**Security context (`SQL SECURITY INVOKER`)**: Always include `SQL SECURITY INVOKER` — it is a required clause and the only accepted value in Databricks SQL (`SQL SECURITY DEFINER` is not valid). Omitting it causes `MISSING_CLAUSES_FOR_OPERATION: SQL SECURITY` (SQLSTATE 42601). The procedure runs with the caller's permissions. Oracle procedures are `DEFINER`-rights by default (`AUTHID DEFINER`) — if the original procedure accessed objects the caller cannot see, flag this change in a migration comment. For procedures that were explicitly `AUTHID CURRENT_USER` in Oracle, the DBSQL `INVOKER` behavior matches directly.\n\n**Calling a procedure with OUT parameter:**\n\n```sql\n-- Oracle\nDECLARE v_result NUMBER;\nBEGIN\n    my_proc(10, 'default', v_result);\n    DBMS_OUTPUT.PUT_LINE(v_result);\nEND;\n/\n\n-- DBSQL\nDECLARE result INT;\nCALL my_catalog.my_schema.my_proc(10, 'default', result);\n```\n\n**BEGIN ATOMIC for SQL warehouse context**: Procedures run on a SQL warehouse require `BEGIN ATOMIC` in the procedure body. A bare `BEGIN` without `ATOMIC` is for notebook scripting context only. The default execution context is notebook/classic compute (see SKILL.md Plan step), so use `BEGIN ATOMIC` only when the user has confirmed a SQL warehouse target.\n\n```sql\n-- DML-only procedure (no RETURNS TABLE)\nCREATE OR REPLACE PROCEDURE my_catalog.my_schema.proc_name(IN param1 INT)\nLANGUAGE SQL\nSQL SECURITY INVOKER\nBEGIN ATOMIC\n    INSERT INTO t SELECT param1;\n    UPDATE t SET status = 'done' WHERE id = param1;\nEND;\n\n-- Procedure that returns a result set (RETURNS TABLE)\nCREATE OR REPLACE PROCEDURE my_catalog.my_schema.proc_name(IN param1 INT)\nRETURNS TABLE (col1 INT, col2 STRING)\nLANGUAGE SQL\nSQL SECURITY INVOKER\nBEGIN ATOMIC\n    SELECT col1, col2 FROM t WHERE id = param1;\nEND;\n```\n\n**`RETURNS TABLE` requires `BEGIN ATOMIC` — never use with bare `BEGIN`**: `RETURNS TABLE` is only valid in SQL warehouse context (`BEGIN ATOMIC`). Do not add `RETURNS TABLE` to any procedure using bare `BEGIN` (notebook/classic compute context) — it is a syntax error in that context. Since the default execution context is notebook/classic compute, the standard notebook procedure does not use `RETURNS TABLE`.\n\n#### Parameter modes (IN / OUT / IN OUT / NOCOPY)\n\nOracle has three parameter modes; DBSQL has the same three with the same names. The behavioral subtleties are largely identical, but a few Oracle-only features need to be handled:\n\n| Oracle parameter clause                                           | DBSQL                             | Notes                                                                                                                                                                                                                                                                                |\n| ----------------------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `p IN NUMBER`                                                     | `IN p INT`                        | Same — input-only.                                                                                                                                                                                                                                                                   |\n| `p OUT NUMBER`                                                    | `OUT p INT`                       | Same — output-only. The caller must supply a `DECLARE`d variable that receives the value.                                                                                                                                                                                            |\n| `p IN OUT NUMBER`                                                 | `INOUT p INT`                     | Same semantics; **note the keyword spelling** — DBSQL uses one word `INOUT`, Oracle uses two words `IN OUT`.                                                                                                                                                                         |\n| `p IN OUT NOCOPY <type>` / `p OUT NOCOPY <type>`                  | `INOUT p <type>` / `OUT p <type>` | Drop `NOCOPY`. It is an Oracle-only performance hint requesting pass-by-reference for large composite parameters; DBSQL has no equivalent and no semantic change occurs from dropping it.                                                                                            |\n| `p NUMBER(5)` / `p VARCHAR2(20)` (sized parameter)                | `IN p INT` / `IN p STRING`        | Drop the precision / length on parameter declarations. DBSQL parameter types are unsized — Oracle would have raised on out-of-range values at the call boundary; DBSQL does not. If the size constraint mattered, validate inside the body with `IF length(p) > 20 THEN SIGNAL ...`. |\n| `p IN NUMBER DEFAULT 0`                                           | `IN p INT DEFAULT 0`              | Same. Only `IN` parameters can have defaults in both dialects — `OUT` / `INOUT` defaults are rejected.                                                                                                                                                                               |\n| Procedure called as a statement: `my_proc(1, 2);` (Oracle PL/SQL) | `CALL my_proc(1, 2);`             | DBSQL requires the `CALL` keyword for procedure invocation — Oracle's bare `name(args);` form does not parse.                                                                                                                                                                        |\n\n**Mixed positional + named arguments at call sites.** Oracle accepts mixed notation (`my_proc(1, p_amount => 100)`) — positional first, then switch to named. DBSQL is **positional only**; convert every named-argument call site to positional during migration:\n\n```sql\n-- Oracle\nmy_proc(p_id => 42, p_amount => 100, p_currency => 'USD');\nmy_proc(42, p_amount => 100);                              -- mixed (positional id, then named)\n\n-- DBSQL — positional, in declaration order\nCALL my_catalog.my_schema.my_proc(42, 100, 'USD');\nCALL my_catalog.my_schema.my_proc(42, 100);\n```\n\nIf the original code skipped a middle parameter via named notation (`my_proc(p_id => 1, p_currency => 'USD')` — skipping `p_amount`), the DBSQL call must explicitly pass the default value or rely on a trailing default — there is no skip-middle syntax.\n\n**Output-parameter capture.** A DBSQL `OUT` / `INOUT` parameter is bound to a `DECLARE`d caller variable that receives the value at procedure return. The caller must `DECLARE` the variable **before** the `CALL`:\n\n```sql\n-- DBSQL\nDECLARE result INT;\nCALL my_catalog.my_schema.my_proc(42, result);\nSELECT result;\n```\n\nInside another procedure, the `DECLARE` must appear inside the calling procedure's `BEGIN ... END` block — see `### Variables` below.\n\n### REF CURSOR / SYS_REFCURSOR result sets\n\nOracle procedures return result sets by returning a `SYS_REFCURSOR` OUT parameter. DBSQL procedures surface result sets via the last `SELECT` statement in the body (or `RETURNS TABLE` in SQL warehouse context).\n\n```sql\n-- Oracle\nCREATE OR REPLACE PROCEDURE get_orders(p_customer IN NUMBER, p_cursor OUT SYS_REFCURSOR) AS\nBEGIN\n    OPEN p_cursor FOR\n        SELECT order_id, amount FROM orders WHERE customer_id = p_customer;\nEND;\n/\n\n-- DBSQL (notebook/classic compute) — last SELECT is returned to the caller\nCREATE OR REPLACE PROCEDURE my_catalog.my_schema.get_orders(IN p_customer INT)\nLANGUAGE SQL\nSQL SECURITY INVOKER\nAS BEGIN\n    SELECT order_id, amount FROM my_catalog.my_schema.orders WHERE customer_id = p_customer;\nEND;\n```\n\n**Multi-result-set mapping:**\n\nDatabricks SQL procedures surface only the **last** SELECT statement's result to the caller. Oracle procedures with multiple `OPEN ref_cursor FOR` statements need to be consolidated before writing the cell. Never leave multiple bare SELECT statements in a procedure body expecting all results to be returned.\n\n- **Option A (preferred when schemas are compatible)**: Combine with `UNION ALL` and a discriminator column. The discriminator column is not in the original output schema — flag this in a comment.\n  ```sql\n  -- NOTE: Oracle proc returned 2 REF CURSORs. Combined with UNION ALL + result_set discriminator.\n  -- The discriminator column was added and was not in the original schema.\n  SELECT 'set1' AS result_set, col1, col2 FROM t1\n  UNION ALL\n  SELECT 'set2' AS result_set, col1, col2 FROM t2\n  ```\n- **Option B**: Restructure to a single final SELECT, staging intermediate results in temp views or CTEs. Only the final SELECT returns to the caller.\n- **Option C (last resort — incompatible schemas only)**: When result sets cannot be combined due to incompatible column counts or types, select the authoritative result set and document what was discarded:\n  ```sql\n  -- NOTE: Oracle proc returned N REF CURSORs. Databricks SQL procedures return only the last\n  -- SELECT result. Returning <which set and why>. Discarded sets: <list>.\n  -- If the application depends on discarded result sets, a refactor is required.\n  ```\n  Only use Option C when A and B are genuinely impossible — it will fail semantic equivalence when discarded sets carry meaningful data.\n\n### Packages — see `oracle/11-user-packages.md`\n\nOracle `CREATE PACKAGE` / `CREATE PACKAGE BODY` migration (spec/body translation, package-level state, overloading, package types, initialization blocks, private members, pragmas) is covered in **`oracle/11-user-packages.md`**. The procedure and function bodies _inside_ a package use the syntax described in this file — the 11 entry covers only the package-as-container concerns.\n\n### User-Defined Functions (UDFs)\n\n```sql\n-- Oracle scalar function\nCREATE OR REPLACE FUNCTION get_full_name(first VARCHAR2, last VARCHAR2)\nRETURN VARCHAR2\nAS BEGIN\n    RETURN first || ' ' || last;\nEND;\n/\n\n-- DBSQL (Unity Catalog three-part name: catalog.schema.function)\nCREATE OR REPLACE FUNCTION my_catalog.my_schema.get_full_name(first STRING, last STRING)\nRETURNS STRING\nRETURN CONCAT(first, ' ', last);\n```\n\n**Terminator removal**: Strip the trailing `/` and `show errors` — they are SQL\\*Plus directives, not PL/SQL syntax. See `00-quirks.md`.\n\n**Security context (`SQL SECURITY INVOKER`)**: For functions, `SQL SECURITY` defaults to `DEFINER` in DBSQL — the only valid value (unlike procedures, which require `INVOKER`). You can omit the clause; if specified, it must be `SQL SECURITY DEFINER`. The function runs with the owner's permissions on referenced objects. Oracle functions are also `DEFINER`-rights by default (`AUTHID DEFINER`) — the mapping is direct. For Oracle functions explicitly declared `AUTHID CURRENT_USER`, there is no DBSQL equivalent — flag with a TODO and convert to a procedure with `SQL SECURITY INVOKER` if invoker-rights semantics are required.\n\n**Calling a function:**\n\n```sql\n-- Oracle (SELECT context, assignment, or any expression position)\nSELECT get_full_name('Ada', 'Lovelace') FROM DUAL;\nv_name := get_full_name('Ada', 'Lovelace');\n\n-- DBSQL (no DUAL; functions are called in any expression position)\nSELECT my_catalog.my_schema.get_full_name('Ada', 'Lovelace');\nSET v_name = my_catalog.my_schema.get_full_name('Ada', 'Lovelace');\n```\n\n### Function bodies — expression-only vs multi-statement\n\nDBSQL SQL UDFs are **expression-only**: the body is a single `RETURN <expression>`. There is no `BEGIN ... END` body, no local variable declarations, no control flow inside the function. Oracle PL/SQL functions allow full procedural bodies with `DECLARE`, multiple statements, IF/LOOP, multiple `RETURN` paths, and exception handlers.\n\n**Translation strategies for Oracle multi-statement function bodies:**\n\n1. **Inline as a single expression using `CASE` and CTEs** (preferred when the logic is pure):\n\n   ```sql\n   -- Oracle\n   CREATE OR REPLACE FUNCTION classify_amount(p_amount NUMBER) RETURN VARCHAR2 AS\n       v_result VARCHAR2(20);\n   BEGIN\n       IF p_amount < 100 THEN\n           v_result := 'small';\n       ELSIF p_amount < 1000 THEN\n           v_result := 'medium';\n       ELSE\n           v_result := 'large';\n       END IF;\n       RETURN v_result;\n   END;\n   /\n\n   -- DBSQL (rewrite as a single CASE expression)\n   CREATE OR REPLACE FUNCTION my_catalog.my_schema.classify_amount(p_amount DECIMAL(18, 2))\n   RETURNS STRING\n   RETURN CASE\n       WHEN p_amount < 100  THEN 'small'\n       WHEN p_amount < 1000 THEN 'medium'\n       ELSE 'large'\n   END;\n   ```\n\n2. **Inline lookups as scalar subqueries**:\n\n   ```sql\n   -- Oracle\n   CREATE OR REPLACE FUNCTION customer_total(p_customer NUMBER) RETURN NUMBER AS\n       v_total NUMBER;\n   BEGIN\n       SELECT SUM(amount) INTO v_total FROM orders WHERE customer_id = p_customer;\n       RETURN NVL(v_total, 0);\n   END;\n   /\n\n   -- DBSQL\n   CREATE OR REPLACE FUNCTION my_catalog.my_schema.customer_total(p_customer INT)\n   RETURNS DECIMAL(18, 2)\n   RETURN COALESCE(\n       (SELECT SUM(amount) FROM my_catalog.my_schema.orders WHERE customer_id = p_customer),\n       0\n   );\n   ```\n\n3. **Convert to a procedure with an OUT parameter** when the logic genuinely cannot be expressed as a single expression (loops over arbitrary collections, exception-driven control flow, side effects). Callers must change from `v := f(x)` to `CALL p(x, v)`.\n\n**Multiple `RETURN` paths**: Oracle allows several `RETURN` statements within IF/CASE branches. Collapse all paths into a single `CASE` expression — a DBSQL UDF body has exactly one `RETURN`.\n\n### Functions with side effects (DML, transactions)\n\nOracle PL/SQL functions can execute DML and transaction control (especially with `PRAGMA AUTONOMOUS_TRANSACTION`). DBSQL SQL UDFs are **pure** — the body must not perform `INSERT`, `UPDATE`, `DELETE`, `MERGE`, `COMMIT`, or `ROLLBACK`. Any function that mutates state must be converted to a **procedure** with an OUT parameter:\n\n```sql\n-- Oracle (function with DML — illegal in DBSQL)\nCREATE OR REPLACE FUNCTION log_and_return(p_msg VARCHAR2) RETURN NUMBER AS\n    PRAGMA AUTONOMOUS_TRANSACTION;\n    v_id NUMBER;\nBEGIN\n    INSERT INTO audit_log (msg) VALUES (p_msg) RETURNING id INTO v_id;\n    COMMIT;\n    RETURN v_id;\nEND;\n/\n\n-- DBSQL: convert to a procedure\nCREATE OR REPLACE PROCEDURE my_catalog.my_schema.log_and_return(IN p_msg STRING, OUT v_id BIGINT)\nLANGUAGE SQL\nSQL SECURITY INVOKER\nAS BEGIN\n    -- NOTE: Oracle FUNCTION converted to PROCEDURE — DBSQL SQL UDFs cannot perform DML.\n    -- Callers that previously wrote `v := log_and_return(msg)` must now use\n    --   DECLARE v BIGINT; CALL log_and_return(msg, v);\n    INSERT INTO my_catalog.my_schema.audit_log (msg) VALUES (p_msg);\n    SET v_id = (SELECT MAX(id) FROM my_catalog.my_schema.audit_log WHERE msg = p_msg);\nEND;\n```\n\nCallers in other functions or views break here — a function called from a `SELECT` cannot be replaced by a procedure call. Flag those call sites in a migration TODO.\n\n### Exception handling in functions\n\nOracle functions can include `EXCEPTION WHEN ... THEN` blocks that catch errors and `RETURN` a fallback value. DBSQL SQL UDFs have **no exception handler** — the body is a single expression. Translate as follows:\n\n- **`NO_DATA_FOUND`** → wrap the lookup with `COALESCE` and a default:\n  ```sql\n  -- Oracle\n  BEGIN\n      SELECT col INTO v FROM t WHERE id = p_id;\n      RETURN v;\n  EXCEPTION WHEN NO_DATA_FOUND THEN RETURN -1;\n  END;\n  -- DBSQL\n  RETURN COALESCE((SELECT col FROM my_catalog.my_schema.t WHERE id = p_id), -1)\n  ```\n- **`ZERO_DIVIDE`** → guard with `CASE WHEN denom = 0 THEN ... ELSE num / denom END` or use `TRY_DIVIDE(num, denom)`.\n- **`VALUE_ERROR` / cast failures** → use `TRY_CAST(expr AS type)` which returns NULL on failure, then `COALESCE` to a default.\n- **`WHEN OTHERS THEN RETURN <default>`** → no expression-level catch-all in DBSQL. If the body is genuinely unsafe, convert to a procedure with a `DECLARE EXIT HANDLER FOR SQLEXCEPTION` block and an OUT parameter.\n\n```sql\n-- Oracle: function with NO_DATA_FOUND fallback\nCREATE OR REPLACE FUNCTION lookup_name(p_id NUMBER) RETURN VARCHAR2 AS\n    v_name VARCHAR2(100);\nBEGIN\n    SELECT name INTO v_name FROM users WHERE id = p_id;\n    RETURN v_name;\nEXCEPTION\n    WHEN NO_DATA_FOUND THEN RETURN 'unknown';\nEND;\n/\n\n-- DBSQL: COALESCE with default\nCREATE OR REPLACE FUNCTION my_catalog.my_schema.lookup_name(p_id INT)\nRETURNS STRING\nRETURN COALESCE(\n    (SELECT name FROM my_catalog.my_schema.users WHERE id = p_id),\n    'unknown'\n);\n```\n\n### Table-valued functions\n\nOracle has two forms: pipelined table functions (`PIPELINED ... PIPE ROW(...)`) and functions returning a nested table or `SYS_REFCURSOR`. DBSQL supports table UDFs via `RETURNS TABLE(...) RETURN <SELECT statement>`:\n\n```sql\n-- Oracle pipelined table function\nCREATE OR REPLACE FUNCTION orders_by_customer(p_customer NUMBER)\nRETURN order_tab_t PIPELINED AS\nBEGIN\n    FOR rec IN (SELECT order_id, amount FROM orders WHERE customer_id = p_customer) LOOP\n        PIPE ROW(order_row_t(rec.order_id, rec.amount));\n    END LOOP;\n    RETURN;\nEND;\n/\n\n-- DBSQL table UDF — single SELECT, no row-by-row piping\nCREATE OR REPLACE FUNCTION my_catalog.my_schema.orders_by_customer(p_customer INT)\nRETURNS TABLE (order_id INT, amount DECIMAL(18, 2))\nRETURN\n    SELECT order_id, amount\n    FROM my_catalog.my_schema.orders\n    WHERE customer_id = p_customer;\n```\n\nCalling a DBSQL table UDF: `SELECT * FROM my_catalog.my_schema.orders_by_customer(42);` — the function appears in the `FROM` clause like a parameterized view.\n\n**Pipelined functions with imperative logic** (transformation per row before `PIPE ROW`): collapse the per-row transformation into the SELECT list. Pipelined functions that drive iteration via cursors over multiple sources translate to a single `SELECT` with joins or `UNION ALL`.\n\n**`TABLE(function(...))` collection unnesting** in Oracle queries (e.g. `SELECT * FROM TABLE(my_func(...))`) — DBSQL calls the table UDF directly: `SELECT * FROM my_func(...)`. Drop the `TABLE(...)` wrapper.\n\n### Default parameter values\n\nBoth dialects support default values. Oracle uses `DEFAULT` (or `:=`); DBSQL uses `DEFAULT`:\n\n```sql\n-- Oracle\nCREATE OR REPLACE FUNCTION greet(name VARCHAR2 DEFAULT 'world') RETURN VARCHAR2 AS\nBEGIN RETURN 'hello ' || name; END;\n/\n\n-- DBSQL\nCREATE OR REPLACE FUNCTION my_catalog.my_schema.greet(name STRING DEFAULT 'world')\nRETURNS STRING\nRETURN CONCAT('hello ', name);\n```\n\n**Named-argument calls** (`my_func(p_name => 'x')`) — Oracle supports this; DBSQL uses positional arguments only in function calls. Convert any named-argument call sites to positional.\n\n### Function overloading — no DBSQL equivalent\n\nOracle packages allow overloading — multiple functions with the same name but different parameter signatures. DBSQL does not support function overloading: a function is uniquely identified by `catalog.schema.name`, regardless of signature. `CREATE OR REPLACE FUNCTION` with a different signature **replaces** the existing function rather than adding an overload.\n\n**Translation:** rename each overload with a signature-suffixed name, or consolidate into a single function whose parameters cover all overloads (using `STRING` + parsing, or nullable parameters):\n\n```sql\n-- Oracle: overloaded in package\nFUNCTION format_amount(p_amt NUMBER) RETURN VARCHAR2;\nFUNCTION format_amount(p_amt NUMBER, p_currency VARCHAR2) RETURN VARCHAR2;\n\n-- DBSQL: rename, or unify with a default\nCREATE OR REPLACE FUNCTION my_catalog.my_schema.format_amount(p_amt DECIMAL(18, 2), p_currency STRING DEFAULT 'USD')\nRETURNS STRING\nRETURN CONCAT(p_currency, ' ', CAST(p_amt AS STRING));\n```\n\n### Recursive functions\n\nOracle PL/SQL allows recursive function calls. **DBSQL SQL UDFs cannot reference themselves** — a recursive call inside a SQL UDF body fails to resolve. Translation options:\n\n- **Rewrite as recursive CTE** at each call site (and inline the function logic into the CTE).\n- **Convert to a Python UDF** if the recursion is required and the call sites cannot be rewritten.\n- **Iterative procedure** with a counter / staging table when the recursion depth is bounded and DML is acceptable.\n\nFlag any recursive Oracle function with a TODO for manual review.\n\n### Subprogram clauses (`DETERMINISTIC`, `RESULT_CACHE`, `PARALLEL_ENABLE`, `AUTHID`, `ACCESSIBLE BY`)\n\nOracle attaches a number of clauses to subprogram declarations. Most have no DBSQL equivalent and are simply dropped:\n\n| Oracle clause                                                                    | DBSQL                                                                                    | Notes                                                                                                                                                                                                                                            |\n| -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `DETERMINISTIC`                                                                  | (no keyword needed)                                                                      | DBSQL pure SQL UDFs are deterministic by construction. Drop the keyword.                                                                                                                                                                         |\n| `RESULT_CACHE [RELIES_ON (t1, t2)]`                                              | (no keyword needed)                                                                      | Photon caches deterministic UDF results inside a query automatically; cross-query caching is handled by the query result cache. Drop the keyword and the dependency list.                                                                        |\n| `PARALLEL_ENABLE [(PARTITION p BY {ANY \\| HASH \\| RANGE} ...)]`                  | (no keyword needed)                                                                      | Spark parallelizes UDFs by default. The `PARTITION BY` clause for parallel-enabled pipelined functions has no equivalent — when collapsing pipelined functions to a single SELECT, partitioning is handled by the query optimizer.               |\n| `PIPELINED [USING <impl_type>]`                                                  | `RETURNS TABLE (...)` form                                                               | See \"Table-valued functions\" above. Drop the `USING <impl_type>` (ODCITable interface).                                                                                                                                                          |\n| `AGGREGATE USING <type>` / `ANALYTIC USING <type>` (user-defined aggregates)     | TODO — manual rewrite                                                                    | Oracle ODCIAggregate user-defined aggregates / window functions have no DBSQL equivalent. Convert to a Spark UDAF or rewrite the call sites to native window functions if possible.                                                              |\n| `AUTHID DEFINER`                                                                 | `SQL SECURITY DEFINER` (procedures); default for functions                               | Default for both Oracle and DBSQL functions. Procedures: explicit `SQL SECURITY DEFINER` runs as the owner.                                                                                                                                      |\n| `AUTHID CURRENT_USER`                                                            | `SQL SECURITY INVOKER` (procedures only); not supported on functions                     | Procedures running with caller's privileges. Functions in DBSQL only support `DEFINER` — if the original was `AUTHID CURRENT_USER` for a function, convert to a procedure or to a row-filter / column-mask dynamic view.                         |\n| `ACCESSIBLE BY (<allowlist>)`                                                    | No equivalent — drop, then `REVOKE`/`GRANT`                                              | Oracle 12c+ compile-time check restricting which packages / procedures can call this subprogram. Drop the clause; enforce at runtime via Unity Catalog `REVOKE EXECUTE ... FROM \\`account users\\``+`GRANT EXECUTE ... TO <specific principals>`. |\n| `SHARING = METADATA \\| NONE` (Edition-Based Redefinition)                        | No equivalent — drop                                                                     | EBR has no DBSQL counterpart.                                                                                                                                                                                                                    |\n| `SQL_MACRO [(SCALAR \\| TABLE)]` (Oracle 19c+ macros)                             | TODO — rewrite                                                                           | SQL macros that expand at parse time have no DBSQL equivalent. Inline the macro body at each call site, or convert to a regular UDF/table UDF (with the loss of parse-time expansion).                                                           |\n| `EDITIONABLE` / `NONEDITIONABLE`                                                 | No equivalent — drop                                                                     | Edition-Based Redefinition modifier; DBSQL has no editions concept.                                                                                                                                                                              |\n| `BEQUEATH CURRENT_USER` (on views, not subprograms — flagged here for proximity) | `WITH SCHEMA EVOLUTION` is unrelated; use a dynamic view with `current_user()` filtering | EBR / definer-rights view modifier. No DBSQL equivalent — use a dynamic view that filters by `current_user()`.                                                                                                                                   |\n\n### Local (nested) subprograms\n\nOracle PL/SQL allows declaring a procedure or function **inside** another procedure / function / anonymous block, in the `DECLARE` section. The nested subprogram is private to the enclosing block and can see the enclosing block's local variables.\n\n```sql\n-- Oracle: nested procedure inside an outer procedure\nCREATE OR REPLACE PROCEDURE outer_proc(p_x IN NUMBER) AS\n    v_total NUMBER := 0;\n\n    PROCEDURE add_to_total(p_y IN NUMBER) IS\n    BEGIN\n        v_total := v_total + p_y;          -- closes over outer's v_total\n    END;\nBEGIN\n    add_to_total(p_x);\n    add_to_total(p_x * 2);\n    INSERT INTO log VALUES (v_total);\nEND;\n/\n```\n\n**DBSQL has no nested subprograms.** Two translation paths:\n\n1. **Inline the nested body at each call site** — works when the nested subprogram is short and called from only a few places (this is the most common case in real Oracle code).\n2. **Promote to a top-level procedure** with explicit parameters for the values that were previously closed over — works when the nested subprogram is reused across multiple call sites and refactoring isn't disruptive.\n\n```sql\n-- DBSQL — option 1: inline (preferred for short nested bodies)\nCREATE OR REPLACE PROCEDURE my_catalog.my_schema.outer_proc(IN p_x INT)\nLANGUAGE SQL\nSQL SECURITY INVOKER\nAS BEGIN\n    DECLARE v_total INT DEFAULT 0;\n    SET v_total = v_total + p_x;\n    SET v_total = v_total + p_x * 2;\n    INSERT INTO my_catalog.my_schema.log VALUES (v_total);\nEND;\n\n-- DBSQL — option 2: promote and pass state explicitly\nCREATE OR REPLACE FUNCTION my_catalog.my_schema.outer_proc_add_to_total(v_total INT, p_y INT)\nRETURNS INT\nRETURN v_total + p_y;\n\nCREATE OR REPLACE PROCEDURE my_catalog.my_schema.outer_proc(IN p_x INT)\nLANGUAGE SQL\nSQL SECURITY INVOKER\nAS BEGIN\n    DECLARE v_total INT DEFAULT 0;\n    SET v_total = my_catalog.my_schema.outer_proc_add_to_total(v_total, p_x);\n    SET v_total = my_catalog.my_schema.outer_proc_add_to_total(v_total, p_x * 2);\n    INSERT INTO my_catalog.my_schema.log VALUES (v_total);\nEND;\n```\n\nIf the nested subprogram had **forward declarations** (`PROCEDURE foo;` declared before its body to allow mutual recursion among nested subprograms), drop the forward declarations and rely on `CREATE OR REPLACE` ordering at the top level.\n\n### `%TYPE` / `%ROWTYPE` in function signatures\n\nOracle allows `RETURN users.name%TYPE` or parameter `p_user IN users%ROWTYPE`. DBSQL has no equivalent — resolve to the concrete column type during migration:\n\n```sql\n-- Oracle\nFUNCTION fetch_name(p_id users.id%TYPE) RETURN users.name%TYPE;\n\n-- DBSQL: resolve to actual types\nCREATE OR REPLACE FUNCTION my_catalog.my_schema.fetch_name(p_id INT)\nRETURNS STRING\nRETURN (SELECT name FROM my_catalog.my_schema.users WHERE id = p_id);\n```\n\nFor `%ROWTYPE` parameters: refactor to individual column parameters, or pass a `STRUCT` / table reference and rewrite the body.\n\n> **Same rule applies to untyped `NUMBER` / `VARCHAR2` parameters and return types — match the underlying column when the column is known.** Oracle PL/SQL frequently declares parameters as bare `NUMBER` or `VARCHAR2` without `%TYPE`, even when the value is destined for a specific known column. The naive translation picks a generic widest-fit (`DECIMAL(38, 2)` for any `NUMBER`, unbounded `STRING` for any `VARCHAR2`); when the target column is already known, **use the column's exact type** instead, end-to-end through every routine that reads or writes it.\n>\n> ```sql\n> -- Schema (already migrated)\n> CREATE TABLE my_catalog.my_schema.orders (\n>     order_id     BIGINT NOT NULL,\n>     total_amount DECIMAL(12, 2),\n>     status       STRING,\n>     ...\n> );\n>\n> -- Oracle (parameter types are bare NUMBER / VARCHAR2 — no %TYPE)\n> PROCEDURE create_order(p_order_id     IN NUMBER,\n>                        p_total_amount IN NUMBER,\n>                        p_status       IN VARCHAR2);\n>\n> -- DBSQL — WRONG: picks generic widest-fit, doesn't propagate the column types\n> CREATE OR REPLACE PROCEDURE my_catalog.my_schema.create_order(\n>     IN p_order_id     STRING,           -- mismatch: column is BIGINT, not STRING\n>     IN p_total_amount DECIMAL(38, 2),   -- mismatch: column is DECIMAL(12, 2)\n>     IN p_status       STRING            -- OK (status is STRING in DBSQL)\n> ) ...\n>\n> -- DBSQL — RIGHT: types match the column at every API boundary\n> CREATE OR REPLACE PROCEDURE my_catalog.my_schema.create_order(\n>     IN p_order_id     BIGINT,\n>     IN p_total_amount DECIMAL(12, 2),\n>     IN p_status       STRING\n> ) ...\n> ```\n>\n> **Why it matters:** type-mismatched parameters succeed at procedure-create time (DBSQL coerces silently across most numeric / string types) but fail unpredictably at runtime — `INSERT` raises a constraint or overflow error, predicates skip rows due to implicit conversion ranking, and callers passing the column's native type get bind-time rejections. Symmetrically, return-type widening (`RETURNS DECIMAL(38, 2)` over a `DECIMAL(12, 2)` column) gives downstream callers more headroom than the column itself can hold, masking overflow until a later DML touches the column.\n>\n> **Rule of thumb:** before writing the parameter list of any migrated procedure or function, identify every column the parameter flows into or out of, and use the **narrowest** column type that covers all of them. Only fall back to widest-fit (`DECIMAL(38, 2)`, unbounded `STRING`) when no column boundary is known.\n\n### Built-in package functions (DBMS*\\*, UTL*\\*)\n\nCalls to Oracle-built-in package functions inside a UDF body (`DBMS_LOB.SUBSTR`, `UTL_RAW.CAST_TO_VARCHAR2`, `DBMS_RANDOM.VALUE`, etc.) have no DBSQL equivalent. Map common ones inline; for the rest, flag a TODO. Quick-reference subset below — for the full per-member catalog (`DBMS_*`, `UTL_*`, `OWA_*`, `CTX_*`, `SDO_*`, `ORD_*`) load `oracle/12-builtin-packages.md`.\n\n| Oracle                      | DBSQL                                          |\n| --------------------------- | ---------------------------------------------- |\n| `DBMS_RANDOM.VALUE`         | `RAND()`                                       |\n| `DBMS_RANDOM.VALUE(lo, hi)` | `lo + (hi - lo) * RAND()`                      |\n| `DBMS_LOB.SUBSTR(c, n, p)`  | `SUBSTR(c, p, n)` (argument order swap)        |\n| `DBMS_LOB.GETLENGTH(c)`     | `LENGTH(c)` (or `OCTET_LENGTH(c)` for bytes)   |\n| `DBMS_UTILITY.GET_TIME`     | `UNIX_MILLIS(CURRENT_TIMESTAMP)`               |\n| `UTL_RAW.CAST_TO_VARCHAR2`  | `CAST(col AS STRING)` for compatible encodings |\n\n> The subset above is the most common case. Anything else — `DBMS_LOCK`, `DBMS_SCHEDULER`, `DBMS_AQ`, `DBMS_CRYPTO`, `DBMS_RLS`, `UTL_FILE`, `UTL_HTTP`, etc. — load `oracle/12-builtin-packages.md` for the full mapping or out-of-scope guidance.\n\n### Cursors — see `oracle/04a-cursors.md`\n\nLiteral cursor translation (explicit cursors, parameterized cursors, `REF CURSOR` / `SYS_REFCURSOR` variables, `FETCH ... BULK COLLECT INTO`, `WHERE CURRENT OF`, `SELECT ... INTO`) is covered in **`oracle/04a-cursors.md`**. Load it whenever the source contains `CURSOR ... IS`, `OPEN`/`FETCH`/`CLOSE`, `FOR rec IN cursor LOOP`, cursor `FOR UPDATE`, or `WHERE CURRENT OF`. The `OUT SYS_REFCURSOR` result-set return pattern stays in this file under `### REF CURSOR / SYS_REFCURSOR result sets` — `04a` covers cursor variables used **inside** a procedure body.\n\n### Error Handling — see `oracle/04b-error-handling.md`\n\nException translation (Oracle `EXCEPTION WHEN ... THEN` → DBSQL `DECLARE ... HANDLER`, predefined exception catalog, `PRAGMA EXCEPTION_INIT`, user-defined named exceptions, propagation across nested blocks, `SQLERRM` / `DBMS_UTILITY.FORMAT_*` introspection helpers, `EXIT` vs `CONTINUE` handlers) is covered in **`oracle/04b-error-handling.md`**. Load it whenever the source contains `EXCEPTION WHEN`, `RAISE_APPLICATION_ERROR`, `RAISE`, a predefined exception name (`NO_DATA_FOUND`, `TOO_MANY_ROWS`, `DUP_VAL_ON_INDEX`, `ZERO_DIVIDE`, etc.), or a `<name> EXCEPTION;` declaration.\n\n### Variables\n\n```sql\n-- Oracle\nDECLARE\n    v_var NUMBER := 10;\n    v_a NUMBER;\n    v_b VARCHAR2(100);\nBEGIN\n    v_var := (SELECT MAX(id) FROM t);\n    SELECT col1, col2 INTO v_a, v_b FROM t WHERE id = 1;\nEND;\n/\n\n-- DBSQL\nBEGIN\n    DECLARE v_var INT DEFAULT 10;\n    DECLARE v_a INT;\n    DECLARE v_b STRING;\n    SET v_var = (SELECT MAX(id) FROM my_catalog.my_schema.t);\n    SET (v_a, v_b) = (SELECT col1, col2 FROM my_catalog.my_schema.t WHERE id = 1);\nEND;\n```\n\n**Inside stored procedure bodies**: `DECLARE` statements must appear inside `BEGIN...END`, not before it. Placing `DECLARE` outside the procedure body causes `PARSE_SYNTAX_ERROR: extra input '<variable_name>'`.\n\n```sql\n-- Oracle procedure with local variables\nCREATE OR REPLACE PROCEDURE my_proc(p1 IN NUMBER) AS\n    v_name VARCHAR2(100);\n    v_count NUMBER := 0;\nBEGIN\n    v_name := 'value';\nEND;\n/\n\n-- DBSQL: DECLARE goes inside BEGIN, immediately after BEGIN\nCREATE OR REPLACE PROCEDURE my_catalog.my_schema.my_proc(IN p1 INT)\nLANGUAGE SQL\nSQL SECURITY INVOKER\nAS BEGIN\n    DECLARE v_name STRING;\n    DECLARE v_count INT DEFAULT 0;\n    SET v_name = 'value';\nEND;\n```\n\n**`%TYPE` and `%ROWTYPE`** — Oracle declares variables with the same type as a column or row (`v_name users.name%TYPE`, `v_row users%ROWTYPE`). DBSQL has no equivalent. Resolve to the concrete type during migration:\n\n```sql\n-- Oracle\nv_name users.name%TYPE;    -- if users.name is VARCHAR2(100)\n\n-- DBSQL\nDECLARE v_name STRING;     -- resolve to the actual type\n```\n\nFor `%ROWTYPE`, DBSQL has no composite row type — refactor to individual variables or a `STRUCT` if available.\n\n**Variable assignment edge cases:**\n\n- If the `SELECT INTO` returns **0 rows**, Oracle raises `NO_DATA_FOUND`; DBSQL leaves the variable at its declared default. If the source relied on `NO_DATA_FOUND`, add a handler that detects the default-valued variable.\n- If the `SELECT` returns **multiple rows**, Oracle raises `TOO_MANY_ROWS`; DBSQL also raises an error. No drift.\n\n### Transaction control (`COMMIT` / `ROLLBACK` / `SAVEPOINT`)\n\nOracle PL/SQL routinely uses `COMMIT;` and `ROLLBACK;` inside procedure bodies to define transaction boundaries. DBSQL has **no Oracle-style multi-statement transaction control inside procedures** — strip these statements and review the call-site semantics.\n\n| Oracle                                                              | DBSQL         | Action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |\n| ------------------------------------------------------------------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `COMMIT;` (statement-level inside a procedure body)                 | None          | **Remove.** In notebook / classic compute context each DML statement auto-commits, so an explicit `COMMIT` is a no-op. Inside a `BEGIN ATOMIC` block the entire body is already one atomic unit, and an explicit `COMMIT` is a syntax error. **Note:** `BEGIN ATOMIC` rejects non-deterministic expressions, including `NEXT VALUE FOR seq` — see the gotcha and escape hatches in `01-schema-ddl.md` \xa7SEQUENCE objects before reaching for `BEGIN ATOMIC` as the atomicity story for any procedure that calls a sequence. |\n| `ROLLBACK;` (typically inside an `EXCEPTION` handler)               | None          | **Remove.** In notebook context every prior statement has already committed, so a rollback cannot reach them. In `BEGIN ATOMIC`, raising via `SIGNAL` rolls the whole block back automatically — explicit `ROLLBACK` is unnecessary and disallowed.                                                                                                                                                                                                                                                                        |\n| `SAVEPOINT sp1; ... ROLLBACK TO sp1;`                               | No equivalent | Flag with a TODO — partial-rollback semantics cannot be preserved. Either widen the rollback to the whole block (`BEGIN ATOMIC` + `SIGNAL`) or move the partial-rollback logic outside the procedure.                                                                                                                                                                                                                                                                                                                      |\n| `SET TRANSACTION READ ONLY` / `SET TRANSACTION ISOLATION LEVEL ...` | No equivalent | Remove. Delta provides snapshot isolation by default; isolation level cannot be tuned per transaction.                                                                                                                                                                                                                                                                                                                                                                                                                     |\n\n```sql\n-- Oracle\nCREATE OR REPLACE PROCEDURE post_payment(p_id IN NUMBER) AS BEGIN\n    UPDATE accounts SET balance = balance - 100 WHERE id = p_id;\n    INSERT INTO ledger (account_id, delta) VALUES (p_id, -100);\n    COMMIT;                                      -- explicit transaction boundary\nEXCEPTION\n    WHEN OTHERS THEN\n        ROLLBACK;                                -- undo both statements on failure\n        RAISE_APPLICATION_ERROR(-20001, 'post_payment failed');\nEND;\n/\n\n-- DBSQL (notebook/classic) — drop COMMIT/ROLLBACK; each DML auto-commits\nCREATE OR REPLACE PROCEDURE my_catalog.my_schema.post_payment(IN p_id INT)\nLANGUAGE SQL\nSQL SECURITY INVOKER\nAS BEGIN\n    DECLARE EXIT HANDLER FOR SQLEXCEPTION\n        SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = 'post_payment failed';\n    UPDATE my_catalog.my_schema.accounts SET balance = balance - 100 WHERE id = p_id;\n    INSERT INTO my_catalog.my_schema.ledger (account_id, delta) VALUES (p_id, -100);\n    -- COMMIT removed: each statement above already committed independently.\n    -- TODO: confirm callers do not rely on atomic UPDATE+INSERT — this body is no\n    -- longer atomic in notebook context. Use BEGIN ATOMIC on a SQL warehouse if\n    -- atomicity is required.\nEND;\n\n-- DBSQL (SQL warehouse, *no sequences in the body*) — body is already atomic;\n-- COMMIT/ROLLBACK still removed.\nCREATE OR REPLACE PROCEDURE my_catalog.my_schema.post_payment(IN p_id INT)\nLANGUAGE SQL\nSQL SECURITY INVOKER\nBEGIN ATOMIC\n    UPDATE my_catalog.my_schema.accounts SET balance = balance - 100 WHERE id = p_id;\n    INSERT INTO my_catalog.my_schema.ledger (account_id, delta) VALUES (p_id, -100);\n    -- No COMMIT — BEGIN ATOMIC commits implicitly on END. SIGNAL inside this block\n    -- rolls back both statements automatically.\nEND;\n```\n\n**Caller-side review (mandatory):** any time you remove a `COMMIT;` from inside a procedure, check whether a caller depended on the procedure committing **before** the caller's next action (e.g. another procedure reading the just-written row, or a notification emitted from a session-context view). With per-statement auto-commit this still works in notebook context, but the ordering guarantee is now per-statement instead of per-procedure-end. Flag with a TODO when in doubt.\n\n**Caller-side review for `ROLLBACK` removal:** if the original procedure caught an exception, rolled back, and **continued** (rather than re-raising), the DBSQL translation must explicitly null-out or compensate the partial writes — the rollback is not going to undo them. Search the original handler for `ROLLBACK;` followed by anything other than `RAISE;` / `RAISE_APPLICATION_ERROR(...)`; those are the at-risk procedures.\n\n#### Atomicity + sequences: a real gotcha\n\n`BEGIN ATOMIC` rejects non-deterministic expressions, and `NEXT VALUE FOR seq` is non-deterministic. Any Oracle procedure that did `INSERT INTO t VALUES (seq.NEXTVAL, ...)` inside a transaction therefore **cannot** be translated by simply wrapping the body in `BEGIN ATOMIC` — the block will fail to compile.\n\nDecision tree when you encounter the pattern:\n\n1. **Can the surrogate key column become an `IDENTITY` column?** → Yes in 90% of cases. Drop the column from the `INSERT`, declare it `GENERATED ALWAYS AS IDENTITY`, and use `BEGIN ATOMIC` freely. This is the recommended path. See `01-schema-ddl.md` \xa7SEQUENCE objects for the schema change and worked example.\n2. **Must the explicit sequence be retained?** → Pre-compute `NEXT VALUE FOR` into a local variable in the **outer** non-atomic `AS BEGIN ... END`, then reference the variable inside the inner `BEGIN ATOMIC`. The outer `SET` advances the sequence; the inner block sees only the deterministic variable. (Sequence numbers are still burned on rollback — this matches Oracle.)\n3. **Did the original procedure actually need atomicity, or was the Oracle `COMMIT` purely a durability marker?** → If callers don't depend on multi-statement atomicity, translate as plain `AS BEGIN ... END`, keep `NEXT VALUE FOR` inline, and accept per-statement auto-commit. Add a TODO if unsure.\n\nWalk through option (1) and (2) on every procedure that contains both a sequence call and DML to other tables; do **not** silently drop the surrounding `COMMIT;` and wrap the body in `BEGIN ATOMIC` — it will look correct in review and fail at runtime.\n\n### Subprogram-level pragmas\n\nOracle PL/SQL supports a number of `PRAGMA` directives inside subprogram bodies. Most are compile-time hints with no semantic effect that DBSQL can preserve — drop them. The exceptions (`AUTONOMOUS_TRANSACTION`, `EXCEPTION_INIT`) need translation work.\n\n| Oracle pragma                                                                    | DBSQL action                                      | Notes                                                                                                                                                                                                             |\n| -------------------------------------------------------------------------------- | ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `PRAGMA AUTONOMOUS_TRANSACTION;`                                                 | Drop (most cases) or refactor (rare cases)        | See \"Autonomous transactions\" below for the decision tree. The pragma itself is removed; behavior may need a small DBSQL rewrite when the caller rolls back.                                                      |\n| `PRAGMA EXCEPTION_INIT(<name>, -<n>);`                                           | Drop the pragma; rewrite the `WHEN <name>` branch | See `04b-error-handling.md` \xa7`PRAGMA EXCEPTION_INIT`.                                                                                                                                                             |\n| `PRAGMA SERIALLY_REUSABLE;`                                                      | Drop                                              | Package-level; affects per-session state lifetime. DBSQL has no per-session package state, so the pragma has no effect after migration. Also covered in `11-user-packages.md`.                                    |\n| `PRAGMA RESTRICT_REFERENCES(<name>, WNDS, RNDS, WNPS, RNPS);`                    | Drop                                              | Pre-Oracle-8 purity declaration; subsumed by `DETERMINISTIC` and inferred by the optimizer. DBSQL infers purity automatically. Also covered in `11-user-packages.md`.                                             |\n| `PRAGMA INLINE(<name>, 'YES' \\| 'NO');`                                          | Drop                                              | Compiler hint requesting / suppressing inlining of a call. DBSQL has no equivalent and no inlining knob. Removing it has no semantic effect.                                                                      |\n| `PRAGMA UDF;`                                                                    | Drop                                              | Hint that the function is intended to be called from SQL. DBSQL has no equivalent and no semantic effect.                                                                                                         |\n| `PRAGMA COVERAGE('NOT_FEASIBLE' \\| 'NOT_FEASIBLE_START' \\| 'NOT_FEASIBLE_END');` | Drop                                              | Marks a code path as unreachable for `DBMS_PLSQL_CODE_COVERAGE`. DBSQL has no PL/SQL coverage tooling.                                                                                                            |\n| `PRAGMA DEPRECATE(<name>[, '<message>']);`                                       | Drop, optionally add a `RAISE NOTICE`             | Oracle 12c+ compile-time deprecation warning. DBSQL has no compile-time warning facility — if the deprecation matters operationally, log the call via `INSERT INTO deprecation_log VALUES (...)` inside the body. |\n| `PRAGMA SUPPRESSES_WARNING_6009;`                                                | Drop                                              | Suppresses a specific PL/SQL compiler warning. DBSQL has no equivalent.                                                                                                                                           |\n| `PRAGMA INTERFACE(C, ...);`                                                      | TODO — drop and re-implement                      | Declares an external C-language implementation. The body is not portable.                                                                                                                                         |\n\n### Autonomous transactions\n\nOracle `PRAGMA AUTONOMOUS_TRANSACTION` lets a subprogram commit independently of its caller — the autonomous block opens a private transaction, runs to its own `COMMIT` / `ROLLBACK`, and returns to the caller without affecting the caller's transaction state. **DBSQL has no equivalent** because it has no multi-statement caller transactions to be autonomous _from_. Translation depends on what the pragma was protecting:\n\n| Original Oracle use case                                                           | DBSQL translation                                                                                                                                                                                                                                                                                            |\n| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| **Audit / log writes that must persist even if the caller rolls back**             | Drop the pragma — each `INSERT INTO audit_log` already commits independently in notebook / classic context. If the surrounding procedure uses `BEGIN ATOMIC` and a `SIGNAL` rolls everything back, move the `INSERT` to a separate top-level procedure called from the handler **outside** the atomic block. |\n| **Error logging inside an `EXCEPTION` handler**                                    | Same — the handler-side `INSERT` commits in notebook / classic context. In `BEGIN ATOMIC`, the `SIGNAL` rolls the audit row back too; move the audit write to the surrounding non-atomic block.                                                                                                              |\n| **Side-effecting function** (Oracle disallows DML in functions without the pragma) | Convert the function to a procedure (DBSQL functions are also pure-by-construction), then call from the original site as a `CALL`.                                                                                                                                                                           |\n| **Mutating-table workaround** (writing to the same table being read)               | Not applicable — DBSQL has no mutating-table restriction. Drop the pragma and the subprogram; merge the logic into the caller.                                                                                                                                                                               |\n| **Independent SCN assignment** (rare; some replication tooling)                    | TODO — flag for reviewer; replication / CDC is generally handled outside the procedure layer in Databricks (Delta CDF, Auto Loader).                                                                                                                                                                         |\n\n```sql\n-- Oracle: audit-log autonomous transaction\nCREATE OR REPLACE PROCEDURE log_audit(p_msg VARCHAR2) AS\n    PRAGMA AUTONOMOUS_TRANSACTION;\nBEGIN\n    INSERT INTO audit_log (message) VALUES (p_msg);\n    COMMIT;\nEND;\n/\n\n-- DBSQL: drop the pragma, drop the COMMIT, leave the body as-is.\n-- Each INSERT in notebook / classic context already commits independently.\nCREATE OR REPLACE PROCEDURE my_catalog.my_schema.log_audit(IN p_msg STRING)\nLANGUAGE SQL\nSQL SECURITY INVOKER\nAS BEGIN\n    INSERT INTO my_catalog.my_schema.audit_log (message) VALUES (p_msg);\nEND;\n```\n\nAlways flag a removed `PRAGMA AUTONOMOUS_TRANSACTION` with a migration TODO so the reviewer can confirm the audit / logging path is preserved across both notebook and `BEGIN ATOMIC` callers.\n\n### `ALTER PROCEDURE` / `ALTER FUNCTION` / `DROP PROCEDURE`\n\n| Oracle                                                                               | DBSQL                                                                   | Notes                                                                                                    |\n| ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |\n| `ALTER PROCEDURE <name> COMPILE [DEBUG \\| REUSE SETTINGS \\| <plsql_param>=<value>];` | Drop                                                                    | Recompiles the body. DBSQL has no separate compile step — every `CREATE OR REPLACE` is also a compile.   |\n| `ALTER FUNCTION <name> COMPILE`                                                      | Drop                                                                    | Same.                                                                                                    |\n| `ALTER PROCEDURE <name> RENAME TO <new>;`                                            | Re-issue `CREATE OR REPLACE PROCEDURE <new>` and `DROP PROCEDURE <old>` | DBSQL has no in-place rename for procedures / functions.                                                 |\n| `DROP PROCEDURE <name>;`                                                             | `DROP PROCEDURE [IF EXISTS] my_catalog.my_schema.<name>;`               | Add the three-part name and the `IF EXISTS` guard to match typical migration script behavior.            |\n| `DROP FUNCTION <name>;`                                                              | `DROP FUNCTION [IF EXISTS] my_catalog.my_schema.<name>;`                | Same.                                                                                                    |\n| `SHOW ERRORS [PROCEDURE \\| FUNCTION] <name>;`                                        | Drop                                                                    | SQL\\*Plus introspection — see `00-quirks.md`. The error stream surfaces directly in the notebook output. |\n\n### `DBMS_OUTPUT.PUT_LINE` → driver logs\n\n`DBMS_OUTPUT.PUT_LINE` writes debug output to SQL\\*Plus. DBSQL has no equivalent within a SQL procedure. Options:\n\n- Strip the call entirely (most common — it's debug code)\n- Replace with `INSERT INTO debug_log` if the output is operationally important\n- In a notebook context, use `PRINT` / `RAISE NOTICE` (not supported in SQL procs) — convert the procedure to a Python notebook instead\n\n### `EXECUTE IMMEDIATE` (dynamic SQL)\n\nOracle `EXECUTE IMMEDIATE 'stmt'` runs dynamic SQL. DBSQL has `EXECUTE IMMEDIATE` as of DBR 14.1+:\n\n```sql\n-- Oracle\nEXECUTE IMMEDIATE 'UPDATE orders SET status = :1 WHERE id = :2' USING 'done', 42;\n\n-- DBSQL (DBR 14.1+)\nEXECUTE IMMEDIATE 'UPDATE my_catalog.my_schema.orders SET status = ? WHERE id = ?' USING 'done', 42;\n```\n\nPositional parameters use `?` in DBSQL, not `:1`, `:2`. Named parameters (`:name`) in Oracle become positional in DBSQL unless you use `EXECUTE IMMEDIATE ... USING NAMED :name = ...` (check current DBR syntax).\n\n**Dynamic SQL clause matrix.** Oracle native dynamic SQL has several variants beyond plain `EXECUTE IMMEDIATE 'stmt' USING ...`. Each maps differently to DBSQL:\n\n| Oracle dynamic-SQL clause                                                                                                       | DBSQL replacement                                                    | Notes                                                                                                                                                                                                                                                                                                                                               |\n| ------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `EXECUTE IMMEDIATE 'SELECT ... INTO v_a, v_b' USING ...`                                                                        | `EXECUTE IMMEDIATE 'SELECT ... INTO v_a, v_b' USING ...` (DBR 14.1+) | Single-row dynamic SELECT. Same shape; same `?` placeholder rules.                                                                                                                                                                                                                                                                                  |\n| `EXECUTE IMMEDIATE 'SELECT ... BULK COLLECT INTO v_arr'`                                                                        | No equivalent — stage into a temp view and read it back              | DBSQL `EXECUTE IMMEDIATE` does not return a multi-row cursor into a variable. Use `EXECUTE IMMEDIATE 'CREATE OR REPLACE TEMPORARY VIEW staged AS SELECT ...' USING ...;` then `SELECT * FROM staged` from the surrounding code.                                                                                                                     |\n| `OPEN v_ref FOR <SQL string> USING ...` (dynamic cursor open)                                                                   | TODO — no DBSQL cursor variable accepts a runtime-built SQL string   | If the dynamic part is just a value, refactor to a static cursor + parameter variable (see Parameterized cursors above). If the dynamic part is the table / column name, use `IDENTIFIER(:name)` in a static cursor. If neither works, stage results to a temp view via `EXECUTE IMMEDIATE` and then declare a static cursor over the view.         |\n| `EXECUTE IMMEDIATE ... USING IN ..., OUT ..., IN OUT ...`                                                                       | `EXECUTE IMMEDIATE ... USING ...` (positional `?`)                   | DBSQL `USING` is **input-only** — there is no `OUT` / `IN OUT` parameter direction in dynamic SQL. If the dynamic statement has `OUT` parameters (typically a procedure call), refactor to a direct `CALL` against a known procedure name, or capture outputs by selecting from the procedure's side effects.                                       |\n| `EXECUTE IMMEDIATE 'INSERT/UPDATE/DELETE ... RETURNING col INTO :v' USING ...`                                                  | TODO — DBSQL has no `RETURNING` on DML                               | Re-query the target by primary key after the DML, or use Delta CDF. See `02-select-dml.md` \xa7RETURNING INTO.                                                                                                                                                                                                                                         |\n| `DBMS_SQL` package (`OPEN_CURSOR`/`PARSE`/`BIND_VARIABLE`/`DEFINE_COLUMN`/`EXECUTE`/`FETCH_ROWS`/`COLUMN_VALUE`/`CLOSE_CURSOR`) | TODO — no DBSQL equivalent                                           | Old-style \"Method 4\" dynamic SQL: statements whose parameter list and result columns are unknown until runtime. There is no DBSQL counterpart. Most uses are either describable as native dynamic SQL after analysis (translate to `EXECUTE IMMEDIATE`) or are introspection / metadata tooling that should move to Python over the Databricks SDK. |\n| `DBMS_SQL.RETURN_RESULT` / `DBMS_SQL.GET_NEXT_RESULT`                                                                           | TODO — no DBSQL equivalent                                           | Implicit-result-set return. Consolidate into a single final `SELECT` per `### REF CURSOR / SYS_REFCURSOR result sets` above.                                                                                                                                                                                                                        |\n| Method 4 dynamic SQL (statement structure unknown until runtime)                                                                | TODO — manual rewrite required                                       | Almost always indicates a tool / framework rather than data-pipeline code. Migrate the framework, not the SQL.                                                                                                                                                                                                                                      |\n\n**Identifier injection (table / column / schema names).** When the dynamic part is an object name rather than a value, use `IDENTIFIER(:name)` in DBSQL — same effect as Oracle string concatenation, but parsed safely:\n\n```sql\n-- Oracle\nEXECUTE IMMEDIATE 'SELECT COUNT(*) FROM ' || p_table_name INTO v_cnt;\n\n-- DBSQL — IDENTIFIER() resolves the variable as an object name\nEXECUTE IMMEDIATE 'SELECT COUNT(*) FROM IDENTIFIER(:t)' INTO v_cnt USING p_table_name AS t;\n```\n\n**Mixed value-and-identifier dynamic SQL.** Some Oracle code interpolates both values and identifiers into the same string. Translate values via `?` placeholders + `USING`, and identifiers via `IDENTIFIER(:name)`:\n\n```sql\n-- Oracle\nEXECUTE IMMEDIATE 'UPDATE ' || p_table || ' SET status = :1 WHERE id = :2' USING 'done', 42;\n\n-- DBSQL\nEXECUTE IMMEDIATE 'UPDATE IDENTIFIER(:t) SET status = ? WHERE id = ?'\n    USING 'done', 42, p_table AS t;\n```\n\n> For full `EXECUTE IMMEDIATE` and `IDENTIFIER()` syntax, load `writing-sql/dynamic-sql.md`.\n\n**`DBMS_SQL` deep dives — typical patterns to call out as TODO.** When the source uses `DBMS_SQL`, identify which of the following shapes it implements; each calls for a different rewrite, and none migrate cleanly:\n\n- **Pure value-driven dynamic SQL with a known column list** — looks like `OPEN_CURSOR` → `PARSE` → `BIND_VARIABLE` (\xd7N) → `DEFINE_COLUMN` (\xd7M) → `EXECUTE` → `FETCH_ROWS` → `COLUMN_VALUE`. This is Method 3 and is migratable to `EXECUTE IMMEDIATE` with `?` placeholders + `USING`. Translate by hand and drop the package calls.\n- **DDL builder** — `DBMS_SQL.PARSE` + `EXECUTE` only, no binds, no fetches. Translate to `EXECUTE IMMEDIATE '<DDL string>';` directly.\n- **Metadata-driven dynamic SELECT** — column list discovered at runtime via `DESCRIBE_COLUMNS` and read back via `COLUMN_VALUE`. Almost never ports cleanly. Move the metadata tool out of SQL.\n- **Implicit result sets via `DBMS_SQL.RETURN_RESULT`** — see the matrix row above.\n\n**Bulk dynamic SQL** (`BULK COLLECT INTO`, `FORALL`) has no direct equivalent — rewrite as set-based SQL. See `05-temp-tables-ctes.md` \xa7BULK COLLECT and \xa7FORALL for the rewrite tables.\n"}}]);
//# sourceMappingURL=https://sourcemaps.dev.databricks.com/monolith/static/js/72770.8711fc17f0.chunk.js.map