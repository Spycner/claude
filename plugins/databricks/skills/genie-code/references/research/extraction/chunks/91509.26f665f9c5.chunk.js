"use strict";(self.webpackChunkdatabricks_monolith=self.webpackChunkdatabricks_monolith||[]).push([[91509],{391509:e=>{e.exports="# Snowflake Dialect — Cross-Cutting Quirks & Behavioral Drift\n\nApplies to `SNOWFLAKE` (Snowflake Data Cloud on AWS, Azure, and GCP). All patterns in this file apply regardless of cloud provider.\n\nLoaded eagerly before any pipeline step. Covers patterns that span all stages. Detailed date/time function mappings are in `references/snowflake/09-functions.md` — fetch that during conversion when needed.\n\n## Cross-cutting syntax quirks\n\n### Identifier case sensitivity\n\nSnowflake **uppercases unquoted identifiers** at parse time: `CREATE TABLE t` stores the object as `T`. DBSQL (Spark SQL) **lowercases unquoted identifiers** and stores metadata case-insensitively. Double-quoted `\"name\"` preserves case in both dialects.\n\n```sql\n-- Snowflake: the object is stored as USERS, and column_name as EMAIL\nCREATE TABLE users (email VARCHAR);\nSELECT email FROM users;                -- matches (case folded to upper)\nSELECT \"email\" FROM users;              -- NO match — quoted lookup is case-sensitive\n\n-- DBSQL: identifiers are lowercased / case-insensitive by default\nCREATE TABLE my_catalog.my_schema.users (email STRING);\nSELECT email FROM my_catalog.my_schema.users;\n```\n\n**Scan risk**: If the Snowflake source mixed quoted and unquoted identifiers that rely on the uppercase-folding (e.g. Python clients reading `\"EMAIL\"` from a view), the DBSQL result will have lowercase column names and break those callers. Flag when quoted identifiers appear in the source.\n\n### Three-part names → Unity Catalog three-part names\n\nSnowflake: `database.schema.object`\nDBSQL: `catalog.schema.object`\n\n```sql\n-- Snowflake\nSELECT * FROM prod_db.sales.orders;\n\n-- DBSQL\nSELECT * FROM prod_catalog.sales.orders;\n```\n\nAccount-level Snowflake objects (`WAREHOUSE`, `STAGE`, `PIPE`, `TASK`, `STREAM`, `SHARE`) have no object-level DBSQL equivalent — see `references/snowflake/01-objects-and-ddl.md` and `references/snowflake/07-pipelines.md` for the platform-level substitutes.\n\n### Session variables and bind syntax\n\n| Snowflake                                                 | DBSQL                                                                                                             |\n| --------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |\n| `SET myvar = 10;` (top-of-file, single var)               | `DECLARE OR REPLACE VARIABLE myvar INT DEFAULT 10;` (notebook/session scope; no `BEGIN ... END` wrapper required) |\n| `SET (a, b) = (val_a, val_b);` (top-of-file, tuple form)  | Split into per-variable `DECLARE OR REPLACE VARIABLE` statements — DBSQL has no tuple-DECLARE                     |\n| `SELECT $myvar;` (session reference)                      | `SELECT myvar;` (bare name; no `$` prefix)                                                                        |\n| `DECLARE myvar INT DEFAULT 10;` (inside `BEGIN ... END`)  | `DECLARE myvar INT DEFAULT 10;` (same — at top of the compound statement)                                         |\n| `EXECUTE IMMEDIATE $$ ... $$ USING (param => 1);` (named) | `EXECUTE IMMEDIATE '...' USING 1;` (positional `?`) or `USING 1 AS param` (named)                                 |\n| `:name` colon-bind inside procedure bodies                | Bare parameter name (strip `:`)                                                                                   |\n\nUse `DECLARE OR REPLACE VARIABLE` for top-of-file Snowflake `SET`s — it preserves the original script shape (top-level DML statements between SET and the first reference). Use compound-statement `DECLARE` only when the Snowflake source itself is wrapped in `BEGIN ... END`. Both forms are first-class DBSQL syntax. Snowflake procedural variables (inside Snowflake Scripting `BEGIN ... END`) are referenced with the bare name; only top-level session vars and procedure binds use `$var` / `:var`.\n\nFor full mapping with worked tuple-form examples, see `references/snowflake/05a-scripting-procs-udfs.md` → Variables.\n\n### Dollar-quoted string literals\n\nSnowflake uses `$$...$$` as an alternate string-literal delimiter — common for JavaScript / Python / Java procedure bodies and for multi-line strings with embedded quotes:\n\n```sql\n-- Snowflake: JS procedure body in $$ delimiters\nCREATE PROCEDURE demo() RETURNS STRING LANGUAGE JAVASCRIPT AS\n$$\n  return 'it works';\n$$;\n```\n\nDBSQL:\n\n- For SQL UDF / procedure bodies, `$$` is accepted as a string delimiter (useful for Python UDF bodies): `CREATE FUNCTION f() RETURNS STRING LANGUAGE PYTHON AS $$ return \"ok\" $$;`.\n- For plain string literals elsewhere, use single-quoted strings; escape embedded single-quotes with `''`.\n\n### `::` casting operator — both dialects support\n\n`expr::TYPE` works in both dialects (Snowflake and DBSQL). Safe to leave unchanged, or rewrite as `CAST(expr AS TYPE)` for clarity.\n\n### Cloud provider differences\n\nSnowflake runs identically on AWS, Azure, and GCP from a SQL surface standpoint. The same SQL source targets all three. Translation to DBSQL uses the matching DBSQL cloud (AWS / Azure / GCP) — UC three-part names are the same in every cloud.\n\n## Behavioral drift\n\nFunctions that exist in both Snowflake and DBSQL but behave differently. Highest-risk patterns — they pass syntax validation and often pass inspection, then silently produce wrong results.\n\n### DATEDIFF / TIMESTAMPDIFF — boundary-crossing vs elapsed-unit (CRITICAL)\n\nSnowflake `DATEDIFF(unit, d1, d2)` counts calendar-**boundary crossings** — `DATEDIFF(MONTH, '2021-01-31', '2021-02-01') = 1`. DBSQL 3-arg `DATEDIFF` / `TIMESTAMPDIFF` count **whole elapsed units** — `DATEDIFF(MONTH, '2021-01-31', '2021-02-01') = 0`. Same argument order, same return type, different values on partial-unit ranges.\n\n```sql\n-- Snowflake: 1 (crossed the Jan→Feb boundary)\nSELECT DATEDIFF(MONTH, '2021-01-31', '2021-02-01');\n\n-- DBSQL: 0 (one full month has NOT elapsed)\nSELECT DATEDIFF(MONTH, '2021-01-31', '2021-02-01');\n\n-- DBSQL — exact boundary-crossing equivalent\nSELECT (YEAR(DATE '2021-02-01') * 12 + MONTH(DATE '2021-02-01'))\n     - (YEAR(DATE '2021-01-31') * 12 + MONTH(DATE '2021-01-31'));\n```\n\nApplies to `YEAR`, `QUARTER`, `MONTH`, `WEEK` (and fractional time units that straddle boundaries). `DAY` / `HOUR` / `MINUTE` / `SECOND` are equivalent. Full mapping + rewrites: `references/snowflake/09-functions.md` → DATEDIFF / TIMESTAMPDIFF drift.\n\n### Integer division — type drift\n\nBoth dialects promote integer division (no integer truncation), but to different types:\n\n- Snowflake: `5 / 2` → `NUMBER(7, 6) = 2.500000` (exact decimal; scale is always 6 for integer operands, precision = leading digits + 6)\n- DBSQL: `5 / 2` → `DOUBLE = 2.5` (floating point)\n\nValues are equivalent for typical inputs; the difference is the result type. Watch for downstream `CAST` chains, schema declarations, and equality comparisons that assume a `DECIMAL` result.\n\n```sql\n-- Snowflake: NUMBER(7,6) = 2.500000\nSELECT 5 / 2;\n\n-- DBSQL: DOUBLE = 2.5 (same value, different type)\nSELECT 5 / 2;\n\n-- DBSQL: cast both operands to DECIMAL to get a DECIMAL result (DECIMAL / INT returns DOUBLE)\nSELECT CAST(5 AS DECIMAL(38,6)) / CAST(2 AS DECIMAL(38,6));   -- 2.500000\n```\n\nScan every `/` division in the source. If operands are DECIMAL/NUMBER columns, no change is needed (DBSQL DECIMAL / DECIMAL is DECIMAL). If operands are INT columns and the source relied on a DECIMAL/NUMBER result type (e.g., for schema compatibility or exact decimal arithmetic), cast both operands to `DECIMAL`.\n\n### DATE arithmetic — `DATE \xb1 INT` and `DATE \xb1 INTERVAL` (CRITICAL)\n\nSnowflake permits `<date> - <int>` and `<date> + <int>` directly, treating the integer as days (so `current_date - 2` returns a DATE two days ago). DBSQL does **not**: `<date> - <int>` is invalid — the `-` operator requires the right operand to be a DATE/TIMESTAMP or INTERVAL, never a plain integer.\n\nThe two valid DBSQL replacements differ in return type:\n\n| DBSQL form                     | Return type   | Use when                              |\n| ------------------------------ | ------------- | ------------------------------------- |\n| `DATE_SUB(<date>, <int>)`      | **DATE**      | You want to preserve the DATE type    |\n| `DATEADD(DAY, -<int>, <date>)` | **TIMESTAMP** | You're already going to compare as TS |\n| `<date> - INTERVAL <int> DAY`  | **TIMESTAMP** | Same — interval arithmetic returns TS |\n\n```sql\n-- Snowflake (returns DATE)\nSELECT current_date - 2;\nDELETE FROM t WHERE dt < current_date - 180;\n\n-- DBSQL — preserve DATE type with DATE_SUB\nSELECT DATE_SUB(current_date, 2);                 -- DATE\nDELETE FROM my_catalog.my_schema.t\n  WHERE dt < DATE_SUB(current_date, 180);\n\n-- DBSQL — INTERVAL form (returns TIMESTAMP, watch downstream comparisons)\nSELECT current_date - INTERVAL 2 DAY;             -- TIMESTAMP — NOT DATE\nSELECT CAST(current_date - INTERVAL 2 DAY AS DATE);  -- explicit cast back to DATE\n```\n\n**Silent risk:** if you translate `current_date - 2` to `current_date - INTERVAL 2 DAY` (or `DATEADD(DAY, -2, current_date)`) and then compare it to a DATE column, DBSQL implicitly promotes the DATE to TIMESTAMP for comparison. Filtering and join semantics can shift; precision-sensitive predicates (`dt = current_date - 2`) may return different rows. **Default rule: use `DATE_SUB(date, n)` to keep the result DATE-typed**, unless you specifically need TIMESTAMP semantics.\n\nThe same rule applies inside `DECLARE OR REPLACE VARIABLE name DATE = …;` — if you store `current_date - INTERVAL 2 DAY` (or `DATEADD(...)`) as the default, the expression is TIMESTAMP-typed and the `DEFAULT` may fail to match the declared `DATE` type. Use `DATE_SUB` in the default expression.\n\nFor variable-INT subtraction (e.g. `start_date - ttl` where `ttl` is `INT`), only `DATE_SUB(start_date, ttl)` works — the INTERVAL form requires a literal integer (`start_date - INTERVAL ttl DAY` is INVALID), and `DATEADD` would still return TIMESTAMP.\n\n### BIGINT / INT / INTEGER are NUMBER(38,0) aliases in Snowflake (CRITICAL)\n\nSnowflake `BIGINT`, `INT`, `INTEGER`, `SMALLINT`, `TINYINT`, `BYTEINT` are **all aliases for `NUMBER(38,0)`** — not 1/2/4/8-byte signed integers. A Snowflake `BIGINT` column can hold values up to 10^38.\n\nDBSQL `BIGINT` is a true 64-bit signed integer (max 9,223,372,036,854,775,807). Migrating a Snowflake `BIGINT` column to DBSQL `BIGINT` silently overflows for values > 2^63−1.\n\n**Safe target**: `DECIMAL(38,0)` preserves the full Snowflake range. Use narrower DBSQL integer types only when values are known to fit.\n\nSee `references/snowflake/02-data-types.md` for scalar type mapping (and `references/snowflake/03-semi-structured.md` for `VARIANT` / `OBJECT` / `ARRAY`).\n\n### GREATEST / LEAST — NULL propagation\n\nSnowflake `GREATEST(a, NULL)` returns **NULL** (NULL-propagating). DBSQL `GREATEST(a, NULL)` returns **`a`** (ignores NULLs).\n\n```sql\n-- Snowflake: returns NULL\nSELECT GREATEST(10, NULL, 5);\n\n-- DBSQL: returns 10\nSELECT GREATEST(10, NULL, 5);\n\n-- DBSQL: match Snowflake NULL-propagating behavior\nSELECT CASE WHEN 10 IS NULL OR NULL IS NULL OR 5 IS NULL THEN NULL\n            ELSE GREATEST(10, NULL, 5) END;\n```\n\nIf the source relied on NULL propagation (e.g. detect \"any-NULL\" downstream), flag and rewrite with an explicit null-check wrapper. Same applies to `LEAST`.\n\n### LISTAGG — `WITHIN GROUP (ORDER BY ...)` — supported on DBR 16.4+\n\nSnowflake: `LISTAGG(col, ',') WITHIN GROUP (ORDER BY other_col)` produces an ordered concatenation.\n\nDBSQL `LISTAGG` (DBR 16.4+) supports `WITHIN GROUP (ORDER BY ...)` — direct substitution.\n\n```sql\n-- Snowflake\nSELECT LISTAGG(name, ',') WITHIN GROUP (ORDER BY priority DESC) FROM tasks;\n\n-- DBSQL (DBR 16.4+) — direct substitution\nSELECT LISTAGG(name, ',') WITHIN GROUP (ORDER BY priority DESC)\nFROM my_catalog.my_schema.tasks;\n```\n\nOn older DBR versions, build a sorted array first and then join:\n\n```sql\n-- DBSQL (pre-16.4) — preserves ordering\nSELECT ARRAY_JOIN(\n  TRANSFORM(\n    ARRAY_SORT(COLLECT_LIST(STRUCT(priority, name)),\n               (a, b) -> CASE WHEN a.priority > b.priority THEN -1\n                              WHEN a.priority < b.priority THEN 1\n                              ELSE 0 END),\n    x -> x.name\n  ),\n  ','\n) FROM my_catalog.my_schema.tasks;\n\n-- DBSQL (when order does not matter — simplest)\nSELECT ARRAY_JOIN(COLLECT_LIST(name), ',') FROM my_catalog.my_schema.tasks;\n```\n\n### DIV0 / DIV0NULL / NULLIFZERO / ZEROIFNULL\n\nSnowflake-specific safe-division and NULL-coalescing helpers. Mappings:\n\n| Snowflake                                                      | DBSQL                                                                        |\n| -------------------------------------------------------------- | ---------------------------------------------------------------------------- |\n| `DIV0(a, b)` — returns **0** if `b = 0`                        | `IF(b = 0, 0, a / b)` (`try_divide` returns **NULL** not 0 — not equivalent) |\n| `DIV0NULL(a, b)` — returns **0** if `b = 0` **or** `b IS NULL` | `IF(b IS NULL OR b = 0, 0, a / b)` or `COALESCE(try_divide(a, b), 0)`        |\n| `NULLIFZERO(x)`                                                | `NULLIF(x, 0)`                                                               |\n| `ZEROIFNULL(x)`                                                | `COALESCE(x, 0)`                                                             |\n\n**Silent drift**: `try_divide` substituted for either `DIV0` or `DIV0NULL` produces NULL instead of 0 — different downstream aggregation results.\n\n### IFF / DECODE / NVL / NVL2 / IFNULL / EQUAL_NULL\n\n| Snowflake                               | DBSQL                                                            | Notes                                            |\n| --------------------------------------- | ---------------------------------------------------------------- | ------------------------------------------------ |\n| `IFF(cond, t, f)`                       | `IF(cond, t, f)` or `IFF(cond, t, f)`                            | DBSQL supports both                              |\n| `DECODE(expr, v1, r1, v2, r2, default)` | `CASE expr WHEN v1 THEN r1 WHEN v2 THEN r2 ELSE default END`     | DBSQL has `DECODE` but prefer `CASE` for clarity |\n| `NVL(a, b)`                             | `COALESCE(a, b)` or `NVL(a, b)`                                  | Same behavior                                    |\n| `NVL2(a, b, c)`                         | `NVL2(a, b, c)`                                                  | Supported natively in DBSQL — safe substitution  |\n| `IFNULL(a, b)`                          | `IFNULL(a, b)` or `COALESCE(a, b)`                               | Same                                             |\n| `EQUAL_NULL(a, b)`                      | `a IS NOT DISTINCT FROM b` or `equal_null(a, b)` (DBR 11.3 LTS+) | NULL-safe equality                               |\n\n### TO_DATE / TO_TIMESTAMP format strings (silent wrong-output risk)\n\nSnowflake uses Oracle-style format tokens with `MI` for minutes. DBSQL uses Java `DateTimeFormatter` patterns with lowercase `mm` for minutes — and `MM` means **month** in DBSQL.\n\n```sql\n-- Snowflake: parses year-month-day hour:minute:second\nSELECT TO_TIMESTAMP('2024-03-20 14:35:00', 'YYYY-MM-DD HH24:MI:SS');\n\n-- DBSQL (WRONG — MI is not a recognized token; HH24 is not either)\nSELECT TO_TIMESTAMP('2024-03-20 14:35:00', 'YYYY-MM-DD HH24:MI:SS');\n\n-- DBSQL (CORRECT)\nSELECT TO_TIMESTAMP('2024-03-20 14:35:00', 'yyyy-MM-dd HH:mm:ss');\n```\n\nThe most dangerous silent mistake is using `MM` for minutes in DBSQL (it's actually month) — this produces plausible-looking wrong timestamps. See `references/snowflake/09-functions.md` for the full token mapping.\n\n### CONCAT / `||` — NULL handling\n\nSnowflake `CONCAT(a, b)` and `a || b` **propagate NULL** — any NULL input makes the result NULL. DBSQL has the same behavior — safe substitution.\n\n**`CONCAT_WS` — silent NULL drift (Snowflake-specific behavior)**\n\nSnowflake `CONCAT_WS(sep, a, b, ...)` **propagates NULL** — returns NULL if any argument (separator or value) is NULL. DBSQL `CONCAT_WS(sep, a, b, ...)` **skips NULL values**. This is a silent wrong-result bug when migrating.\n\n```sql\n-- Snowflake: returns NULL\nSELECT CONCAT_WS('-', 'a', NULL, 'b');\n\n-- DBSQL: returns 'a-b'\nSELECT CONCAT_WS('-', 'a', NULL, 'b');\n```\n\nTo preserve Snowflake semantics in DBSQL, wrap with an explicit null-check on the arguments.\n\nBare `CONCAT` and `||` — both dialects propagate NULL. No drift.\n\n### SPLIT — delimiter semantics differ\n\n| Snowflake                                               | DBSQL                                                  |\n| ------------------------------------------------------- | ------------------------------------------------------ |\n| `SPLIT(str, delim)` — delimiter is a **literal string** | `SPLIT(str, delim)` — delimiter is a **regex pattern** |\n\n```sql\n-- Snowflake: splits on the literal \".\"\nSELECT SPLIT('a.b.c', '.');   -- ['a','b','c']\n\n-- DBSQL (WRONG — '.' is regex \"any char\")\nSELECT SPLIT('a.b.c', '.');   -- ['','','','','']\n\n-- DBSQL (CORRECT — escape the regex metachar)\nSELECT SPLIT('a.b.c', '\\\\.');  -- ['a','b','c']\n```\n\nScan every Snowflake `SPLIT` call and escape regex metacharacters (`.`, `|`, `(`, `)`, `[`, `]`, `*`, `+`, `?`, `\\`, `^`, `$`, `{`, `}`) in the delimiter.\n\n### MINUS / EXCEPT\n\nBoth Snowflake and DBSQL accept `MINUS` and `EXCEPT` as set-difference operators (synonyms in both). Safe to leave unchanged.\n\n```sql\n-- Both dialects\nSELECT id FROM a MINUS SELECT id FROM b;\nSELECT id FROM a EXCEPT SELECT id FROM b;\n```\n\n### Case sensitivity in string comparisons\n\nBoth Snowflake and DBSQL are **case-sensitive by default** in equality and LIKE. If the Snowflake source uses `COLLATE 'en-ci'` or the `ILIKE` operator for case-insensitive matching:\n\n- `ILIKE` — DBSQL supports `ILIKE` directly. Safe substitution.\n- `COLLATE 'en-ci'` / `COLLATE 'en-ci-ai'` — DBSQL supports equivalent collations with different spelling (underscores, not hyphens): `COLLATE EN_CI` (case-insensitive), `COLLATE EN_CI_AI` (case- and accent-insensitive), `COLLATE UNICODE_CI_AI` (locale-neutral). This is a syntax remap, not a capability gap.\n\n```sql\n-- Snowflake\nWHERE name COLLATE 'en-ci' = 'john'\nWHERE name COLLATE 'en-ci-ai' = 'jos\xe9'\nWHERE name ILIKE 'jo%'\n\n-- DBSQL (underscore spelling)\nWHERE name COLLATE EN_CI = 'john'\nWHERE name COLLATE EN_CI_AI = 'jos\xe9'\nWHERE name ILIKE 'jo%'\n```\n\n> For full DBSQL collation syntax and session-level collation settings, load `skills/writing-sql/collation.md`.\n\n### UUID_STRING vs UUID\n\nSnowflake `UUID_STRING()` generates a UUID v4 by default; optional v5 with namespace/name args. DBSQL `UUID()` generates UUID v4 only.\n\n```sql\n-- Snowflake\nSELECT UUID_STRING();\n\n-- DBSQL\nSELECT UUID();\n```\n\nIf the source uses `UUID_STRING(namespace, name)` (v5, deterministic) — DBSQL has no UUID v5 generator. Substituting `SHA2(CONCAT(namespace, name), 256)` produces a stable hash but the bytes are not RFC 4122 v5; downstream consumers that expect deterministic UUIDs will see different values from Snowflake. Treat as a TODO unless the caller explicitly accepts new identifier values:\n\n```sql\n-- TODO: manual review required — UUID_STRING(namespace, name) is UUID v5; DBSQL has no v5 generator. Choose SHA2-truncated (values diverge from Snowflake) or a Python UDF that emits RFC 4122 v5.\n```\n\n## Safe functions — no translation needed\n\nIdentical syntax and semantics in both dialects:\n\n`SUBSTR` / `SUBSTRING`, `LEFT`, `RIGHT`, `TRIM`, `LTRIM`, `RTRIM`, `REPLACE`, `REVERSE`, `UPPER`, `LOWER`, `INITCAP`, `ABS`, `FLOOR`, `CEIL` / `CEILING`, `ROUND`, `SQRT`, `POWER`, `MOD`, `SIGN`, `SUM`, `AVG`, `MIN`, `MAX`, `COUNT`, `COUNT_IF`, `QUALIFY`, `ILIKE`, `RLIKE`, `CURRENT_TIMESTAMP`, `CURRENT_DATE`, `RANK`, `DENSE_RANK`, `ROW_NUMBER`, `LEAD`, `LAG`, `FIRST_VALUE`, `LAST_VALUE`, `NTILE`, `PERCENT_RANK`, `CUME_DIST`, `UNION`, `EXCEPT`, `INTERSECT`.\n\n---\n\n## Construct Routing Table\n\nDuring the Plan step, scan the live source SQL for these patterns. Load the matching reference files via `readSkillFile` using the full paths shown. When adding a new reference file, update this table and `references/00-index.md` (human reference).\n\n| Pattern in source SQL                                                                                                                                                                                                                                           | Reference file (full path for `readSkillFile`)                                        |\n| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |\n| `CREATE TABLE`, `ALTER TABLE`, constraints, `IDENTITY` / `AUTOINCREMENT`, `SEQUENCE`, `TRANSIENT` / `TEMPORARY` tables, external tables + stages, `COMMENT`, `CLUSTER BY`, materialized views, secure views, row access / masking policies, `CREATE TABLE LIKE` | `skills/migration-code-conversion/references/snowflake/01-objects-and-ddl.md`         |\n| Scalar type declarations — `NUMBER(p,s)`, `TIMESTAMP_NTZ` / `LTZ` / `TZ`, `CHAR(N)`, `CAST`, `TRY_CAST`, `TO_NUMBER`, `TO_DATE`, `TO_TIMESTAMP`, `BOOLEAN` literals; integer / float / decimal precision drift                                                  | `skills/migration-code-conversion/references/snowflake/02-data-types.md`              |\n| `VARIANT`, `OBJECT`, `ARRAY`, `OBJECT_CONSTRUCT`, `ARRAY_CONSTRUCT`, `FLATTEN`, `:` / `::` path access, `TO_VARIANT`, `TO_OBJECT`, `TO_ARRAY`, `PARSE_JSON`, `TO_JSON`                                                                                          | `skills/migration-code-conversion/references/snowflake/03-semi-structured.md`         |\n| `QUALIFY`, `PIVOT` / `UNPIVOT`, `LISTAGG`, `MATCH_RECOGNIZE`, `SAMPLE`, `UPDATE ... FROM`, `DELETE USING`, `MERGE` multi-action, `INFORMATION_SCHEMA`, recursive CTEs, transactions (`BEGIN` / `COMMIT` / `ROLLBACK`), `SAVEPOINT`, `FOR UPDATE`                | `skills/migration-code-conversion/references/snowflake/04-select-and-dml.md`          |\n| Stored procedures (Snowflake Scripting / JavaScript / Python / Java / Scala), `LANGUAGE` clauses, UDFs, `CALL`, `EXECUTE AS`, session variables                                                                                                                 | `skills/migration-code-conversion/references/snowflake/05a-scripting-procs-udfs.md`   |\n| Cursors, `RESULTSET`, `EXCEPTION` handlers, `IF` / `ELSEIF` / `ELSE`, `FOR` / `WHILE` / `REPEAT` / `LOOP`, `EXECUTE IMMEDIATE`, `SYSTEM$*`                                                                                                                      | `skills/migration-code-conversion/references/snowflake/05b-scripting-control-flow.md` |\n| Time Travel queries (`AT` / `BEFORE`), retention (`DATA_RETENTION_TIME_IN_DAYS`), `CLONE`, `UNDROP`, Fail-safe                                                                                                                                                  | `skills/migration-code-conversion/references/snowflake/06-time-travel-and-clones.md`  |\n| Snowpipe, `STREAM`, `TASK`, `COPY INTO`, `CREATE STAGE`, dynamic tables                                                                                                                                                                                         | `skills/migration-code-conversion/references/snowflake/07-pipelines.md`               |\n| Clustering keys, search optimization service, materialized views, warehouse sizing, `QUERY_TAG`, `RESULT_SCAN`, `OPTIMIZE` / `VACUUM`                                                                                                                           | `skills/migration-code-conversion/references/snowflake/08-performance.md`             |\n| Date / time functions (`DATEADD`, `DATEDIFF`, `DATE_TRUNC`, `TO_DATE`, `TO_TIMESTAMP`, `CONVERT_TIMEZONE`, `MONTHNAME`, `DAYNAME`, `LAST_DAY`, `EXTRACT`), format strings, scalar function mappings                                                             | `skills/migration-code-conversion/references/snowflake/09-functions.md`               |\n| Identifier case, `$session_var` / `:bind`, `DIV0` / `DIV0NULL` / `NULLIFZERO` / `ZEROIFNULL`, `EQUAL_NULL`, `GREATEST` / `LEAST`, LISTAGG ordering, integer division, `UUID_STRING`, `SPLIT` regex semantics                                                    | Covered in this file — no additional load needed                                      |\n\n**Dynamic SQL — procedure is untranslatable**: If a procedure declares a string variable, builds it across `IF` / `WHILE` branches, and executes it via `EXECUTE IMMEDIATE` — the procedure cannot be mechanically translated. Do not load reference files. Do not attempt to translate the variable-building scaffolding piecewise. Output a single TODO:\n\n```sql\n-- TODO: procedure builds and executes dynamic SQL — manual rewrite required\n```\n\n---\n\n## Large-file Chunking\n\nSnowflake has no explicit batch separator. When the source exceeds 300 lines, run `grepAsset` with this pattern to locate procedure/function definition boundaries:\n\n```\nCREATE\\s+(?:OR\\s+REPLACE\\s+)?(?:PROCEDURE|FUNCTION)\\s\n```\n\nUse each match as the start of an independent unit and process one procedure/function at a time. If no matches are found, process as a single unit.\n\n---\n\n## TODO Reference\n\n### Constructs that cannot be translated (valid TODO)\n\n| Snowflake construct                                                                  | Reason                                                                                       |\n| ------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------- |\n| `MATCH_RECOGNIZE(...)`                                                               | Row-pattern matching — no DBSQL equivalent                                                   |\n| `LANGUAGE JAVASCRIPT` procedure bodies                                               | No JavaScript runtime in DBSQL — rewrite in SQL or Python                                    |\n| `LANGUAGE JAVA` / `LANGUAGE SCALA` procedures with complex logic                     | Manual rewrite required; Databricks supports JAR-registered UDFs but procedure shape differs |\n| External functions (API-based, `CREATE EXTERNAL FUNCTION ... API_INTEGRATION = ...`) | Rewrite as a Databricks notebook / Python UDF calling the external API                       |\n| `RESULT_SCAN(LAST_QUERY_ID())`                                                       | No DBSQL equivalent — re-run the query                                                       |\n| `SYSTEM$<admin_fn>` (e.g. `SYSTEM$WAIT`, `SYSTEM$PIPE_STATUS`)                       | Most admin functions have no SQL-level equivalent                                            |\n| `CREATE SHARE` / `CREATE ACCOUNT` / data sharing DDL                                 | Use Delta Sharing separately (see platform docs)                                             |\n| Snowflake Cortex functions (`SNOWFLAKE.CORTEX.*`)                                    | Use Databricks Foundation Model APIs / `ai_query` separately                                 |\n| Snowpark DataFrame API code                                                          | Rewrite as PySpark DataFrame API (not SQL migration)                                         |\n\n### Constructs that ARE supported (not valid TODO reasons)\n\n| Snowflake construct                          | DBSQL equivalent                        | Reference file                               |\n| -------------------------------------------- | --------------------------------------- | -------------------------------------------- |\n| Stored procedures (Snowflake Scripting, SQL) | DBSQL SQL procedures                    | `05a-scripting-procs-udfs.md`                |\n| Cursors                                      | DBSQL cursors (SQL warehouse)           | `05b-scripting-control-flow.md`              |\n| `EXCEPTION` handlers                         | `DECLARE EXIT HANDLER FOR SQLEXCEPTION` | `05b-scripting-control-flow.md`              |\n| `MERGE` multi-action                         | DBSQL MERGE (DBR 13.0+)                 | `04-select-and-dml.md`                       |\n| Recursive CTEs                               | `WITH RECURSIVE` (keyword required)     | `04-select-and-dml.md`                       |\n| `QUALIFY`                                    | `QUALIFY` (DBR 10.4 LTS+)               | `04-select-and-dml.md`                       |\n| `PIVOT` / `UNPIVOT`                          | Supported                               | `04-select-and-dml.md`                       |\n| `FLATTEN`                                    | `LATERAL VIEW EXPLODE` / `explode()`    | `03-semi-structured.md`                      |\n| `VARIANT` semi-structured access             | DBSQL VARIANT (DBR 15.3+)               | `03-semi-structured.md`                      |\n| Clustering keys                              | Liquid Clustering (`CLUSTER BY`)        | `01-objects-and-ddl.md`, `08-performance.md` |\n| Sequences                                    | `CREATE SEQUENCE`                       | `01-objects-and-ddl.md`                      |\n| Time travel (`AT(TIMESTAMP => ...)`)         | `TIMESTAMP AS OF` / `VERSION AS OF`     | `06-time-travel-and-clones.md`               |\n\n---\n\n## Validation Checklist\n\nAction table moved to `references/snowflake/00-validation-patterns.md` — load that file via `readSkillFile` during the Validate step. It contains both the combined `grepAsset` regex and the pattern → action rows.\n"}}]);
//# sourceMappingURL=https://sourcemaps.dev.databricks.com/monolith/static/js/91509.26f665f9c5.chunk.js.map