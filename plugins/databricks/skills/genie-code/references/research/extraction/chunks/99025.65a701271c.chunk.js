"use strict";(self.webpackChunkdatabricks_monolith=self.webpackChunkdatabricks_monolith||[]).push([[99025],{321406:e=>{e.exports="# SQL Function Catalog\n\nMaster catalog of Oracle SQL function → Databricks SQL mappings. Covers format models, date/time, numeric, aggregate, window, statistical, JSON, conversion, hash, string, identifier/environment, and collection functions.\n\nThe Oracle SQL Reference defines ~318 built-in SQL functions across single-row, aggregate, analytic, model, object-reference, and OLAP categories. The high-traffic patterns (NULL handling overload semantics, hierarchical, Flashback, XML, `MATCH_RECOGNIZE`) are covered in the topic files referenced below; this file catches everything else, including the long tail and Oracle-only feature areas with no equivalent.\n\n## ORACLE\n\n> Also load when relevant:\n>\n> - `00-quirks.md` — `NVL`, `NVL2`, `COALESCE`, `NULLIF`, `LNNVL`, `DECODE`, `INSTR`, `SUBSTR`, `MOD`, `TRUNC` overload semantics, \"Safe functions\" list.\n> - `02-select-dml.md` — XML construction (`XMLELEMENT`/`XMLAGG`/`XMLFOREST`/`XMLATTRIBUTES`/`XMLCONCAT`), `MATCH_RECOGNIZE`, Flashback queries, hierarchical pseudo-columns (`LEVEL`, `CONNECT_BY_ROOT`, `SYS_CONNECT_BY_PATH`).\n> - `03-data-types.md` — `CAST`, decimal arithmetic precision, IEEE 754 drift, `COLLATE` clause.\n\n### Format model translation (`TO_DATE` / `TO_CHAR` / `TO_TIMESTAMP`)\n\nOracle format models differ from DBSQL's Java-style patterns. Translate every component:\n\n| Oracle                | DBSQL            | Meaning                                                         |\n| --------------------- | ---------------- | --------------------------------------------------------------- |\n| `YYYY`                | `yyyy`           | 4-digit year                                                    |\n| `YY`                  | `yy`             | 2-digit year                                                    |\n| `RRRR`                | `yyyy`           | Round-year (Oracle century rollover rules lost — verify intent) |\n| `MM`                  | `MM`             | 2-digit month **(case-sensitive in DBSQL: `MM` = month)**       |\n| `MON`                 | `MMM`            | Short month name (`JAN`)                                        |\n| `MONTH`               | `MMMM`           | Full month name (`JANUARY`)                                     |\n| `DD`                  | `dd`             | **2-digit day — `dd`, not `DD`** (DBSQL is case-sensitive)      |\n| `DDD`                 | `DDD`            | Day of year (1–366)                                             |\n| `DY`                  | `EEE`            | Short day name (`MON`)                                          |\n| `DAY`                 | `EEEE`           | Full day name (`MONDAY`)                                        |\n| `D`                   | `u` or `e`       | Day of week number — see note below                             |\n| `HH24`                | `HH`             | 24-hour                                                         |\n| `HH` / `HH12`         | `hh`             | 12-hour                                                         |\n| `MI`                  | `mm`             | **Minutes — `mm`, not `MM`** (DBSQL is case-sensitive)          |\n| `SS`                  | `ss`             | Seconds                                                         |\n| `FF` / `FF3` / `FF6`  | `SSS` / `SSSSSS` | Fractional seconds. DBSQL: 3 digits = millis, 6 = micros        |\n| `AM` / `PM`           | `a`              | AM/PM marker                                                    |\n| `TZH` / `TZM` / `TZR` | `XXX` / `ZZZZZ`  | Timezone — approximation                                        |\n\n**Pattern case-sensitivity is the #1 migration gotcha**: DBSQL uses `MM` for month and `mm` for minutes. Oracle uses `MM` for month and `MI` for minutes. A mechanical find-replace of the format string that keeps `MM` and maps `MI → mm` will silently flip to minutes-where-month-was-expected when the Oracle format had both components. Always translate the full format string component-by-component.\n\n```sql\n-- Oracle\nTO_DATE('2024-03-20 14:30:45', 'YYYY-MM-DD HH24:MI:SS')\nTO_CHAR(SYSDATE, 'YYYY-MM-DD HH24:MI:SS')\nTO_CHAR(SYSDATE, 'DD-MON-YYYY')\n\n-- DBSQL\nTO_DATE('2024-03-20 14:30:45', 'yyyy-MM-dd HH:mm:ss')\nto_char(CURRENT_TIMESTAMP, 'yyyy-MM-dd HH:mm:ss')   -- DBR 14.1+\nto_char(CURRENT_TIMESTAMP, 'dd-MMM-yyyy')           -- DBR 14.1+\n-- Equivalent (any DBR):\nDATE_FORMAT(CURRENT_TIMESTAMP, 'yyyy-MM-dd HH:mm:ss')\nDATE_FORMAT(CURRENT_TIMESTAMP, 'dd-MMM-yyyy')\n```\n\n**Parsing vs rendering — both `TO_CHAR` and `DATE_FORMAT` work for rendering dates in DBSQL.** DBSQL's `to_char(date_or_timestamp, fmt)` is a native date-formatting function as of **DBR 14.1+** (per the [DBSQL `to_char` docs](https://docs.databricks.com/sql/language-manual/functions/to_char.html)) and accepts the same DBSQL date format pattern as `DATE_FORMAT`. Use whichever reads better — `TO_CHAR` keeps source-side parity with Oracle, `DATE_FORMAT` is the historic Spark name. For parsing strings into dates, use `TO_DATE(str, fmt)` or `TO_TIMESTAMP(str, fmt)` — `TO_CHAR` is render-only in DBSQL (it does not parse strings to dates the way Oracle's overloaded `TO_CHAR` does in some implicit contexts).\n\n> **Pre-DBR 14.1 only:** if the target runtime is older than 14.1, fall back to `DATE_FORMAT` for rendering — `TO_CHAR` over a date will not resolve there.\n\n**`D` (day-of-week number) — locale-dependent**: Oracle `D` depends on `NLS_TERRITORY` (1 = Sunday in US, 1 = Monday in many other territories). DBSQL `DAYOFWEEK(date)` returns 1 = Sunday. If the source uses `D`, verify the Oracle `NLS_TERRITORY` and add an explicit offset if needed. Use `u` (1=Mon..7=Sun) or `e` (locale-specific) in DBSQL `DATE_FORMAT` patterns when rendering; use the `DAYOFWEEK`/`WEEKDAY` functions when computing.\n\n**Text output differences**: Oracle `MON` / `MONTH` / `DAY` render uppercase by default (controlled by `NLS_DATE_LANGUAGE`). DBSQL `MMM` / `MMMM` / `EEEE` render title-case (`Jan`, `January`, `Monday`). If uppercase matches the source, wrap with `UPPER(...)`:\n\n```sql\nUPPER(DATE_FORMAT(dt, 'MMM'))  -- 'JAN' to match Oracle TO_CHAR(dt, 'MON')\n```\n\n### Date and time function mapping\n\n| Oracle                                      | DBSQL                                                                                | Notes                                                                                                                                                                                                            |\n| ------------------------------------------- | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `SYSDATE`                                   | `CURRENT_TIMESTAMP`                                                                  | **Type change**: Oracle `SYSDATE` returns `DATE` (which includes time to seconds); DBSQL `CURRENT_TIMESTAMP` returns `TIMESTAMP` (microsecond precision). When used for point-in-time, the mapping is safe.      |\n| `SYSTIMESTAMP`                              | `CURRENT_TIMESTAMP`                                                                  | Oracle `SYSTIMESTAMP` includes timezone; DBSQL `CURRENT_TIMESTAMP` does not preserve per-value offset.                                                                                                           |\n| `CURRENT_DATE`                              | `CURRENT_DATE`                                                                       | Both return date-only (DBSQL) or date + time (Oracle session-zone `DATE`). If time is used, migrate to `CURRENT_TIMESTAMP`.                                                                                      |\n| `CURRENT_TIMESTAMP`                         | `CURRENT_TIMESTAMP`                                                                  | Direct.                                                                                                                                                                                                          |\n| `LOCALTIMESTAMP`                            | `CURRENT_TIMESTAMP`                                                                  | Both return session-local timestamp.                                                                                                                                                                             |\n| `SYSDATE + n` (add days)                    | `CURRENT_TIMESTAMP + INTERVAL n DAYS`                                                | **Oracle date arithmetic uses days as the unit** — adding 1 = one day, adding 1/24 = one hour. DBSQL requires explicit `INTERVAL`. Fractional day additions must be expressed as explicit hour/minute intervals. |\n| `date + 1/24`                               | `date + INTERVAL 1 HOUR`                                                             | Oracle idiom for \"plus one hour\". Translate fractional-day arithmetic to explicit intervals.                                                                                                                     |\n| `date1 - date2` (returns days as NUMBER)    | `DATEDIFF(date1, date2)`                                                             | Oracle returns a `NUMBER` of days with fractional part (for `DATE` subtraction). DBSQL `DATEDIFF` returns integer days. For fractional days use `(unix_timestamp(date1) - unix_timestamp(date2)) / 86400.0`.     |\n| `ADD_MONTHS(date, n)`                       | `ADD_MONTHS(date, n)`                                                                | Direct.                                                                                                                                                                                                          |\n| `MONTHS_BETWEEN(d1, d2)`                    | `MONTHS_BETWEEN(d1, d2)`                                                             | Direct — both return fractional months.                                                                                                                                                                          |\n| `LAST_DAY(date)`                            | `LAST_DAY(date)`                                                                     | Direct.                                                                                                                                                                                                          |\n| `NEXT_DAY(date, 'dayname')`                 | `NEXT_DAY(date, 'dayname')`                                                          | Direct. Day names must match DBSQL expectations (`'SUNDAY'`, `'MONDAY'`, etc.).                                                                                                                                  |\n| `TRUNC(date)`                               | `DATE_TRUNC('DAY', date)` or `DATE(date)`                                            | Oracle `TRUNC(date)` without format arg truncates to midnight. DBSQL `DATE_TRUNC('DAY', date)` is the direct equivalent.                                                                                         |\n| `TRUNC(date, 'MM')`                         | `DATE_TRUNC('MONTH', date)`                                                          | Month start.                                                                                                                                                                                                     |\n| `TRUNC(date, 'YY')` / `TRUNC(date, 'YYYY')` | `DATE_TRUNC('YEAR', date)`                                                           | Year start.                                                                                                                                                                                                      |\n| `TRUNC(date, 'DY')` / `TRUNC(date, 'IW')`   | `DATE_TRUNC('WEEK', date)`                                                           | Week start. Oracle `DY` = locale week start, `IW` = ISO week start (Monday). DBSQL `'WEEK'` is ISO — matches `IW` exactly, may drift for `DY` depending on `NLS_TERRITORY`.                                      |\n| `TRUNC(date, 'Q')`                          | `DATE_TRUNC('QUARTER', date)`                                                        | Quarter start.                                                                                                                                                                                                   |\n| `TRUNC(date, 'HH')`                         | `DATE_TRUNC('HOUR', date)`                                                           | Hour truncation.                                                                                                                                                                                                 |\n| `TRUNC(date, 'MI')`                         | `DATE_TRUNC('MINUTE', date)`                                                         | Minute truncation.                                                                                                                                                                                               |\n| `ROUND(date, 'MM')`                         | `DATE_TRUNC('MONTH', ADD_MONTHS(date, CASE WHEN DAY(date) >= 16 THEN 1 ELSE 0 END))` | Rounds to nearest month boundary — no direct DBSQL equivalent. Manual CASE required.                                                                                                                             |\n| `EXTRACT(YEAR FROM date)`                   | `EXTRACT(YEAR FROM date)` or `YEAR(date)`                                            | Direct.                                                                                                                                                                                                          |\n| `EXTRACT(MONTH FROM date)`                  | `MONTH(date)`                                                                        | Direct.                                                                                                                                                                                                          |\n| `EXTRACT(DAY FROM date)`                    | `DAY(date)` or `DAYOFMONTH(date)`                                                    | Direct.                                                                                                                                                                                                          |\n| `EXTRACT(HOUR FROM ts)`                     | `HOUR(ts)` or `EXTRACT(HOUR FROM ts)`                                                | Works only on `TIMESTAMP`, not `DATE` in DBSQL — cast first if needed.                                                                                                                                           |\n| `EXTRACT(MINUTE FROM ts)`                   | `MINUTE(ts)`                                                                         |                                                                                                                                                                                                                  |\n| `EXTRACT(SECOND FROM ts)`                   | `SECOND(ts)`                                                                         |                                                                                                                                                                                                                  |\n| `EXTRACT(TIMEZONE_HOUR FROM ts)`            | No direct equivalent                                                                 | DBSQL `TIMESTAMP` has no per-value timezone — emit warning comment.                                                                                                                                              |\n| `NUMTODSINTERVAL(n, 'unit')`                | `INTERVAL n unit`                                                                    | e.g. `NUMTODSINTERVAL(5, 'HOUR')` → `INTERVAL 5 HOURS`.                                                                                                                                                          |\n| `NUMTOYMINTERVAL(n, 'unit')`                | `INTERVAL n unit`                                                                    | e.g. `NUMTOYMINTERVAL(2, 'YEAR')` → `INTERVAL 2 YEARS`.                                                                                                                                                          |\n\n### Date and time — timezone helpers\n\n| Oracle                                                 | DBSQL                                                        | Notes                                                                                                                                            |\n| ------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `DBTIMEZONE`                                           | `current_timezone()`                                         | DBSQL returns the cluster session timezone (a string like `\"UTC\"` or `\"America/New_York\"`). Oracle returns DB-level TZ — usually equal.          |\n| `SESSIONTIMEZONE`                                      | `current_timezone()`                                         | Same — DBSQL has only one timezone abstraction.                                                                                                  |\n| `TZ_OFFSET('region')`                                  | No direct equivalent                                         | Resolve at app layer or with a Python UDF over `pytz`. Hardcode if the source uses a fixed region.                                               |\n| `SYS_EXTRACT_UTC(ts_with_tz)`                          | `to_utc_timestamp(ts, current_timezone())`                   | Drift: Oracle `TIMESTAMP WITH TIME ZONE` carries a per-value offset; DBSQL `TIMESTAMP` does not — the cast assumes the session TZ.               |\n| `FROM_TZ(ts, 'region')`                                | `from_utc_timestamp(ts, 'region')`                           | Approximation only — semantics differ. Oracle attaches a per-value timezone; DBSQL has no per-value timezone. See `03-data-types.md` \xa7timestamp. |\n| `NEW_TIME(date, src_tz, dst_tz)`                       | `from_utc_timestamp(to_utc_timestamp(date, src_tz), dst_tz)` | Two-step conversion via UTC.                                                                                                                     |\n| `TO_TIMESTAMP_TZ(str, fmt)`                            | `to_timestamp(str, fmt)` + manual TZ note                    | DBSQL drops the per-value offset. Flag if the source consumer relies on the original TZ component.                                               |\n| `TO_DSINTERVAL('1 02:30:00')`                          | `INTERVAL 1 DAY 2 HOUR 30 MINUTE`                            | Use literal `INTERVAL` syntax. No string-to-interval parser in DBSQL.                                                                            |\n| `TO_YMINTERVAL('1-3')`                                 | `INTERVAL 1 YEAR 3 MONTH`                                    | Same.                                                                                                                                            |\n| `ORA_DST_AFFECTED`, `ORA_DST_CONVERT`, `ORA_DST_ERROR` | TODO                                                         | DST-affected detection helpers — no DBSQL equivalent. Usually safe to drop for migrated reports.                                                 |\n\n### SYSDATE type pitfall — example\n\nOracle `DATE` includes a time component (to seconds). Naively migrating `SYSDATE` calls can lose time when the target column is DBSQL `DATE`:\n\n```sql\n-- Oracle: both date and time stored\nINSERT INTO events (event_time) VALUES (SYSDATE);\n-- event_time = 2024-03-20 14:30:45\n\n-- DBSQL (CORRECT — column typed as TIMESTAMP)\nINSERT INTO my_catalog.my_schema.events (event_time) VALUES (CURRENT_TIMESTAMP);\n-- event_time = 2024-03-20 14:30:45.000000\n\n-- DBSQL (WRONG — column typed as DATE, time is lost)\nINSERT INTO my_catalog.my_schema.events (event_time) VALUES (CURRENT_DATE);\n-- event_time = 2024-03-20 (time dropped)\n```\n\nSee `03-data-types.md Oracle DATE` for the column-type mapping.\n\n### General scalar function mappings\n\n| Oracle                                         | DBSQL                                                                                    | Drift warning                                                                                                                                                                                                                                                                                                                           |\n| ---------------------------------------------- | ---------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `NVL(a, b)`                                    | `NVL(a, b)` or `COALESCE(a, b)`                                                          | Direct in DBSQL. Note: Oracle `NVL` always evaluates both args; `COALESCE` short-circuits. Semantically equivalent when b has no side effects.                                                                                                                                                                                          |\n| `NVL2(a, if_not_null, if_null)`                | `nvl2(a, if_not_null, if_null)`                                                          | Direct — DBSQL has native `nvl2(expr1, expr2, expr3)`. Do not rewrite as `IF`/`CASE WHEN ... IS NOT NULL`.                                                                                                                                                                                                                              |\n| `COALESCE(a, b, ...)`                          | `COALESCE(a, b, ...)`                                                                    | Direct.                                                                                                                                                                                                                                                                                                                                 |\n| `NULLIF(a, b)`                                 | `NULLIF(a, b)`                                                                           | Direct.                                                                                                                                                                                                                                                                                                                                 |\n| `DECODE(expr, s1, r1, s2, r2, def)`            | `decode(expr, s1, r1, s2, r2, def)`                                                      | Direct — DBSQL `decode()` preserves Oracle's NULL=NULL match semantics. Avoid the `CASE` rewrite (it does not match NULL keys). See `00-quirks.md DECODE`.                                                                                                                                                                              |\n| `GREATEST(a, b, ...)`                          | `GREATEST(a, b, ...)`                                                                    | Direct.                                                                                                                                                                                                                                                                                                                                 |\n| `LEAST(a, b, ...)`                             | `LEAST(a, b, ...)`                                                                       | Direct.                                                                                                                                                                                                                                                                                                                                 |\n| `SIGN(n)`                                      | `SIGN(n)`                                                                                | Direct.                                                                                                                                                                                                                                                                                                                                 |\n| `ABS(n)` / `CEIL(n)` / `FLOOR(n)`              | `ABS(n)` / `CEIL(n)` / `FLOOR(n)`                                                        | Direct.                                                                                                                                                                                                                                                                                                                                 |\n| `ROUND(n, d)`                                  | `ROUND(n, d)`                                                                            | Direct.                                                                                                                                                                                                                                                                                                                                 |\n| `TRUNC(n, d)` (numeric truncation)             | `CASE WHEN n >= 0 THEN floor(n * pow(10, d)) ELSE ceil(n * pow(10, d)) END / pow(10, d)` | **No direct numeric form.** DBSQL `trunc()` is **date-only** — passing a number raises a type error. The rewrite above truncates toward zero for both positive and negative `n`, matching Oracle. See `00-quirks.md TRUNC` for full discussion including negative `d`. For date truncation use `TRUNC(date, fmt)` (date section above). |\n| `MOD(a, b)`                                    | `MOD(a, b)` or `a % b`                                                                   | Direct. Oracle `MOD` returns Oracle-specific result for negative operands; DBSQL follows ANSI — usually same.                                                                                                                                                                                                                           |\n| `POWER(a, b)` / `EXP(n)` / `LN(n)` / `SQRT(n)` | same                                                                                     | Direct.                                                                                                                                                                                                                                                                                                                                 |\n| `LOG(base, x)`                                 | `LOG(base, x)`                                                                           | Direct — argument order `(base, x)` matches.                                                                                                                                                                                                                                                                                            |\n\n### Numeric — trigonometric and transcendental\n\n| Oracle                            | DBSQL                             | Notes                                                                                                                                                                                                  |\n| --------------------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `ACOS(n)` / `ASIN(n)` / `ATAN(n)` | `ACOS(n)` / `ASIN(n)` / `ATAN(n)` | Direct.                                                                                                                                                                                                |\n| `ATAN2(y, x)`                     | `ATAN2(y, x)`                     | Direct. Argument order matches.                                                                                                                                                                        |\n| `COS(n)` / `SIN(n)` / `TAN(n)`    | `COS(n)` / `SIN(n)` / `TAN(n)`    | Direct. Already in `00-quirks.md` \"Safe functions\" list.                                                                                                                                               |\n| `COSH(n)` / `SINH(n)` / `TANH(n)` | `COSH(n)` / `SINH(n)` / `TANH(n)` | Direct.                                                                                                                                                                                                |\n| `WIDTH_BUCKET(expr, lo, hi, n)`   | `WIDTH_BUCKET(expr, lo, hi, n)`   | Direct — same 4-arg signature, same edge-bucket assignment for out-of-range values.                                                                                                                    |\n| `NANVL(expr, replacement)`        | `nanvl(expr, replacement)`        | Direct — DBSQL has native `nanvl(expr1, expr2)` (returns `expr1` if not NaN, else `expr2`). Only meaningful for `BINARY_FLOAT` / `BINARY_DOUBLE` / `DOUBLE`.                                           |\n| `REMAINDER(a, b)`                 | No direct equivalent              | Oracle's `REMAINDER` uses round-half-to-even for the quotient. Rewrite as `a - ROUND(a / b) * b` with explicit half-even rounding if precision matters — otherwise `MOD` is the closest approximation. |\n\n### Numeric — bit operations\n\n| Oracle                    | DBSQL          | Notes                                                                                                                                                                |\n| ------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `BITAND(a, b)`            | `a & b`        | DBSQL exposes only the bitwise `&` operator — there is **no scalar `BITAND(a, b)` function** (the docs page 404s). Result type matches the wider input integer type. |\n| `BIN_TO_NUM(b1, b2, ...)` | manual rewrite | No DBSQL equivalent. Replace with explicit `b1 * 4 + b2 * 2 + b3` arithmetic, or pack and `CAST(... AS BIGINT)`.                                                     |\n\n### Aggregate — variance, stddev, shape\n\n| Oracle                                                                                                                                                    | DBSQL                                                                                                                 | Notes                                                                                                                                                                      |\n| --------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `STDDEV(x)`                                                                                                                                               | `STDDEV(x)`                                                                                                           | Sample stddev. Direct.                                                                                                                                                     |\n| `STDDEV_POP(x)` / `STDDEV_SAMP(x)`                                                                                                                        | `STDDEV_POP(x)` / `STDDEV_SAMP(x)`                                                                                    | Direct.                                                                                                                                                                    |\n| `VARIANCE(x)`                                                                                                                                             | `VARIANCE(x)`                                                                                                         | Direct (sample variance).                                                                                                                                                  |\n| `VAR_POP(x)` / `VAR_SAMP(x)`                                                                                                                              | `VAR_POP(x)` / `VAR_SAMP(x)`                                                                                          | Direct.                                                                                                                                                                    |\n| `KURTOSIS_POP(x)` / `KURTOSIS_SAMP(x)`                                                                                                                    | `KURTOSIS(x)`                                                                                                         | **Single function** — DBSQL does not distinguish population/sample. Drift: KURTOSIS_SAMP applies a `(n-1)`-style bias correction; verify if statistical exactness matters. |\n| `SKEWNESS_POP(x)` / `SKEWNESS_SAMP(x)`                                                                                                                    | `SKEWNESS(x)`                                                                                                         | Same caveat as above.                                                                                                                                                      |\n| `CORR(x, y)`                                                                                                                                              | `CORR(x, y)`                                                                                                          | Direct.                                                                                                                                                                    |\n| `CORR_S(x, y)` / `CORR_K(x, y)`                                                                                                                           | No direct equivalent                                                                                                  | Spearman / Kendall — implement with `RANK() OVER (...)` then `CORR`, or use a Python UDF.                                                                                  |\n| `COVAR_POP(x, y)` / `COVAR_SAMP(x, y)`                                                                                                                    | `COVAR_POP(x, y)` / `COVAR_SAMP(x, y)`                                                                                | Direct.                                                                                                                                                                    |\n| `MEDIAN(x)`                                                                                                                                               | `MEDIAN(x)` (DBR 16.2+) or `PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY x)`                                           | DBSQL also supports `PERCENTILE_APPROX(x, 0.5)` for large datasets.                                                                                                        |\n| `REGR_*` linear regression family (`REGR_SLOPE`, `REGR_INTERCEPT`, `REGR_R2`, `REGR_AVGX`, `REGR_AVGY`, `REGR_COUNT`, `REGR_SXX`, `REGR_SYY`, `REGR_SXY`) | `REGR_SLOPE`, `REGR_INTERCEPT`, `REGR_R2`, `REGR_AVGX`, `REGR_AVGY`, `REGR_COUNT`, `REGR_SXX`, `REGR_SYY`, `REGR_SXY` | All available in DBSQL with same signatures.                                                                                                                               |\n\n### Aggregate — approximate (`APPROX_*` family)\n\nOracle's `APPROX_*` functions come in two flavors: a one-shot form (`APPROX_COUNT_DISTINCT`) and a two-stage form for materializing intermediate state (`APPROX_COUNT_DISTINCT_DETAIL` / `APPROX_COUNT_DISTINCT_AGG`, `TO_APPROX_COUNT_DISTINCT`). The one-shot forms map; the two-stage forms have **no DBSQL equivalent** because Databricks does not expose a per-group HyperLogLog/quantile sketch as a column type.\n\n| Oracle                                                                 | DBSQL                                                     | Notes                                                                                                                                     |\n| ---------------------------------------------------------------------- | --------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |\n| `ANY_VALUE(x)`                                                         | `ANY_VALUE(x)`                                            | Direct. Returns a non-deterministic value from the group.                                                                                 |\n| `APPROX_COUNT(x)`                                                      | `COUNT(x)`                                                | Oracle `APPROX_COUNT` returns an approximate count; DBSQL has no approx variant for plain `COUNT`. Use exact `COUNT(x)` — usually fine.   |\n| `APPROX_COUNT_DISTINCT(x)`                                             | `APPROX_COUNT_DISTINCT(x)`                                | Direct.                                                                                                                                   |\n| `APPROX_COUNT_DISTINCT_DETAIL(x)` + `TO_APPROX_COUNT_DISTINCT(detail)` | No equivalent                                             | Oracle's two-stage HLL flow is not exposed in DBSQL. Either run `APPROX_COUNT_DISTINCT` per query, or use Python UDF over `datasketches`. |\n| `APPROX_COUNT_DISTINCT_AGG(detail)`                                    | No equivalent                                             | Same as above — sketch combination is not a SQL primitive in DBSQL.                                                                       |\n| `APPROX_MEDIAN(x)`                                                     | `APPROX_PERCENTILE(x, 0.5)`                               | Direct. `PERCENTILE_APPROX` is the Spark-style alias.                                                                                     |\n| `APPROX_PERCENTILE(x, p)` (and `WITHIN GROUP`)                         | `APPROX_PERCENTILE(x, p)`                                 | Direct. DBSQL supports an `accuracy` 3rd arg.                                                                                             |\n| `APPROX_PERCENTILE_DETAIL(x, p)` + `TO_APPROX_PERCENTILE(detail)`      | No equivalent                                             | Same two-stage limitation.                                                                                                                |\n| `APPROX_PERCENTILE_AGG(detail)`                                        | No equivalent                                             | Same.                                                                                                                                     |\n| `APPROX_RANK(...)`                                                     | `RANK() OVER (...)` or `APPROX_PERCENTILE` for cumulative | No direct approx-rank function; rewrite based on intent.                                                                                  |\n| `APPROX_SUM(x)`                                                        | `SUM(x)`                                                  | DBSQL has no approximate sum. Use exact `SUM` (the Oracle approx version exists for HLL streaming, rarely needed).                        |\n\n### Aggregate — bit, boolean, set, grouping\n\n| Oracle                                        | DBSQL                                                                                         | Notes                                                                                                                                                                                                                                                                                                                                                                                                                              |\n| --------------------------------------------- | --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `BIT_AND_AGG(x)`                              | `BIT_AND(x)`                                                                                  | Note name change (`_AGG` suffix dropped). Operates on integer bit patterns.                                                                                                                                                                                                                                                                                                                                                        |\n| `BIT_OR_AGG(x)`                               | `BIT_OR(x)`                                                                                   | Same.                                                                                                                                                                                                                                                                                                                                                                                                                              |\n| `BIT_XOR_AGG(x)`                              | `BIT_XOR(x)`                                                                                  | Same.                                                                                                                                                                                                                                                                                                                                                                                                                              |\n| `BOOLEAN_AND_AGG(x)`                          | `BOOL_AND(x)` or `EVERY(x)`                                                                   | DBSQL accepts both names. Returns true iff all non-null inputs are true.                                                                                                                                                                                                                                                                                                                                                           |\n| `BOOLEAN_OR_AGG(x)`                           | `BOOL_OR(x)` or `ANY(x)`                                                                      | Same.                                                                                                                                                                                                                                                                                                                                                                                                                              |\n| `EVERY(x)`                                    | `EVERY(x)` or `BOOL_AND(x)`                                                                   | Direct.                                                                                                                                                                                                                                                                                                                                                                                                                            |\n| `CHECKSUM(x)`                                 | `XXHASH64(x)` (aggregate via group) or `MD5(CONCAT_WS(',', collect_list(CAST(x AS STRING))))` | No native `CHECKSUM` aggregate. For change-detection use `XXHASH64` over a deterministic concat of the row.                                                                                                                                                                                                                                                                                                                        |\n| `COLLECT(x)`                                  | `COLLECT_LIST(x)` or `COLLECT_SET(x)`                                                         | `COLLECT` returns a nested table. DBSQL `COLLECT_LIST` preserves duplicates and order (unstable); `COLLECT_SET` deduplicates.                                                                                                                                                                                                                                                                                                      |\n| `GROUPING(col)`                               | `GROUPING(col)`                                                                               | Direct — used inside `ROLLUP` / `CUBE` / `GROUPING SETS` to detect aggregate rows.                                                                                                                                                                                                                                                                                                                                                 |\n| `GROUPING_ID(c1, c2, ...)`                    | `GROUPING_ID(c1, c2, ...)`                                                                    | Direct.                                                                                                                                                                                                                                                                                                                                                                                                                            |\n| `GROUP_ID()`                                  | No direct equivalent                                                                          | Oracle returns a duplicate-row distinguisher when `GROUPING SETS` produces identical groupings. Rewrite with explicit `GROUPING_ID` checks.                                                                                                                                                                                                                                                                                        |\n| `LISTAGG(x, ',') WITHIN GROUP (ORDER BY ...)` | `LISTAGG(x, ',') WITHIN GROUP (ORDER BY ...)` (DBR 16.4+, alias of `string_agg`)              | Direct translation. `ON OVERFLOW TRUNCATE` has no equivalent — DBSQL has no per-row size limit. **Do not** rewrite as `ARRAY_JOIN(COLLECT_LIST(...))` — `COLLECT_LIST` does not preserve order without an explicit window/sort, so the rewrite produces nondeterministic output and silently loses the source's `ORDER BY` semantics. See `02-select-dml.md` for fallback patterns when the target runtime is older than DBR 16.4. |\n\n### Window / analytic — ranking and offsets\n\nAll of the following exist in DBSQL with **identical syntax and semantics**. Translate as-is — no rewrite needed.\n\n| Oracle                                                             | DBSQL                                                          | Notes                                                                                                                                                           |\n| ------------------------------------------------------------------ | -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `RANK() OVER (...)`, `DENSE_RANK() OVER (...)`                     | `RANK() OVER (...)`, `DENSE_RANK() OVER (...)`                 | Direct.                                                                                                                                                         |\n| `ROW_NUMBER() OVER (...)`                                          | `ROW_NUMBER() OVER (...)`                                      | Direct.                                                                                                                                                         |\n| `NTILE(n) OVER (...)`                                              | `NTILE(n) OVER (...)`                                          | Direct.                                                                                                                                                         |\n| `LAG(expr, offset, default) OVER (...)`                            | `LAG(expr, offset, default) OVER (...)`                        | Direct. `IGNORE NULLS` / `RESPECT NULLS` modifier supported in both.                                                                                            |\n| `LEAD(expr, offset, default) OVER (...)`                           | `LEAD(expr, offset, default) OVER (...)`                       | Direct.                                                                                                                                                         |\n| `FIRST_VALUE(expr) OVER (...)` / `LAST_VALUE(expr) OVER (...)`     | `FIRST_VALUE(expr) OVER (...)` / `LAST_VALUE(expr) OVER (...)` | Direct. Default frame is `RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW` in both — `LAST_VALUE` without an explicit frame is a common bug in both dialects. |\n| `NTH_VALUE(expr, n) OVER (...)`                                    | `NTH_VALUE(expr, n) OVER (...)`                                | Direct. `FROM FIRST` / `FROM LAST` modifiers supported.                                                                                                         |\n| `PERCENT_RANK() OVER (...)`                                        | `PERCENT_RANK() OVER (...)`                                    | Direct.                                                                                                                                                         |\n| `CUME_DIST() OVER (...)`                                           | `CUME_DIST() OVER (...)`                                       | Direct.                                                                                                                                                         |\n| `PERCENTILE_CONT(p) WITHIN GROUP (ORDER BY x) OVER (...)`          | `PERCENTILE_CONT(p) WITHIN GROUP (ORDER BY x) OVER (...)`      | Direct, both as aggregate and as analytic.                                                                                                                      |\n| `PERCENTILE_DISC(p) WITHIN GROUP (ORDER BY x) OVER (...)`          | `PERCENTILE_DISC(p) WITHIN GROUP (ORDER BY x) OVER (...)`      | Direct.                                                                                                                                                         |\n| `RATIO_TO_REPORT(x) OVER (...)`                                    | `x / SUM(x) OVER (...)`                                        | Manual rewrite — no direct DBSQL function.                                                                                                                      |\n| `KEEP (DENSE_RANK FIRST/LAST ORDER BY ...)` modifier on aggregates | rewrite with subquery + `ROW_NUMBER`                           | No direct equivalent of the `FIRST` / `LAST` aggregate modifiers; rewrite as `(SELECT first ... FROM (...) WHERE rn = 1)`.                                      |\n\n### Statistical hypothesis tests — blockers\n\nThe Oracle `STATS_*` family runs hypothesis tests inside the database. **None have a DBSQL equivalent**. Output a TODO and recommend either:\n\n- A Python UDF that calls `scipy.stats` with the same test, returning the test statistic and p-value.\n- Move the test out of SQL and run it in a Databricks notebook with `pyspark.pandas` or a Pandas DataFrame.\n\n| Oracle                                                | DBSQL   | Action                                                                                                                    |\n| ----------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------- |\n| `STATS_BINOMIAL_TEST(...)`                            | TODO    | Use `scipy.stats.binom_test` in a UDF.                                                                                    |\n| `STATS_CROSSTAB(...)`                                 | TODO    | Build the contingency table with `GROUP BY` + `PIVOT`; run chi-square in a UDF.                                           |\n| `STATS_F_TEST(...)`                                   | TODO    | `scipy.stats.f_oneway` or manual F = var1 / var2 with `f.sf` for p-value.                                                 |\n| `STATS_KS_TEST(...)`                                  | TODO    | `scipy.stats.ks_2samp`.                                                                                                   |\n| `STATS_MODE(x)`                                       | rewrite | Use `MODE(x)` if available, otherwise `(SELECT x FROM (SELECT x, COUNT(*) c FROM t GROUP BY x) ORDER BY c DESC LIMIT 1)`. |\n| `STATS_MW_TEST(...)`                                  | TODO    | `scipy.stats.mannwhitneyu`.                                                                                               |\n| `STATS_ONE_WAY_ANOVA(...)`                            | TODO    | `scipy.stats.f_oneway`.                                                                                                   |\n| `STATS_T_TEST_ONE` / `_PAIRED` / `_INDEP` / `_INDEPU` | TODO    | `scipy.stats.ttest_1samp` / `ttest_rel` / `ttest_ind` (with `equal_var=False` for `_INDEPU`).                             |\n| `STATS_WSR_TEST(...)`                                 | TODO    | `scipy.stats.wilcoxon`.                                                                                                   |\n\n### Conversion / cast helpers\n\n| Oracle                                       | DBSQL                                                               | Notes                                                                                                            |\n| -------------------------------------------- | ------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |\n| `TO_BINARY_DOUBLE(expr)`                     | `CAST(expr AS DOUBLE)`                                              | Direct.                                                                                                          |\n| `TO_BINARY_FLOAT(expr)`                      | `CAST(expr AS FLOAT)`                                               | Direct.                                                                                                          |\n| `VALIDATE_CONVERSION(expr, type)`            | `try_cast(expr AS type) IS NOT NULL`                                | Returns 1/0 in Oracle; DBSQL boolean. Wrap in `CASE` if the call site expects an integer.                        |\n| `CAST(expr AS type)` / `TREAT(expr AS type)` | `CAST(expr AS type)`                                                | `TREAT` is for object subtypes (no DBSQL analogue). For ANSI casts, `CAST` matches.                              |\n| `HEXTORAW('A1B2')`                           | `unhex('A1B2')`                                                     | Returns BINARY.                                                                                                  |\n| `RAWTOHEX(raw)`                              | `hex(raw)`                                                          | Returns STRING (uppercase).                                                                                      |\n| `RAWTONHEX(raw)`                             | `hex(raw)`                                                          | Same — DBSQL has no NCHAR/NVARCHAR distinction.                                                                  |\n| `ASCIISTR(str)`                              | manual rewrite                                                      | Escapes non-ASCII to `\\uXXXX`. Use `regexp_replace` over chars or a Python UDF if the output format is required. |\n| `UNISTR('\\u00E9')`                           | `decode(unhex(replace(...)), 'UTF-8')` or string literal `'\\u00E9'` | Use a literal Unicode string in DBSQL where possible.                                                            |\n| `COMPOSE(str)` / `DECOMPOSE(str)`            | No direct equivalent                                                | Unicode normalization. Use a Python UDF over `unicodedata.normalize('NFC' / 'NFD', s)`.                          |\n\n### UUID family (Oracle 23ai)\n\n| Oracle           | DBSQL                                                                           | Notes                                                                       |\n| ---------------- | ------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |\n| `UUID()`         | `uuid()`                                                                        | Both return UUIDv4 as a 36-char hyphenated STRING.                          |\n| `IS_UUID(s)`     | `s rlike '^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-...'`                                  | No native predicate; use a regex or `try_cast` against UUID-shaped strings. |\n| `UUID_TO_RAW(s)` | `unhex(replace(s, '-', ''))`                                                    | Strips hyphens then converts hex.                                           |\n| `RAW_TO_UUID(b)` | `regexp_replace(hex(b), '^(.{8})(.{4})(.{4})(.{4})(.{12})$', '$1-$2-$3-$4-$5')` | Pads hex with hyphens to canonical UUID layout.                             |\n\n### Hash and inspection\n\n| Oracle                            | DBSQL                                           | Notes                                                                                                                                                               |\n| --------------------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `ORA_HASH(expr [, max [, seed]])` | `xxhash64(expr)` (mod the result)               | DBSQL has no `ORA_HASH` (RNG-style). For deterministic bucketing use `xxhash64` then `% max`. **Different hash algorithm — not bit-compatible** with Oracle output. |\n| `STANDARD_HASH(expr [, 'algo'])`  | `md5(expr)` / `sha1(expr)` / `sha2(expr, bits)` | Direct map per algorithm.                                                                                                                                           |\n| `VSIZE(expr)`                     | `length(cast(expr AS BINARY))`                  | Returns byte count.                                                                                                                                                 |\n| `DUMP(expr)`                      | No equivalent                                   | Internal byte dump — usually a debug-only call. Drop or replace with application-level inspection.                                                                  |\n\n### JSON construction (Oracle 23ai SQL/JSON)\n\nOracle 23ai introduced full SQL/JSON construction. DBSQL handles JSON via `from_json`, `to_json`, `get_json_object`, `json_tuple`, `named_struct`, and the array/struct types. Most Oracle SQL/JSON calls can be rewritten one-to-one.\n\n| Oracle                                          | DBSQL                                                                            | Notes                                                                                                                  |\n| ----------------------------------------------- | -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |\n| `JSON_OBJECT(KEY 'a' VALUE x, KEY 'b' VALUE y)` | `to_json(named_struct('a', x, 'b', y))`                                          | Returns STRING. For a `MAP`-shaped column, use `map('a', x, 'b', y)` and skip the JSON serialization.                  |\n| `JSON_ARRAY(x, y, z)`                           | `to_json(array(x, y, z))`                                                        | Direct.                                                                                                                |\n| `JSON_OBJECTAGG(k VALUE v)`                     | `to_json(map_from_arrays(collect_list(k), collect_list(v)))`                     | If keys may collide, prefer `map_from_entries(collect_list(struct(k, v)))` — last-write-wins on duplicates.            |\n| `JSON_ARRAYAGG(x ORDER BY ...)`                 | `to_json(sort_array(collect_list(x)))` or `to_json(array_sort(collect_list(x)))` | Add a window/CTE if the order key is independent of `x`.                                                               |\n| `JSON_VALUE(doc, '$.a.b')`                      | `get_json_object(doc, '$.a.b')`                                                  | Returns STRING. Cast as needed.                                                                                        |\n| `JSON_QUERY(doc, '$.a')`                        | `get_json_object(doc, '$.a')`                                                    | Both return the JSON-encoded fragment as a STRING.                                                                     |\n| `JSON_TABLE(doc, '$.items[*]' COLUMNS (...))`   | `from_json(doc, schema)` + `LATERAL VIEW explode`                                | Rewrite: parse with a known schema, then `explode` the array column.                                                   |\n| `JSON_EXISTS(doc, '$.a')`                       | `get_json_object(doc, '$.a') IS NOT NULL`                                        | Direct.                                                                                                                |\n| `JSON_SERIALIZE(doc)`                           | `to_json(from_json(doc, schema))`                                                | Round-trip via a parsed struct, or just keep the original STRING.                                                      |\n| `JSON_TRANSFORM(doc, ...)`                      | manual rewrite                                                                   | No DBSQL equivalent for in-place mutation. Parse → struct → `transform_*` → `to_json`.                                 |\n| `JSON_DATAGUIDE(col)`                           | TODO                                                                             | Schema-inference aggregate — DBSQL has no equivalent. Use `schema_of_json(first(col))` for a single-row schema sample. |\n| `JSON_SCALAR(x)`                                | `to_json(x)` for primitives                                                      | Wraps a scalar as a JSON value.                                                                                        |\n\n### String / regex extras\n\n`UPPER`, `LOWER`, `INITCAP`, `LTRIM`, `RTRIM`, `TRIM`, `LENGTH`, `REPLACE`, `TRANSLATE`, `LPAD`, `RPAD`, `INSTR`, `SUBSTR`, `CONCAT`, `REGEXP_REPLACE`, `REGEXP_SUBSTR`, `REGEXP_INSTR` are direct (already in `00-quirks.md` \"Safe functions\"). Beyond those:\n\n| Oracle                                    | DBSQL                                                                            | Notes                                                                                                                |\n| ----------------------------------------- | -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |\n| `REGEXP_COUNT(str, pattern)`              | `regexp_count(str, pattern)`                                                     | Direct — DBSQL has native `regexp_count(str, regex)` (DBR 11.3+). Returns the number of non-overlapping matches.     |\n| `SOUNDEX(str)`                            | `soundex(str)`                                                                   | Direct.                                                                                                              |\n| `CHR(n)` / `ASCII(c)`                     | `chr(n)` / `ascii(c)`                                                            | Direct.                                                                                                              |\n| `COLLATION(expr)` (function)              | No direct equivalent                                                             | DBSQL exposes the collation via column metadata only. Drop the call or hardcode to `'UTF8_BINARY'` / `'UTF8_LCASE'`. |\n| `NLSSORT(str [, 'NLS_SORT=...'])`         | `str COLLATE UTF8_LCASE` etc.                                                    | Use the `COLLATE` clause on the column or expression. See `03-data-types.md` \xa7collation.                             |\n| `NLS_INITCAP` / `NLS_LOWER` / `NLS_UPPER` | `INITCAP(... COLLATE ...)` / `LOWER(... COLLATE ...)` / `UPPER(... COLLATE ...)` | Apply the desired collation explicitly — DBSQL does not key case-folding off NLS settings.                           |\n\n### Identifier and environment\n\n| Oracle                                                                   | DBSQL                                                                          | Notes                                                                                                                                                                          |\n| ------------------------------------------------------------------------ | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `USER` / `USERNAME`                                                      | `current_user()`                                                               | Direct mapping for the session principal.                                                                                                                                      |\n| `UID`                                                                    | `xxhash64(current_user())` (best effort)                                       | Oracle returns a stable numeric user id. DBSQL has no equivalent. Hash the username for a deterministic int.                                                                   |\n| `SYS_CONTEXT('USERENV', 'attr')`                                         | `current_user()` / `current_database()` / `current_catalog()` / session config | Per-attribute mapping. `SESSION_USER` → `current_user()`; `DB_NAME` → `current_database()`; `CON_NAME` → `current_catalog()`. Most other attributes have no DBSQL equivalent.  |\n| `SYS_CONTEXT('USERENV', 'IP_ADDRESS')`                                   | TODO                                                                           | Not exposed in DBSQL. Drop or move to application layer.                                                                                                                       |\n| `SYS_TYPEID(obj)`                                                        | TODO                                                                           | Object-type subtype id — has no DBSQL equivalent (no object types).                                                                                                            |\n| `SYS_GUID()`                                                             | `uuid()`                                                                       | Oracle returns 32-char hex `RAW(16)`; DBSQL `uuid()` returns 36-char hyphenated string. If the downstream depends on the hex format, use `REPLACE(uuid(), '-', '')` + `UPPER`. |\n| `USERENV('SESSIONID')`                                                   | TODO                                                                           | Session id not exposed in DBSQL SQL. Emit a TODO.                                                                                                                              |\n| `ORA_INVOKING_USER` / `ORA_INVOKING_USERID`                              | `current_user()`                                                               | Definer-vs-invoker distinction is handled by `SQL SECURITY` clause on the routine, not by a function call.                                                                     |\n| `CON_DBID_TO_ID` / `CON_GUID_TO_ID` / `CON_NAME_TO_ID` / `CON_UID_TO_ID` | TODO                                                                           | Multitenant container helpers — no DBSQL equivalent.                                                                                                                           |\n\n### Collection sizing\n\n| Oracle                                                      | DBSQL                                         | Notes                                                                                                                   |\n| ----------------------------------------------------------- | --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |\n| `CARDINALITY(nested_table)`                                 | `size(array_col)` or `cardinality(array_col)` | DBSQL `cardinality` works on `ARRAY` and `MAP`; `NULL` input returns `-1`, not `NULL` — wrap with `coalesce` if needed. |\n| `POWERMULTISET(nt)` / `POWERMULTISET_BY_CARDINALITY(nt, n)` | TODO                                          | No DBSQL equivalent — Oracle nested-table set algebra is not expressible in DBSQL. Rewrite at app layer.                |\n| `SET(nt)`                                                   | `array_distinct(arr)`                         | Returns a deduplicated array.                                                                                           |\n\n### `SCN_TO_TIMESTAMP` / `TIMESTAMP_TO_SCN`\n\n| Oracle                  | DBSQL                     | Notes                                                                                                                               |\n| ----------------------- | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |\n| `SCN_TO_TIMESTAMP(scn)` | TODO — flag SCN reference | Oracle SCNs are an internal monotonic counter. Delta versions are similar in spirit but not 1:1. See `02-select-dml.md` \xa7Flashback. |\n| `TIMESTAMP_TO_SCN(ts)`  | TODO                      | Same.                                                                                                                               |\n\n---\n\n## Out of scope (Oracle-only feature areas)\n\nThese categories have **no DBSQL equivalent**. When the source uses any of them, emit a TODO that names the feature and recommend the Databricks alternative.\n\n| Oracle area                                  | Functions involved                                                                                                                                                                                                                                                    | Recommended Databricks alternative                                                                                                  |\n| -------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |\n| Calendar tables (23ai)                       | `CALENDAR`, `FISCAL`, `RETAIL`, `*_START_END`, `*_X_OF_Y`, `*_ADD_X_PERIODS`, `CALENDAR_SINCE`, `RETAIL_DAY_EXISTS`                                                                                                                                                   | Build a calendar dim table once and join — far more flexible.                                                                       |\n| Oracle Machine Learning for SQL              | `CLUSTER_DETAILS/DISTANCE/ID/PROBABILITY/SET`, `FEATURE_*`, `ORA_DM_PARTITION_NAME`, `PREDICTION*`                                                                                                                                                                    | MLflow + Spark MLlib or Mosaic AI Model Serving.                                                                                    |\n| AI Vector Search (23ai)                      | `VECTOR_DISTANCE`, `L1_DISTANCE`, `L2_DISTANCE`, `COSINE_DISTANCE`, `INNER_PRODUCT`, `TO_VECTOR`, `VECTOR`, `FROM_VECTOR`, `VECTOR_SERIALIZE`, `VECTOR_CHUNKS`, `VECTOR_DIMS`, `VECTOR_DIMENSION_COUNT`, `VECTOR_DIMENSION_FORMAT`, `VECTOR_EMBEDDING`, `VECTOR_NORM` | Databricks Vector Search index.                                                                                                     |\n| SQL Domains (23ai)                           | `DOMAIN_DISPLAY`, `DOMAIN_ORDER`, `DOMAIN_NAME`, `DOMAIN_CHECK`, `DOMAIN_CHECK_TYPE`                                                                                                                                                                                  | DBSQL has no domains. Encode the constraint inline or via a `CHECK` clause on each column.                                          |\n| MODEL clause                                 | `CV`, `ITERATION_NUMBER`, `PRESENTNNV`, `PRESENTV`, `PREVIOUS`                                                                                                                                                                                                        | Rewrite with window functions and self-joins.                                                                                       |\n| Object Reference                             | `DEREF`, `MAKE_REF`, `REF`, `REFTOHEX`, `VALUE`                                                                                                                                                                                                                       | DBSQL has no object types — flatten to STRUCT/ARRAY/MAP columns.                                                                    |\n| OLAP cubes                                   | `CUBE_TABLE`                                                                                                                                                                                                                                                          | Build via `GROUPING SETS` + materialized table.                                                                                     |\n| Data Cartridge                               | `DATAOBJ_TO_MAT_PARTITION`, `DATAOBJ_TO_PARTITION`                                                                                                                                                                                                                    | No equivalent.                                                                                                                      |\n| BFILE / external LOB                         | `BFILENAME`, `EMPTY_BLOB`, `EMPTY_CLOB`, `TO_BLOB(bfile)`, `TO_CLOB(bfile)`, `TO_LOB`                                                                                                                                                                                 | UC Volumes for file references; STRING/BINARY columns for in-row data.                                                              |\n| ROWID conversions                            | `ROWIDTOCHAR`, `ROWIDTONCHAR`, `CHARTOROWID`                                                                                                                                                                                                                          | DBSQL has no ROWID — Delta uses `_metadata.file_path` + `_metadata.row_index` for lineage.                                          |\n| XML SQL/XML construction (covered elsewhere) | `XMLCAST`, `XMLCOLATTVAL`, `XMLDIFF`, `XMLEXISTS`, `XMLISVALID`, `XMLPARSE`, `XMLPATCH`, `XMLPI`, `XMLQUERY`, `XMLSEQUENCE`, `XMLSERIALIZE`, `XMLTABLE`, `XMLTRANSFORM`, `SYS_XMLAGG`, `SYS_XMLGEN`, `SYS_DBURIGEN`, `EXISTSNODE`, `EXTRACTVALUE`, `DEPTH`, `PATH`    | See `02-select-dml.md` \xa7XML construction. Emit JSON in DBSQL; if real XML is required, manual string formatting + `xml_escape` UDF. |\n"}}]);
//# sourceMappingURL=https://sourcemaps.dev.databricks.com/monolith/static/js/99025.65a701271c.chunk.js.map