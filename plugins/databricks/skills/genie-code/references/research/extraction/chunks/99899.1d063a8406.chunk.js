"use strict";(self.webpackChunkdatabricks_monolith=self.webpackChunkdatabricks_monolith||[]).push([[99899],{599899:e=>{e.exports="# Data Type Mapping\n\nSource dialect type → DBSQL type mapping. Includes precision notes and behavioral differences.\n\n## ORACLE\n\n### Types with behavioral differences\n\n| Oracle                                                                       | DBSQL                                                          | Notes                                                                                                                                                                                                                                                                                                                                                                           |\n| ---------------------------------------------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `NUMBER` (no precision)                                                      | `DECIMAL(38, 10)`                                              | Oracle `NUMBER` with no precision stores up to 38 digits with arbitrary scale. DBSQL `DECIMAL` requires fixed precision/scale. `DECIMAL(38, 10)` is a safe default; tune scale if values are known to be integer-only or require more fractional digits.                                                                                                                        |\n| `NUMBER(p)` (integer)                                                        | `INT` / `BIGINT`                                               | `NUMBER(p)` with no scale maps by precision: p ≤ 9 → `INT`; 10 ≤ p ≤ 18 → `BIGINT`; p > 18 → `DECIMAL(p, 0)`                                                                                                                                                                                                                                                                    |\n| `NUMBER(p, s)`                                                               | `DECIMAL(p, s)`                                                | Direct. `p ≤ 38` in both dialects.                                                                                                                                                                                                                                                                                                                                              |\n| `NUMBER(*, s)`                                                               | `DECIMAL(38, s)`                                               | Wildcard precision: use max (38).                                                                                                                                                                                                                                                                                                                                               |\n| `INTEGER` / `INT`                                                            | `DECIMAL(38, 0)`                                               | Oracle `INTEGER` is a synonym for `NUMBER(38, 0)` — **not** 32-bit. For true integer semantics consider `INT` / `BIGINT`, but exact-size mapping requires `DECIMAL(38, 0)`.                                                                                                                                                                                                     |\n| `BINARY_FLOAT`                                                               | `FLOAT`                                                        | 32-bit IEEE 754                                                                                                                                                                                                                                                                                                                                                                 |\n| `BINARY_DOUBLE`                                                              | `DOUBLE`                                                       | 64-bit IEEE 754                                                                                                                                                                                                                                                                                                                                                                 |\n| `FLOAT(p)`                                                                   | `DOUBLE`                                                       | Oracle `FLOAT` is binary-precision `NUMBER`; `DOUBLE` is the closest drop-in.                                                                                                                                                                                                                                                                                                   |\n| `VARCHAR2(n)` / `NVARCHAR2(n)`                                               | `STRING`                                                       | Databricks is UTF-8 natively — no separate Unicode type. `STRING` has no length limit; if the source relied on Oracle's max-length enforcement to reject overlong values, add a `CHECK (LENGTH(col) <= n)` constraint. **`BYTE` vs `CHAR` length** — see \"VARCHAR2 length semantics\" below.                                                                                     |\n| `VARCHAR(n)` (Oracle synonym for `VARCHAR2`)                                 | `STRING`                                                       | Same mapping as `VARCHAR2`. The keyword exists in Oracle but is officially deprecated and aliased to `VARCHAR2`. DBSQL also accepts `VARCHAR` as a synonym for `STRING`.                                                                                                                                                                                                        |\n| `CHAR(n)` / `NCHAR(n)`                                                       | `STRING`                                                       | Oracle `CHAR(n)` is **blank-padded** to n characters (`'ab'` stored as `'ab        '`). DBSQL `STRING` is variable-length, no padding. Equality comparisons on padded values that worked in Oracle may fail in DBSQL. If the source depends on blank-padding behavior, add `RPAD(col, n)` during reads or on write. Same `BYTE` vs `CHAR` length-semantics rules as `VARCHAR2`. |\n| `DATE`                                                                       | `TIMESTAMP`                                                    | **CRITICAL**: Oracle `DATE` stores **both date and time** (to seconds resolution) — it is not the same as a date-only type. Map to `TIMESTAMP`, not `DATE`, to preserve the time component. Mapping to DBSQL `DATE` silently drops the time.                                                                                                                                    |\n| `TIMESTAMP`                                                                  | `TIMESTAMP`                                                    | Oracle `TIMESTAMP` has nanosecond resolution; DBSQL `TIMESTAMP` has microsecond. Sub-microsecond precision is lost.                                                                                                                                                                                                                                                             |\n| `TIMESTAMP(p)` for `p` ∈ `0..9`                                              | `TIMESTAMP`                                                    | DBSQL `TIMESTAMP` is fixed at microsecond precision (≈ `TIMESTAMP(6)`). `TIMESTAMP(0)` (whole seconds) round-trips losslessly; `TIMESTAMP(7)`–`TIMESTAMP(9)` lose sub-microsecond digits silently. If the application relied on truncation to a coarser precision, add an explicit `date_trunc('SECOND', col)` (or `'MILLISECOND'`) at the read site.                           |\n| `TIMESTAMP WITH TIME ZONE`                                                   | `TIMESTAMP`                                                    | **Timezone offset is lost**. Oracle stores the offset per value; DBSQL `TIMESTAMP` is a point in time with session-level timezone display. If per-row timezone matters, normalize to UTC on write (`CAST(col AT TIME ZONE 'UTC' AS TIMESTAMP)` in Oracle before export) or add a separate `STRING` column preserving the original zone.                                         |\n| `TIMESTAMP WITH LOCAL TIME ZONE`                                             | `TIMESTAMP`                                                    | DBSQL `TIMESTAMP` is the closest equivalent — both normalize to UTC storage and display in session zone. Safe mapping.                                                                                                                                                                                                                                                          |\n| `INTERVAL YEAR(p) TO MONTH`                                                  | `INTERVAL YEAR TO MONTH`                                       | Drop the leading-field precision `(p)`. DBSQL year-month intervals do not declare leading-field precision; behavior is identical for valid ranges. Out-of-range values (Oracle's `(p)` would have caught them) flow through silently.                                                                                                                                           |\n| `INTERVAL DAY(p) TO SECOND(s)`                                               | `INTERVAL DAY TO SECOND`                                       | Drop both `(p)` and `(s)`. DBSQL day-time intervals are fixed-precision; the Oracle fractional-second precision rounds to DBSQL's microsecond resolution.                                                                                                                                                                                                                       |\n| `JSON` (Oracle 21c+ native JSON type)                                        | `STRING` (with JSON functions) or `VARIANT`                    | Oracle's native binary `JSON` columns serialize to JSON text on export. Map to `STRING` and use DBSQL's `from_json` / `to_json` / `:` path access, or to `VARIANT` (DBR 16.0+) for schema-on-read. Drop the `OSON` storage hint.                                                                                                                                                |\n| `VECTOR` / `VECTOR(dim, float32 \\| float64 \\| int8 \\| binary)` (Oracle 23ai) | `ARRAY<FLOAT>` / `ARRAY<DOUBLE>` / `ARRAY<TINYINT>` / `BINARY` | Map by element type. For similarity search at scale, ingest into a Databricks Vector Search index; `ARRAY<FLOAT>` is fine for in-table dot-product / cosine math via `aggregate(zip_with(...), ...)`. Drop the `EMBEDDING` clause and any `VECTOR_DISTANCE` indexes — see `10-function-catalog.md` \xa7Out of scope.                                                               |\n| `ANYDATA` / `ANYTYPE` / `ANYDATASET`                                         | `VARIANT` (DBR 16.0+) or `STRING`                              | Heterogeneous-payload type with no compile-time shape. `VARIANT` covers schema-flexible JSON-like values; `STRING` works as a serialized fallback. The `ANYTYPE` introspection API has no DBSQL counterpart — flag with TODO if callers branched on `GETTYPENAME` / `GETTYPE`.                                                                                                  |\n| `URITYPE` / `HTTPURIType` / `XDBURIType` / `DBURIType`                       | `STRING`                                                       | URI wrappers with HTTP/XDB-fetch behavior. The string portion migrates; the fetch behavior does not. If the consumer dereferenced the URI inside SQL, refactor to a notebook + `requests`.                                                                                                                                                                                      |\n| `CLOB` / `NCLOB`                                                             | `STRING`                                                       | No size limit in DBSQL. Writes that relied on LOB-specific APIs need application-level changes.                                                                                                                                                                                                                                                                                 |\n| `BLOB`                                                                       | `BINARY`                                                       |                                                                                                                                                                                                                                                                                                                                                                                 |\n| `RAW(n)` / `LONG RAW`                                                        | `BINARY`                                                       | Oracle `RAW(n)` length limit is not enforced in DBSQL `BINARY`.                                                                                                                                                                                                                                                                                                                 |\n| `LONG`                                                                       | `STRING`                                                       | Deprecated in Oracle. Always `STRING` in DBSQL.                                                                                                                                                                                                                                                                                                                                 |\n| `ROWID` / `UROWID`                                                           | `STRING`                                                       | Oracle physical row address. DBSQL has no equivalent — column is meaningless post-migration. Drop the column unless it is used as an application-level identifier, in which case it must be replaced with a durable key during ETL.                                                                                                                                             |\n| `XMLTYPE`                                                                    | `STRING`                                                       | Parse with `xpath_string`, `xpath_int`, etc. For semi-structured data, consider `VARIANT` or `parse_json` if the XML can be converted to JSON during ETL.                                                                                                                                                                                                                       |\n| `BFILE`                                                                      | `STRING`                                                       | Oracle `BFILE` is a pointer to an external file. Migrate to a `STRING` path and load file contents via `COPY INTO` or `read_files`.                                                                                                                                                                                                                                             |\n| `SDO_GEOMETRY`                                                               | `GEOMETRY(srid)` or `STRING`                                   | Native `GEOMETRY` type available (DBSQL + DBR 17.1+, preview). Convert Oracle Spatial values via WKT: `SDO_UTIL.TO_WKTGEOMETRY(geom)` in Oracle during export, then `ST_GEOMFROMTEXT` in DBSQL to read. Use `GEOGRAPHY(4326)` if SRID is 4326 and geographic semantics are needed.                                                                                              |\n\n### Identical types (no translation)\n\n`DATE` (when mapped to `TIMESTAMP` — see above), `DECIMAL(p, s)`, `NUMERIC(p, s)`, `BOOLEAN` (Oracle 23c+)\n\n**Note**: Oracle added `BOOLEAN` in 23c. Pre-23c code typically uses `NUMBER(1)` or `CHAR(1)` as a boolean proxy — map those columns manually to `BOOLEAN` after inspecting values.\n\n### Empty string is NULL\n\nThis is covered in `00-quirks.md` but repeated here because it affects type mapping: Oracle stores empty string `''` as `NULL`. DBSQL stores empty string as a distinct value. If migrated `VARCHAR2` / `NVARCHAR2` columns may contain empty strings post-migration that did not exist in Oracle (they were NULL there), code checking `IS NULL` will miss them. Audit predicates on migrated string columns — convert `IS NULL` to `(IS NULL OR col = '')` if the application may populate empty strings.\n\n### `NUMBER` precision pitfalls\n\nOracle `NUMBER` accepts any numeric value without a declared precision, including floats up to 40 digits. DBSQL `DECIMAL(p, s)` rejects values exceeding the declared precision. Rows that inserted successfully in Oracle may raise `ARITHMETIC_OVERFLOW` in DBSQL.\n\nAudit query to run in Oracle before migration:\n\n```sql\n-- Find max precision actually used in a NUMBER column\nSELECT MAX(LENGTH(TRANSLATE(TO_CHAR(col), '0123456789', '0000000000')))\nFROM my_table;\n```\n\nIf the result exceeds 38 (or your declared DBSQL precision), the column must be migrated as `DOUBLE` (lossy) or the application must be changed to reject overlong values.\n\n### `NUMBER(p)` integer mapping by precision\n\n| Oracle precision          | DBSQL type      | Range                                   |\n| ------------------------- | --------------- | --------------------------------------- |\n| `NUMBER(1)`–`NUMBER(2)`   | `TINYINT`       | ⚠️ Only safe if values stay within \xb1127 |\n| `NUMBER(3)`–`NUMBER(4)`   | `SMALLINT`      | \xb132,767                                 |\n| `NUMBER(5)`–`NUMBER(9)`   | `INT`           | \xb12.1 billion                            |\n| `NUMBER(10)`–`NUMBER(18)` | `BIGINT`        | \xb19.2\xd710\xb9⁸                               |\n| `NUMBER(19)`–`NUMBER(38)` | `DECIMAL(p, 0)` | Beyond `BIGINT` range                   |\n\n`TINYINT` in Oracle (`NUMBER(1)`, `NUMBER(2)`) is **signed** in both dialects — no drift — but note that `NUMBER(3)` = up to 999 exceeds `TINYINT` range and must map to `SMALLINT`.\n\n### `DATE` with time component — example\n\n```sql\n-- Oracle — stores date + time to seconds\nCREATE TABLE orders (order_id NUMBER, placed_at DATE);\nINSERT INTO orders VALUES (1, TO_DATE('2024-03-20 14:30:00', 'YYYY-MM-DD HH24:MI:SS'));\n\n-- DBSQL (CORRECT — preserves time)\nCREATE TABLE my_catalog.my_schema.orders (order_id INT, placed_at TIMESTAMP);\n\n-- DBSQL (WRONG — silently drops the time)\nCREATE TABLE my_catalog.my_schema.orders (order_id INT, placed_at DATE);\n```\n\nOnly map to DBSQL `DATE` when you have explicitly verified the Oracle column only carries date values (e.g. `SELECT MAX(EXTRACT(HOUR FROM col) + EXTRACT(MINUTE FROM col) + EXTRACT(SECOND FROM col))` returns 0).\n\n### Decimal arithmetic precision\n\nOracle `NUMBER` is essentially unbounded (up to 38 significant digits with arbitrary scale, no operand-level type widening) and arithmetic results are **always exact**. Databricks `DECIMAL(p, s)` is bounded at `p ≤ 38`; arithmetic follows ANSI promotion rules, and when the result of a multiply/divide would exceed precision 38, the **scale is truncated** (with rounding) to fit. This produces silently different values from Oracle for the same inputs.\n\n**Promotion formulas** (DBSQL, ANSI SQL):\n\n| Operation                           | Result precision                          | Result scale          |\n| ----------------------------------- | ----------------------------------------- | --------------------- |\n| `DECIMAL(p1, s1) + DECIMAL(p2, s2)` | `max(p1 - s1, p2 - s2) + max(s1, s2) + 1` | `max(s1, s2)`         |\n| `DECIMAL(p1, s1) - DECIMAL(p2, s2)` | same as `+`                               | `max(s1, s2)`         |\n| `DECIMAL(p1, s1) * DECIMAL(p2, s2)` | `p1 + p2 + 1`                             | `s1 + s2`             |\n| `DECIMAL(p1, s1) / DECIMAL(p2, s2)` | `p1 - s1 + s2 + max(6, s1 + p2 + 1)`      | `max(6, s1 + p2 + 1)` |\n\nWhen the computed precision exceeds 38, DBSQL caps `p` at 38 and **reduces `s` to fit** (minimum scale of 6 is preserved for division). This is where Oracle code drifts:\n\n```sql\n-- DECIMAL(38, 10) / DECIMAL(38, 10): formula gives p = 38 - 10 + 10 + max(6, 10 + 38 + 1) = 87, s = 49.\n-- DBSQL caps: p = 38, s ≈ 6  (most fractional digits collapsed).\n-- Oracle: NUMBER / NUMBER returns exact result with up to 38 digits in whatever scale fits.\n\n-- Workaround: cast both operands DOWN to a smaller declared precision/scale before the\n-- operation, so the promoted result still fits in 38 digits without truncation.\nSELECT CAST(a AS DECIMAL(18, 4)) / CAST(b AS DECIMAL(18, 4)) FROM my_catalog.my_schema.t;\n```\n\n**IEEE 754 rounding drift on `BINARY_DOUBLE` / `BINARY_FLOAT` columns (FLOAT/DOUBLE in DBSQL):**\n\nIf the source uses Oracle `BINARY_FLOAT` / `BINARY_DOUBLE` (or maps `NUMBER` to DBSQL `FLOAT`/`DOUBLE` for performance), arithmetic follows IEEE 754 — the same input value can render differently across systems because of float-to-string rounding. The classic case:\n\n```sql\n-- Both Oracle BINARY_DOUBLE and DBSQL DOUBLE store 23.15 as the same IEEE 754 bit pattern.\n-- The displayed string differs because of rounding-on-display rules:\n--   Oracle TO_CHAR(CAST(23.15 AS BINARY_DOUBLE), 'FM999.99')  -> '23.15'   (round half to even)\n--   DBSQL  CAST(23.15 AS DOUBLE)                              -> 23.149999999999999 (full precision)\n-- Aggregations over many such values diverge in the last places.\n```\n\nMitigation: use `DECIMAL(p, s)` end-to-end for monetary or audit-grade arithmetic. Only use `FLOAT`/`DOUBLE` when the source explicitly used `BINARY_FLOAT`/`BINARY_DOUBLE` and the application is already tolerant of IEEE drift. **Never silently downgrade a `NUMBER` column to `DOUBLE` for \"performance\"** without flagging the precision change to the user.\n\n**Bottom line for translation**: prefer `DECIMAL` + explicit `CAST` to a narrower type before any divide. Flag any Oracle `NUMBER`-arithmetic chain wider than ~18 digits — the user must decide whether to accept truncation or to refactor.\n\n### Conversion functions\n\n| Oracle                                                   | DBSQL                                               | Notes                                                                                                                                                                                     |\n| -------------------------------------------------------- | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `CAST(expr AS type)`                                     | `CAST(expr AS type)`                                | Direct                                                                                                                                                                                    |\n| `TO_NUMBER(str)` / `TO_NUMBER(str, fmt)`                 | `CAST(str AS DECIMAL(p,s))` or `TRY_CAST`           | For simple numeric strings use `CAST` / `TRY_CAST`. Format-model conversions (e.g. `TO_NUMBER('1,234.56', '9,999.99')`) need manual rewrite with string cleanup.                          |\n| `TO_CHAR(num)` / `TO_CHAR(num, fmt)`                     | `CAST(num AS STRING)` / `FORMAT_NUMBER(num, 'fmt')` | Simple → `CAST`. Format models (`'999,999.99'`) → `FORMAT_NUMBER`. Oracle format models differ from DBSQL — see `10-function-catalog.md` \xa7Format model translation for date/time formats. |\n| `TO_DATE(str, fmt)`                                      | `TO_DATE(str, fmt)` or `TO_TIMESTAMP(str, fmt)`     | Format pattern differs — Oracle uses `MM/DD/YYYY HH24:MI:SS`, DBSQL uses `MM/dd/yyyy HH:mm:ss`. See `10-function-catalog.md` \xa7Format model translation.                                   |\n| `TO_TIMESTAMP(str, fmt)`                                 | `TO_TIMESTAMP(str, fmt)`                            | Same pattern differences as `TO_DATE`.                                                                                                                                                    |\n| `CAST(x AS type) DEFAULT val ON CONVERSION ERROR` (12c+) | `COALESCE(TRY_CAST(x AS type), val)`                | Oracle 12c+ safe-cast with default. Rewrite using `TRY_CAST` + `COALESCE`.                                                                                                                |\n\n> For TO_DATE / TO_CHAR date format model translation (Oracle `YYYY-MM-DD HH24:MI:SS` → DBSQL `yyyy-MM-dd HH:mm:ss`), see `references/oracle/10-function-catalog.md` \xa7Format model translation.\n\n### `NUMBER(1)` as boolean proxy\n\nPre-Oracle-23c code uses `NUMBER(1)` with `CHECK (col IN (0, 1))` as a boolean. Map the column to `BOOLEAN` during migration and translate predicates:\n\n```sql\n-- Oracle pre-23c\nCREATE TABLE users (id NUMBER, is_active NUMBER(1) CHECK (is_active IN (0, 1)));\nSELECT * FROM users WHERE is_active = 1;\n\n-- DBSQL\nCREATE TABLE my_catalog.my_schema.users (id BIGINT, is_active BOOLEAN);\nSELECT * FROM my_catalog.my_schema.users WHERE is_active = true;\n\n-- Migration cast: CASE WHEN col = 1 THEN true WHEN col = 0 THEN false ELSE NULL END\n```\n\n`CHAR(1)` with `Y`/`N` values is another common boolean proxy — translate similarly.\n\n### `VARCHAR2` length semantics — `BYTE` vs `CHAR`\n\nOracle `VARCHAR2(n)` and `CHAR(n)` accept an optional length-semantics qualifier:\n\n```sql\n-- Oracle\nCREATE TABLE t (\n    name_b   VARCHAR2(20 BYTE),  -- max 20 bytes (default in most installs)\n    name_c   VARCHAR2(20 CHAR)   -- max 20 characters\n);\n```\n\nFor multibyte characters (UTF-8: up to 4 bytes per character), `VARCHAR2(20 BYTE)` may store only 5 four-byte emoji characters, while `VARCHAR2(20 CHAR)` always stores 20 characters regardless of byte width. Which one was the default depends on the database's `NLS_LENGTH_SEMANTICS` setting.\n\n**DBSQL has no length cap on `STRING`**. Drop both qualifiers entirely:\n\n```sql\n-- DBSQL\nCREATE TABLE my_catalog.my_schema.t (\n    name_b STRING,\n    name_c STRING\n);\n```\n\nIf the original schema relied on the length cap to enforce a contract, encode it in DBSQL with a `CHECK` constraint:\n\n```sql\n-- DBSQL with explicit length enforcement\nCREATE TABLE my_catalog.my_schema.t (\n    name_b STRING CONSTRAINT name_b_byte_len CHECK (octet_length(name_b) <= 20),\n    name_c STRING CONSTRAINT name_c_char_len CHECK (length(name_c) <= 20)\n);\n```\n\n`octet_length` (bytes) and `length` (UTF-8 characters) preserve the semantic distinction.\n\n### `RAW(n)` and `LONG RAW` length\n\nSame drop-the-qualifier rule as above. DBSQL `BINARY` has no declared length; encode any contractual length cap as a `CHECK (octet_length(col) <= n)` constraint.\n\n### Object types — `CREATE TYPE`, `REF`, nested tables, VARRAY at SQL level\n\nOracle supports user-defined object types at the SQL level (not just inside PL/SQL packages — `11-user-packages.md` covers package-scoped types):\n\n```sql\n-- Oracle\nCREATE OR REPLACE TYPE address AS OBJECT (\n    street VARCHAR2(100),\n    city   VARCHAR2(50),\n    zip    VARCHAR2(10),\n    MEMBER FUNCTION format_oneline RETURN VARCHAR2\n);\n/\n\nCREATE OR REPLACE TYPE address_tab AS TABLE OF address;\n/\n\nCREATE TABLE customers (\n    id        NUMBER PRIMARY KEY,\n    home      address,\n    shipping  address_tab\n) NESTED TABLE shipping STORE AS customers_shipping_nt;\n\n-- Object table — every row is one address object\nCREATE TABLE address_book OF address;\n```\n\n**DBSQL has no user-defined object types, no member methods, no nested-table storage, no object tables, and no `REF`.** Translation strategies:\n\n| Oracle construct                                               | DBSQL translation                                                                                                                                                                                                                         |\n| -------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `CREATE TYPE addr AS OBJECT (a, b, c)` + column `home addr`    | Inline as `STRUCT<a: ..., b: ..., c: ...>` at every column site. The `CREATE TYPE` itself is dropped — DBSQL has no shared type registry.                                                                                                 |\n| `MEMBER FUNCTION m RETURN ...` inside the type                 | Promote to a top-level UDF taking the `STRUCT` as a parameter: `CREATE FUNCTION addr_format_oneline(a STRUCT<...>) RETURNS STRING RETURN ...`.                                                                                            |\n| `MAP MEMBER FUNCTION` / `ORDER MEMBER FUNCTION`                | Promote to a top-level UDF and call it explicitly in `ORDER BY` / `GROUP BY`. No automatic ordering inheritance in DBSQL.                                                                                                                 |\n| `CONSTRUCTOR FUNCTION ...`                                     | Drop. Build the `STRUCT` inline with `named_struct(...)` or struct literals.                                                                                                                                                              |\n| `CREATE TYPE ... UNDER <super>` (inheritance)                  | Flatten — DBSQL has no type inheritance. Choose a single struct shape that is the union of the super and sub fields, or split into separate columns / tables.                                                                             |\n| `CREATE TYPE addr_tab AS TABLE OF addr` + nested-table column  | `ARRAY<STRUCT<...>>` column. Drop the `NESTED TABLE ... STORE AS ...` clause entirely — DBSQL stores arrays inline in the row.                                                                                                            |\n| `CREATE TYPE addr_arr AS VARRAY(10) OF addr` + column          | `ARRAY<STRUCT<...>>`. Drop the `VARRAY(n)` cap; if the cap was contractual, add `CHECK (size(col) <= n)`. See `05-temp-tables-ctes.md` for the in-memory PL/SQL collection variant.                                                       |\n| `CREATE TABLE t OF addr` (object table)                        | `CREATE TABLE t (a ..., b ..., c ...)` — flatten to columns. The object-row identity (system-generated `OID`) has no DBSQL counterpart; if foreign keys referenced the OID via `REF`, switch to a surrogate `BIGINT` / `STRING UUID` key. |\n| `REF addr` column / `REF(o)` / `DEREF(r)` / `MAKE_REF(t, ...)` | TODO — drop the `REF` column. Replace with a `BIGINT` / `STRING` foreign-key column referencing the surrogate id of the object table after flattening. `DEREF` becomes a `JOIN`.                                                          |\n| `IS OF (TYPE sub_t)` / `TREAT(o AS sub_t)`                     | TODO — flatten the inheritance hierarchy first; the type-test / downcast operators have no DBSQL counterpart.                                                                                                                             |\n| `SQLDATA` / `CUSTOMDATUM` / `ORADATA` Java mappings            | Out of scope — drop. DBSQL has no JDBC custom-type API parallel.                                                                                                                                                                          |\n\n### PL/SQL-only numeric types\n\nThese types appear only in PL/SQL bodies (variables, parameters, return types) — not on table columns. Inside a procedure body, replace per the table:\n\n| Oracle PL/SQL type                          | DBSQL                               | Notes                                                                                                                                       |\n| ------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |\n| `PLS_INTEGER` / `BINARY_INTEGER`            | `INT`                               | Both are 32-bit signed integers in Oracle; `INT` matches.                                                                                   |\n| `SIMPLE_INTEGER`                            | `INT`                               | Same as `PLS_INTEGER` but disallows NULL and wraps on overflow. `INT` does not wrap — flag if the source relied on overflow-wrap semantics. |\n| `NATURAL` / `NATURALN` (`NATURAL` not null) | `INT` + `CHECK (col >= 0)`          | `NATURALN` adds NOT NULL. DBSQL has no subtype layer — encode via `CHECK`.                                                                  |\n| `POSITIVE` / `POSITIVEN`                    | `INT` + `CHECK (col >= 1)`          | Same idea.                                                                                                                                  |\n| `SIGNTYPE`                                  | `INT` + `CHECK (col IN (-1, 0, 1))` | Restricted to {−1, 0, 1}.                                                                                                                   |\n| `SIMPLE_FLOAT` / `SIMPLE_DOUBLE`            | `FLOAT` / `DOUBLE`                  | NULL-disallowed variants of `BINARY_FLOAT` / `BINARY_DOUBLE`. Map and add `NOT NULL` if the parameter / variable was guaranteed non-NULL.   |\n\n### PL/SQL anchored types — `%TYPE`, `%ROWTYPE`, `SUBTYPE`\n\nOracle PL/SQL lets variables and parameters anchor to a column or row shape, so the type follows DDL changes automatically:\n\n```sql\n-- Oracle\nDECLARE\n    v_id     users.id%TYPE;             -- inherits the column type\n    v_user   users%ROWTYPE;             -- inherits the row shape\n    SUBTYPE  positive_int IS INTEGER NOT NULL;\n    v_n      positive_int;\nBEGIN\n    SELECT * INTO v_user FROM users WHERE id = v_id;\nEND;\n/\n```\n\n**DBSQL has no anchored type system.** Resolution:\n\n| Oracle                                                 | DBSQL                                                                                                                                                                                                   |\n| ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `col%TYPE` (variable / parameter)                      | Resolve to the concrete column type at translation time. DBSQL `DECLARE v INT` (or whatever the column's type is). If the column type later changes, the migrated PL/SQL must be re-translated by hand. |\n| `tab%ROWTYPE`                                          | Refactor to either a `STRUCT<...>` with the explicit field list, or to individual scalar variables for each column.                                                                                     |\n| `cursor_name%ROWTYPE`                                  | Same — spell out the cursor's projection as a `STRUCT` or scalar variables.                                                                                                                             |\n| `SUBTYPE name IS base_type [NOT NULL] [RANGE lo..hi];` | Drop the `SUBTYPE` declaration. Substitute the base type at every reference site, and encode `NOT NULL` / `RANGE` constraints via `CHECK` (table) or `IF ... THEN SIGNAL` (procedure body).             |\n| `%CHARSET`                                             | Drop. DBSQL is UTF-8 only.                                                                                                                                                                              |\n\nSee also `04-procedures-variables.md` \xa7`%TYPE` / `%ROWTYPE` in function signatures for the parameter/return-type variant.\n\n### LOB storage clauses — `SECUREFILE`, `BASICFILE`, `LOB ... STORE AS`\n\nOracle DDL frequently includes LOB storage hints that have no DBSQL equivalent:\n\n```sql\n-- Oracle\nCREATE TABLE docs (\n    id   NUMBER,\n    body CLOB,\n    img  BLOB\n)\nLOB (body) STORE AS SECUREFILE doc_body_lob (\n    TABLESPACE lob_data\n    ENABLE STORAGE IN ROW\n    CHUNK 8192\n    NOCACHE\n    COMPRESS HIGH\n    DEDUPLICATE\n    ENCRYPT USING 'AES256'\n)\nLOB (img) STORE AS BASICFILE (DISABLE STORAGE IN ROW);\n```\n\nDrop the entire `LOB (...) STORE AS ...` clause set — Delta storage on the Lakehouse is fully managed and handles compression / encryption / chunking transparently. The migrated DDL is just:\n\n```sql\nCREATE TABLE my_catalog.my_schema.docs (id INT, body STRING, img BINARY);\n```\n\nIf the source table specified `ENCRYPT` because of compliance requirements, ensure the Unity Catalog metastore + cloud-object-store encryption is configured at the workspace level — the SQL-side `ENCRYPT` keyword has no per-column counterpart.\n\n### `JSON` type (Oracle 21c+ binary OSON)\n\nOracle's native `JSON` type is a binary representation (`OSON`). Migration paths:\n\n| Oracle DDL                                                         | DBSQL DDL                                                    | Notes                                                                                                                                      |\n| ------------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------ |\n| `body JSON`                                                        | `body STRING`                                                | Use DBSQL JSON functions (`from_json`, `to_json`, `:` path access, `get_json_object`).                                                     |\n| `body JSON` (DBR 16.0+ target, schema-flexible)                    | `body VARIANT`                                               | `VARIANT` preserves the JSON shape without flattening. Path access via `body:author.name`.                                                 |\n| `IS JSON` / `IS NOT JSON` constraint                               | `CHECK (try_parse_json(body) IS NOT NULL)`                   | Validates parse-ability. Stricter than Oracle's `LAX` mode; if the source relied on `STRICT`/`LAX`, document the change.                   |\n| `JSON_OBJECT_T` / `JSON_ARRAY_T` / `JSON_ELEMENT_T` PL/SQL types   | Inline `STRUCT<...>` or `MAP<STRING, STRING>`                | The PL/SQL JSON DOM has no DBSQL counterpart. Inside a procedure body, manipulate JSON via `from_json` / `to_json` or `VARIANT` operators. |\n| `OSON` / `BSON` / `JSON_TRANSFORM` PUT / REMOVE / RENAME operators | `to_json(named_struct(...))` rebuild or string-level rewrite | DBSQL has no in-place JSON mutation API. Read → modify in code → write back.                                                               |\n| `JSON_TABLE(...)` (covered in `02-select-dml.md`)                  | Lateral `from_json` + explode                                | See `02-select-dml.md` for the SQL-level JSON_TABLE rewrite.                                                                               |\n\n### Implicit conversion drift\n\nOracle is famously generous with implicit conversions; DBSQL ANSI SQL is stricter. Common drift cases:\n\n| Source Oracle pattern                                             | DBSQL behavior                                                                                                                                                                    | Required rewrite                                                                                                 |\n| ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |\n| `WHERE numeric_col = '1'` (string literal compared to NUMBER)     | Oracle implicitly converts `'1'` → `1`. DBSQL ANSI mode rejects with type-mismatch (`CANNOT_RESOLVE_FUNCTION`).                                                                   | Quote-strip: `WHERE numeric_col = 1` or `WHERE numeric_col = CAST('1' AS INT)`.                                  |\n| `WHERE date_col = '2024-01-01'` (string literal compared to DATE) | Oracle implicitly applies `NLS_DATE_FORMAT`. DBSQL parses ISO-8601 (`yyyy-MM-dd`) literals only.                                                                                  | Use a typed literal: `WHERE date_col = DATE '2024-01-01'`.                                                       |\n| `numeric_col \\|\\| 'abc'` (concat NUMBER with string)              | Oracle implicitly converts `numeric_col` to its `TO_CHAR` form. DBSQL `\\|\\|` (and `concat`) require explicit string operands; numeric operands cause type errors in strict modes. | `CAST(numeric_col AS STRING) \\|\\| 'abc'` or `concat(cast(numeric_col as string), 'abc')`.                        |\n| `INSERT INTO t (date_col) VALUES ('2024-01-01')`                  | Oracle converts. DBSQL requires the typed literal or `CAST`.                                                                                                                      | `VALUES (DATE '2024-01-01')`.                                                                                    |\n| `WHERE varchar_col > 100` (string compared to NUMBER)             | Oracle compares lexicographically as strings after implicit string→number on the literal — full of footguns.                                                                      | `WHERE varchar_col > '100'` (lexical) or `WHERE try_cast(varchar_col AS INT) > 100` (numeric).                   |\n| `SUM(varchar_col)`                                                | Oracle implicitly converts the column. DBSQL rejects.                                                                                                                             | `SUM(CAST(varchar_col AS DECIMAL(38, 10)))` — and verify all values are numeric, or use `try_cast` + `COALESCE`. |\n\n**General rule:** wherever a literal of one type is used in an expression with a column of another type, add an explicit `CAST` (or `TRY_CAST` for fail-soft conversion). Test predicates that previously relied on implicit conversion before deploying — silently-wrong query plans are the failure mode, not parse errors.\n\n### Collation and case-insensitive comparison\n\nOracle uses the `NLS_SORT` and `NLS_COMP` session parameters to control sort and equality comparison. The two most common Oracle patterns:\n\n```sql\n-- Oracle: per-session case-insensitive equality\nALTER SESSION SET NLS_COMP = 'LINGUISTIC';\nALTER SESSION SET NLS_SORT = 'BINARY_CI';   -- _CI = case-insensitive\n\n-- Oracle: per-call linguistic sort\nSELECT name FROM users ORDER BY NLSSORT(name, 'NLS_SORT=GERMAN_CI');\n```\n\nDBSQL uses **column-level collation** via the `COLLATE` clause (DBR 16.1+):\n\n| Oracle session setting / call                                 | DBSQL                                                                                       | Notes                                                                                                                                                                    |\n| ------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |\n| `NLS_SORT = 'BINARY'` (default)                               | `COLLATE UTF8_BINARY` (default)                                                             | No change needed — both default to byte-wise comparison.                                                                                                                 |\n| `NLS_SORT = 'BINARY_CI'`                                      | `name STRING COLLATE UTF8_LCASE` (column DDL) or `name COLLATE UTF8_LCASE` (per-expression) | Use column-level for table-wide case-insensitivity; per-expression for one-off comparisons.                                                                              |\n| `NLS_SORT = 'BINARY_AI'` (accent-insensitive)                 | TODO — `RDB_COLLATION` / `regexp_replace(name, '\\\\p{M}', '')` workaround                    | Accent-folding collations are limited in DBSQL. For Latin-script languages, normalize via `regexp_replace` to strip combining marks, or use a Unicode-normalization UDF. |\n| `NLS_SORT = 'GERMAN'` / `'FRENCH'` / `'SPANISH'` (linguistic) | per-locale collation with `COLLATE <locale>` (DBR 16.4+, e.g. `COLLATE de`)                 | Limited locales available. Check the supported-collation list; if the source locale is not present, flag with a TODO.                                                    |\n| `NLSSORT(col, 'NLS_SORT=...')` in `ORDER BY`                  | `ORDER BY col COLLATE <locale>` or `ORDER BY <UDF>(col)`                                    | If the locale is supported, direct rewrite. If not, add a normalization UDF and sort by its output.                                                                      |\n| `NLS_COMP = 'LINGUISTIC'` (session-wide)                      | column-level `COLLATE` on every relevant column                                             | DBSQL has no session-wide collation switch; collation is per-column or per-expression.                                                                                   |\n| `LIKE '%abc%' COLLATE NLS_SORT=BINARY_CI`                     | `name COLLATE UTF8_LCASE LIKE '%abc%'`                                                      | Apply the collation to the column expression, not the literal.                                                                                                           |\n\nIf the original code relied on the `NLS_COMP = 'LINGUISTIC'` session switch to make every comparison case-insensitive, the safe migration is to declare every relevant text column with `COLLATE UTF8_LCASE` so all comparisons inherit the new default. Adding `COLLATE UTF8_LCASE` to a column also makes equality joins, `IN`, `DISTINCT`, `GROUP BY`, and `LIKE` case-insensitive uniformly — confirm that is the intent before applying it broadly.\n\n### `DEFAULT` clause behavior\n\nOracle and DBSQL both support `DEFAULT <expr>` on column declarations, with two important drifts:\n\n| Oracle DDL                                     | DBSQL                                                 | Notes                                                                                                                                  |\n| ---------------------------------------------- | ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |\n| `col NUMBER DEFAULT 0`                         | `col INT DEFAULT 0`                                   | Direct.                                                                                                                                |\n| `col DATE DEFAULT SYSDATE`                     | `col TIMESTAMP DEFAULT current_timestamp()`           | Function-based defaults are supported; ensure the function exists in DBSQL.                                                            |\n| `col NUMBER DEFAULT ON NULL 0`                 | TODO — see `01-schema-ddl.md` \xa7`DEFAULT ON NULL`      | Oracle's `DEFAULT ON NULL` fires on explicit `NULL` writes; DBSQL `DEFAULT` fires only on column-omitted writes.                       |\n| `col NUMBER DEFAULT seq.NEXTVAL`               | `col INT GENERATED ALWAYS AS IDENTITY` or pre-compute | See `00-quirks.md` row about `NEXT VALUE FOR` inside `BEGIN ATOMIC` — the sequence reference cannot appear in atomic procedure bodies. |\n| `col VARCHAR2 DEFAULT USER`                    | `col STRING DEFAULT current_user()`                   | Direct rename of the function.                                                                                                         |\n| `col TIMESTAMP DEFAULT SYSDATE WITH TIME ZONE` | `col TIMESTAMP DEFAULT current_timestamp()`           | Drop `WITH TIME ZONE` — DBSQL is single-zone.                                                                                          |\n\n### Catalog of out-of-scope types\n\nThese Oracle types appear less frequently and have no standard DBSQL counterpart. When the source uses any of them, emit a TODO that names the type and recommends manual review.\n\n| Oracle type                                                                       | Recommended DBSQL handling                                                                             |\n| --------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |\n| `SDO_TOPO_GEOMETRY`, `SDO_GEORASTER`                                              | Same recommendation as `SDO_GEOMETRY` — convert via WKT export; topology / raster are out of scope.    |\n| `MDSYS.SDO_PC` (point cloud), `MDSYS.SDO_TIN` (TIN)                               | TODO — no DBSQL equivalent; rebuild as `BINARY` blobs and a notebook-side processor.                   |\n| `ORDIMAGE`, `ORDAUDIO`, `ORDVIDEO`, `ORDDOC` (Oracle Multimedia)                  | TODO — store the bytes as `BINARY`, lose the codec metadata, handle in a notebook.                     |\n| `URITYPE` family                                                                  | `STRING` (above).                                                                                      |\n| `ANYDATA` / `ANYTYPE` / `ANYDATASET`                                              | `VARIANT` or `STRING` (above).                                                                         |\n| `XMLTYPE` (in-place query / update functions)                                     | See `02-select-dml.md` \xa7XML construction for SELECT-side; updates require export → modify → re-import. |\n| `HTTPURIType` / `XDBURIType` / `DBURIType`                                        | `STRING`; the resolve-and-fetch behavior moves to a notebook.                                          |\n| `SYS.AnyData` collection columns                                                  | Refactor to per-payload columns or to `VARIANT`.                                                       |\n| `JSON_OBJECT_T` / `JSON_ARRAY_T` / `JSON_ELEMENT_T` (PL/SQL DOM)                  | Inside procedures, use DBSQL JSON functions on `STRING` / `VARIANT`.                                   |\n| Object types with `INSTANTIABLE` / `FINAL` / `OVERRIDING` / `MAP MEMBER FUNCTION` | Flatten — DBSQL has no inheritance / dispatch.                                                         |\n| Reference types (`REF addr`)                                                      | Surrogate FK — see \"Object types\" above.                                                               |\n| `Opaque` types (`SYS.AnyData`-backed user-defined opaque)                         | TODO — migrate the opaque-type implementation to a notebook.                                           |\n"}}]);
//# sourceMappingURL=https://sourcemaps.dev.databricks.com/monolith/static/js/99899.1d063a8406.chunk.js.map